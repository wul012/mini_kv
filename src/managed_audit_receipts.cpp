#include "minikv/managed_audit_receipts.hpp"

// Managed audit receipt implementations are split by boundary family:
// core adapter guards, adapter participation guards, and sandbox connection guards.
