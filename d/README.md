# mini-kv Archive Root d

Starting with mini-kv v122, new command-result screenshots and explanations should be written here:

```text
d/<version>/图片/
d/<version>/解释/说明.md
```

Current v129 evidence keeps the same layout and records the retention-check receipt plus the final configure/build/test/CLI evidence under `d/129/`.
Current v130 evidence keeps the same layout and records the disabled runtime shell non-participation receipt plus final verification evidence under `d/130/`.
Current v131 evidence keeps the same layout and records the disabled runtime shell candidate gate non-participation receipt plus final verification evidence under `d/131/`.
Current v132 evidence keeps the same layout and records the runtime shell decision record non-participation receipt plus final verification evidence under `d/132/`.
Current v133 evidence keeps the same layout and records the runtime shell post-decision plan intake non-participation receipt plus final verification evidence under `d/133/`.

Historical archive roots `a/`, `b/`, and `c/` remain in place and should not be moved just to rebalance folder size.
