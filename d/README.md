# mini-kv Archive Root d

Starting with mini-kv v122, new command-result screenshots and explanations should be written here:

```text
d/<version>/图片/
d/<version>/解释/说明.md
```

Historical archive roots `a/`, `b/`, and `c/` remain in place and should not be moved just to rebalance folder size.
