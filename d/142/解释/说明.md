# mini-kv v142 命令归档说明

本目录记录 mini-kv v142 `abort/rollback semantics contract` 非参与回执的最终命令证据。截图通过 Playwright MCP 生成；由于 MCP 阻止直接访问 `file://`，本次临时启动本地 HTTP server 仅服务 `d/142/_logs` 中的归档 HTML，截图完成后已按清理门停止。

## 截图

1. `01-cmake-configure.png`
   - 命令：`cmake -S . -B cmake-build-v142 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
   - 结果：配置成功，生成 `cmake-build-v142` 构建文件。
   - 意义：确认 v142 的 CMake 目标和新增源文件已进入构建图。

2. `02-cmake-build.png`
   - 命令：`cmake --build cmake-build-v142 --parallel 2`
   - 结果：全部目标构建成功，包括 `minikv_abort_rollback_semantics_contract_receipt_tests`。
   - 意义：确认 v142 新回执实现、SMOKEJSON 挂载和测试目标均可编译链接。

3. `03-targeted-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v142 -R "abort_rollback_semantics_contract_receipt_tests|no_network_safety_fixture_contract_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|smokejson_command_receipt_tests" --output-on-failure`
   - 结果：5/5 通过。
   - 意义：确认 v142 新回执、v141 历史回归、runtime smoke、release manifest 和 SMOKEJSON 聚合入口都保持一致。

4. `04-full-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v142 --output-on-failure`
   - 结果：69/69 通过。
   - 意义：确认本版没有破坏现有 KV、WAL、snapshot、TCP、fixture、restore 和长期证据链测试。

5. `05-cli-smoke.png`
   - 命令：`minikv_cli.exe`，输入 `SMOKEJSON`、`CHECKJSON SMOKEJSON`、`GET restore:real-read-token`、`EXIT`
   - 结果：`SMOKEJSON` 可解析并暴露 `credential_resolver_abort_rollback_semantics_contract_non_participation_receipt`，digest 为 `fnv1a64:639d716863136ad1`；`CHECKJSON SMOKEJSON` 保持只读；`GET restore:real-read-token` 返回 `(nil)`。
   - 意义：确认 CLI 真实入口能消费 v142 回执，且没有写入 restore token 或触发恢复/压缩/写命令。

6. `06-tcp-smoke.png`
   - 命令：启动 `minikv_server.exe 6512 127.0.0.1`，再用 `minikv_client.exe 127.0.0.1 6512 5000 --connect-retries 10 --retry-delay-ms 100` 读取 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`
   - 结果：TCP 真实链路读取到同一 v142 回执，`rollback_execution_allowed=false`、`mini_kv_write_command_allowed=false`、`order_authoritative=false`；`HEALTH` 为 OK，`GET restore:real-read-token` 返回 `(nil)`。
   - 意义：确认 server/client 路径也只是只读证据读取，验证后服务端已停止。

## 边界结论

v142 只回显 Node v326 abort/rollback semantics contract intake。mini-kv 不执行 rollback、deployment、Java SQL、runtime shell、mini-kv write command、`LOAD`、`COMPACT`、`RESTORE`、`SETNXEX`，也不成为 abort/rollback、audit 或 order authority。
