# mini-kv v143 command archive

本目录记录 mini-kv v143 的关键命令截图。v143 主题是 `credential_resolver_implementation_candidate_gate_input_hardening_non_participation_receipt`：只读回显 Node v329 input-hardening candidate gate decision，并稳定 mini-kv current receipt export，不打开 runtime shell、provider/client、network、Java write、mini-kv write/admin 或 `LOAD/COMPACT/RESTORE/SETNXEX`。

## 截图说明

1. `../图片/01-cmake-configure.png`
   - 命令：`cmake -S . -B cmake-build-v143 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
   - 结果：CMake configure/generate 成功，构建目录为 `cmake-build-v143`。
   - 意义：证明 v143 新增源码、测试目标和 CMake wiring 可被本机工具链识别。

2. `../图片/02-cmake-build.png`
   - 命令：`cmake --build cmake-build-v143 --parallel 2`
   - 结果：所有目标构建成功，包括 `minikv_input_hardening_candidate_gate_receipt_tests`。
   - 意义：证明 v143 新 receipt 源文件、SMOKEJSON 接入和测试代码均通过 C++ 编译链接。

3. `../图片/03-targeted-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v143 --output-on-failure -R "input_hardening_candidate_gate_receipt_tests|abort_rollback_semantics_contract_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|smokejson_command_receipt_tests"`
   - 结果：5/5 targeted tests passed。
   - 意义：重点验证 v143 standalone fixture、runtime smoke fixture、manifest、live SMOKEJSON、v142 rolling node_consumption 回归。

4. `../图片/04-full-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v143 --output-on-failure`
   - 结果：70/70 tests passed。
   - 意义：证明 v143 没有破坏 WAL、snapshot、TCP、restore fixtures、历史 credential resolver receipts 和 release manifest。

5. `../图片/05-cli-smoke.png`
   - 命令：通过 `minikv_cli.exe` 执行 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`。
   - 结果：CLI smoke 确认 v143 receipt 和 `require-input-export-hardening-before-disabled-runtime-design` 可读，restore token 返回 `(nil)`。
   - 意义：证明本机真实 CLI 路径暴露 v143 只读证据，同时没有写入 restore token。

6. `../图片/06-tcp-smoke.png`
   - 命令：临时启动 `minikv_server.exe 6513 127.0.0.1`，通过 `minikv_client.exe 127.0.0.1 6513 ...` 执行同一组读命令。
   - 结果：TCP smoke 确认 v143 receipt 和 candidate gate decision 可读，restore token 返回 `(nil)`，验证后服务端已停止。
   - 意义：证明真实 server/client 路径同样保持只读 evidence 语义。

## 清理记录

- Playwright MCP 已用于生成本轮截图。
- 截图生成用的临时 HTTP server 已停止。
- 临时 `_logs` 目录将在最终 cleanup gate 中删除。
