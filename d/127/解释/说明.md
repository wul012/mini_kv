# mini-kv v127 Command Archive

This version adds the `credential_resolver_disabled_fake_harness_non_participation_receipt` for the Node v288 disabled fake harness contract. It is a read-only receipt for Node v289 verification. It does not implement or run a fake harness runtime, does not instantiate real or fake resolver providers, does not read credential values, does not parse raw endpoint URLs, does not connect managed audit, and does not execute LOAD, COMPACT, RESTORE, or SETNXEX.

## 01-cmake-configure.png

- Command: `cmake -S . -B cmake-build-v127 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
- Result: configure and generation succeeded; build files were written to `cmake-build-v127`.
- Meaning: the new v127 source file, test target, and CMake registration are accepted by the local CLion/MinGW toolchain.

## 02-cmake-build.png

- Command: `cmake --build cmake-build-v127 --parallel 2`
- Result: full build succeeded, including `minikv_credential_resolver_disabled_fake_harness_non_participation_receipt_tests`.
- Meaning: the new receipt implementation, SMOKEJSON formatter exposure, fixtures, and tests compile together with the existing mini-kv targets.

## 03-targeted-tests.png

- Command: `ctest --test-dir cmake-build-v127 --output-on-failure -R "credential_resolver_disabled_fake_harness_non_participation_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|smokejson_command_receipt_tests"`
- Result: 4/4 targeted tests passed.
- Meaning: the standalone v127 receipt, runtime smoke fixture, release manifest exposure, and live SMOKEJSON receipt checks all protect the new Node v288 contract echo.

## 04-full-ctest.png

- Command: `ctest --test-dir cmake-build-v127 --output-on-failure`
- Result: 54/54 tests passed.
- Meaning: v127 did not regress existing WAL, snapshot, command, TCP, restore evidence, runtime evidence, release manifest, or credential resolver receipt behavior.

## 05-cli-smoke.png

- Command: `SMOKEJSON`, `CHECKJSON SMOKEJSON`, `GET restore:real-read-token`, `QUIT`
- Result: `SMOKEJSON` exposes `credential_resolver_disabled_fake_harness_non_participation_receipt`; `CHECKJSON SMOKEJSON` remains read-only and non-write; `GET restore:real-read-token` returns `(nil)`.
- Meaning: real CLI execution exposes the v127 receipt and keeps the fake harness, credential, endpoint, write, restore, compact, SETNXEX, managed audit, and auto-start boundaries closed.

## 06-line-counts.png

- Command: count C++ and header lines under `src/`, `include/`, and `tests/`.
- Result: the largest files remain `src/command.cpp` at 800 lines, `tests/command_tests.cpp` at 792 lines, `src/runtime_evidence_receipts.cpp` at 752 lines, `tests/release_verification_manifest_tests.cpp` at 749 lines, and `tests/runtime_smoke_evidence_tests.cpp` at 724 lines. The new v127 source file is 442 lines.
- Meaning: no file crossed 1000 lines in this version. The new receipt file is still small enough to keep as one contract-owned compilation unit.

## Process And Cleanup Notes

- No `minikv_server.exe` or other long-running service was started.
- CLI smoke used a short-lived `minikv_cli.exe` process and exited with `QUIT`.
- `cmake-build-v127` and `d/127/_logs` were temporary verification directories and should be removed before final handoff.
