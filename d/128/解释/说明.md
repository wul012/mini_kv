# mini-kv v128 Command Archive

This version adds the `credential_resolver_disabled_fake_harness_execution_denied_non_participation_receipt` for the Node v290 disabled fake harness execution-denied route preflight. It is a read-only receipt for Node v291 verification. It does not implement or run a fake harness runtime, does not instantiate real or fake providers or clients, does not read credential values, does not parse raw endpoint URLs, does not send HTTP/TCP, does not connect managed audit, does not write storage, approval ledger, or schema state, and does not execute LOAD, COMPACT, RESTORE, or SETNXEX.

## 01-cmake-configure.png

- Command: `cmake -S . -B cmake-build-v128 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
- Result: configure and generation succeeded; build files were written to `cmake-build-v128`.
- Meaning: the new v128 source file, test target, and CMake registration are accepted by the local CLion/MinGW toolchain.

## 02-cmake-build.png

- Command: `cmake --build cmake-build-v128 --parallel 2`
- Result: full build succeeded, including `minikv_credential_resolver_disabled_fake_harness_execution_denied_non_participation_receipt_tests`.
- Meaning: the new receipt implementation, SMOKEJSON formatter exposure, fixtures, and tests compile together with the existing mini-kv targets.

## 03-targeted-tests.png

- Command: `ctest --test-dir cmake-build-v128 --output-on-failure -R "credential_resolver_disabled_fake_harness_non_participation_receipt_tests|credential_resolver_disabled_fake_harness_execution_denied_non_participation_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests|smokejson_command_receipt_tests"`
- Result: 5/5 targeted tests passed.
- Meaning: the standalone v128 receipt, the historical v127 receipt after the diagnostics prepend, runtime smoke fixture, release manifest exposure, and live SMOKEJSON receipt checks all protect the Node v290 execution-denied echo.

## 04-full-ctest.png

- Command: `ctest --test-dir cmake-build-v128 --output-on-failure`
- Result: 55/55 tests passed.
- Meaning: v128 did not regress existing WAL, snapshot, command, TCP, restore evidence, runtime evidence, release manifest, or credential resolver receipt behavior.

## 05-cli-smoke.png

- Command: `SMOKEJSON`, `CHECKJSON SMOKEJSON`, `GET restore:real-read-token`, `QUIT`
- Result: `SMOKEJSON` exposes `credential_resolver_disabled_fake_harness_execution_denied_non_participation_receipt`; `CHECKJSON SMOKEJSON` remains read-only and non-write; `GET restore:real-read-token` returns `(nil)`.
- Meaning: real CLI execution exposes the v128 receipt with `receipt_digest=fnv1a64:d60057a3760cad62`, `preflight_digest=21ea6d7a6c641489048fc2540126e2f461909246fdd2129bcb0fc6d1ffbbf1cb`, source Node v289 digest `e5b596b65f68e753459be163f8a58d1f677098e64b3be01d4e83737ef8c4d94f`, 25 passed checks, 8 simulated attempts, 8 denied attempts, and 0 actual execution attempts.

## 06-line-counts.png

- Command: count C++ and header lines under `src/`, `include/`, and `tests/`.
- Result: the largest files are `src/command.cpp` at 800 lines, `tests/command_tests.cpp` at 792 lines, `tests/release_verification_manifest_tests.cpp` at 754 lines, `src/runtime_evidence_receipts.cpp` at 752 lines, and `tests/runtime_smoke_evidence_tests.cpp` at 729 lines. The new v128 source file is 423 lines.
- Meaning: no file crossed 1000 lines in this version. The new receipt file is small enough to keep as one contract-owned compilation unit, and the current large files remain below the earlier split threshold.

## Process And Cleanup Notes

- No `minikv_server.exe` or other long-running service was started.
- CLI smoke used a short-lived `minikv_cli.exe` process and exited with `QUIT`.
- `cmake-build-v128` and `d/128/_logs` were temporary verification directories and should be removed before final handoff.
