# Production Excellence Progress

Source plan: `D:\nodeproj\orderops-node\docs\plans\production-excellence-minikv-playbook.md`.

This file is the mini-kv local progress ledger. The Node playbook is read-only from this repo; mini-kv records milestone state here and keeps all edits inside `D:\C\mini-kv`.

## Deviations

- K0 keeps the configured `MINIKV_CURRENT_ARCHIVE_VERSION` default at `c/102/`. That value is not treated as the latest git tag; it is the frozen current-runtime fixture hint consumed by existing runtime receipts and tests. Moving it to CMake removes the hard-coded template value without changing evidence output or breaking downstream path/digest references.
- K1 starts with a diagnostic sanitizer lane instead of making sanitizer required in the same commit. The local CLion Windows MinGW toolchain does not ship `-lasan`/`-lubsan`; `MINIKV_SANITIZE=ON` therefore fails fast on that local toolchain with an explicit message, while the actual ASan/UBSan signal is collected by the Ubuntu CI job. Once that job is green, a follow-up version should remove `continue-on-error: true`.
- K2 also uses Ubuntu as the source of truth for coverage. The local CLion Windows MinGW toolchain can compile default Debug builds, but gcov instrumentation writes `.gcno` sidecar paths long enough to exceed the bundled Windows MinGW object-path limit in this repository. `MINIKV_COVERAGE=ON` therefore fails fast on Windows MinGW with an explicit message, while the Ubuntu coverage CI job builds, runs CTest, and publishes the core coverage report artifact.
- K2 v1612 proved the Ubuntu coverage build and full CTest path, but `gcovr 7.0` failed while parsing GCC/gcov negative branch hits in `src/command.cpp` (`branch 7 taken -2`). v1613 follows the tool's own recommendation and adds `--gcov-ignore-parse-errors negative_hits.warn_once_per_file`; this keeps the job strict for build/test/report generation while treating the known gcov negative-hit parser issue as a warning.
- K2 v1614 freezes the first green Ubuntu coverage artifact from GitHub Actions run `27408621850`: selected core modules report 2,292 total lines, 2,082 executed lines, and 90% line coverage. The enforced floor is `--fail-under-line 88`, exactly the measured baseline minus two points, and the next step is the Claude review checkpoint before K3.
- Stage 3 v1658 remeasures the current core set from green run `29095734744` at 2,345 lines / 2,122 executed / 90%. Because the completion brief requires a floor increase when the actual value is at least two points above the floor, the workflow and its contract tighten `--fail-under-line 88` to 90. The same version corrects the wildcard TCP bind default to loopback and makes E1-E10 evidence, the exact two program deviations, secret-pattern scan, source-size ratchet, and external-review status mechanical. Claude independently reviewed the track on 2026-07-11 and returned PASS while retaining the single-project plus contract-alignment maturity label.
- Post-K2 review advisory: Claude reviewed K2 as PASS on 2026-06-12 and flagged the GitHub Actions Node 20 runtime deprecation before GitHub's June 16, 2026 Node 24 enforcement. v1615 closes that advisory inside mini-kv by bumping `actions/checkout` and `actions/upload-artifact` to the latest GitHub release tags verified via `gh api`.
- K3 v1616 intentionally keeps the logger in mini-kv code, not in a third-party dependency. The only stdout/stderr behavior change is that server callback runtime logs now use stderr through the new logger; startup, WAL, metrics-file exports, command responses, and JSON evidence outputs remain on their existing channels.
- K4 v1617 expands the Ubuntu core coverage filter when command dispatch logic is split into new translation units. The floor remains `--fail-under-line 88`; the next Ubuntu coverage artifact should be treated as the refreshed command-split baseline, while the existing negative-hit parser workaround stays in place until the whole command-side split is reviewed.
- K4 v1618 keeps the command split contract-preserving: `src/command_dispatch.cpp` becomes a thin router, while string/expiry/persistence/introspection command families move to separate translation units. The coverage filter follows the new files so the K2 floor still protects moved command logic. K4 is not ready for planner review yet; the shard-readiness split remains the next required slice.
- K4 v1619 keeps the shard-readiness split contract-preserving: `src/shard_readiness.cpp` is only the public fixture-path shim, while SHARDJSON formatting, digest construction, and boundary/catalog helper rendering move into focused translation units. The required K4 split is now ready for planner review. `src/shard_readiness_boundary_field_names.cpp` is intentionally not converted in this slice: it is a large path-stable boundary-name registry with existing tests, and converting it into a different table representation would be a separate E9 maintenance migration that needs its own byte-for-byte fixture proof.
- K4 review PASS was confirmed on 2026-06-12. The program-end E9 gate should cite the accepted `src/shard_readiness_boundary_field_names.cpp` waiver. K5 v1620 re-tested the K2/K4 coverage advisory by removing the `gcovr --gcov-ignore-parse-errors negative_hits.warn_once_per_file` workaround in pushed commit `f35dd0d3`; GitHub Actions run `27428515080` failed in `ubuntu coverage` while generating the core report because `src/command_catalog.cpp` still produced `branch 4 taken -32`. Final v1620 restores and protects the workaround as a documented gcov/gcovr tool limitation.
- K5 v1620 adds warning-only archive retention measurement. Historical archive roots remain path-stable evidence; the new inventory index and script only measure, warn, and document forward policy. They do not move, delete, compress, or rewrite `a/` through `f/` or `fixtures/release/*.json`.
- K6 v1621 closeout was completed by Claude acting as executor (operating-model change: the user asked Claude to take over the three projects on 2026-06-14). Full local CTest is 337/338; the only failure, `cli_log_level_flag_smoke`, times out because its `add_test` runs `minikv_cli --log-level debug` with no stdin redirection — on Windows ctest does not feed EOF so the REPL blocks, whereas Linux CI connects stdin to /dev/null and it passes. The test runs the `minikv_cli` binary (command.cpp), not the `tcp_server.cpp` changed here, and was not recompiled; piping input to `minikv_cli --log-level debug` exits 0 in ~0s. Recorded as a Windows-local limitation; advisory: a future quick-win should redirect that smoke test's stdin for cross-platform robustness. Screenshots were substituted with text evidence (`f/1621/证据/full-ctest-summary.txt`) since the executor cannot capture CLI screenshots.
- K6 closeout repair v1622 closes the v1621 quality gaps: the changed C++ files are formatted with the CLion-bundled clang-format and pass `--dry-run --Werror`; the three Markdown EOF blank-line findings from `git show --check v1621` are removed; `cli_log_level_flag_smoke` now feeds `QUIT` through a CMake script instead of relying on platform-specific stdin behavior; focused CTest and full local CTest are both green (338/338).
- Post-K6 v1623 records a maintained Chinese project orientation guide under `项目通俗说明/` and protects it with `project_orientation_doc_contract`, a CMake-script CTest that reads the guide and asserts the mechanism diagrams, command flow, WAL, Snapshot, TCP/RESP, CHECKJSON, SMOKEJSON, and boundary-field explanations remain present. This is an onboarding/maintainability surface only: it does not change runtime commands, WAL/snapshot behavior, router authority, write authority, credential boundaries, managed-audit participation, or execution authority.
- Post-K6 v1624 turns the orientation guide's smallest command story into executable evidence with `project_orientation_examples_tests`. The test drives `SET order:100 paid`, `GET order:100`, `INFOJSON`, `CHECKJSON LOAD data/prod.snap`, and `SMOKEJSON` through the real `Store` + `CommandProcessor` path, then re-reads the key to prove `CHECKJSON` did not execute `LOAD`, replace the store, touch WAL, start services, route writes, or grant execution authority. The guide contract now also requires those examples to stay documented.
- Post-K6 v1625 opens the optional K4 receipts-consolidation stretch only at the design-gate level. The measured baseline is 28 `runtime_*_receipts.cpp` files / 788,811 bytes, including 26 credential-resolver files / 706,166 bytes. `docs/receipts-consolidation-note.md` defines an ordered zero-dependency builder, requires a fixture-subobject byte-parity harness before any migration, keeps committed fixtures and existing expectations immutable, and phases the work from parity infrastructure to the no-network/abort-rollback pair before broader families. No runtime formatter or fixture changes in this version.
- Post-K6 v1626 implements only the Slice 0 foundation from the v1625 receipts-consolidation design gate. It adds an internal ordered JSON builder and a fail-closed fixture subobject extractor, then freezes the v141 no-network and v142 abort/rollback nested receipt objects by byte length and FNV digest. The candidate parity state is intentionally explicit: abort/rollback already equals the current public formatter byte-for-byte, while no-network remains a known pre-migration drift input. No fixture, public formatter, runtime command, router authority, write path, WAL behavior, network behavior, credential boundary, or execution authority changes.
- Post-K6 v1627 follows the Slice 1 safety order by migrating abort/rollback before no-network. Abort/rollback had byte parity in v1626, so the formatter was moved onto the ordered JSON builder and re-proved against the frozen fixture subobject. no-network was not migrated: v1627 first diagnoses the drift and records it as one legacy apostrophe escaping difference only (`Node v323\u0027s` in the fixture versus `Node v323's` in the current formatter), with identical parsed values, identical field order/count, identical receipt digest `fnv1a64:3d8c483c93f8acf9`, and unchanged no-network/no-write/no-execution boundaries. The next no-network slice must decide explicitly whether to preserve the fixture byte spelling or treat the current formatter spelling as the canonical runtime surface; it must not silently normalize this drift.
- Post-K6 v1628 intentionally migrates no additional receipt formatter. It corrects the v1625 design note's false shrink metric by making line-count shrink a review metric rather than a stop-condition; v1627's +116 diff-line abort/rollback growth is therefore accepted only as an explained, byte-preserving builder-readability tradeoff, not as a precedent for noisy growth. The same slice drives no-network drift to ground in tests: both fixture/current objects have 118 top-level fields, the only raw-byte delta is the single `boundary` phrase `Node v323\u0027s` versus `Node v323's`, and receipt digest plus no-network/no-write/WAL/credential/execution boundaries remain unchanged. No no-network formatter migration occurs until the canonical surface decision is explicit.
- Post-K6 v1637 makes that canonical surface decision explicit and then migrates no-network under the named rule: the current runtime formatter spelling `Node v323's no-network fixture contract` is the canonical runtime surface, while the committed fixture spelling `Node v323\u0027s no-network fixture contract` remains frozen and is not rewritten. `runtime_receipt_json_builder_tests` now names the one allowed compatibility rule and fails if there is any second `\u0027`, field-count drift, digest drift, or no-network/no-write/WAL/credential/execution boundary drift. After this version, Slice 0 infrastructure is complete and two receipt formatters are on the ordered builder: abort/rollback and no-network.
- Post-K6 v1638 intentionally stops before the next formatter migration and creates the Slice 2 approval-contract baseline. The selected trio is signed-human approval artifact, credential-handle approval, and endpoint-handle allowlist approval; approval-prerequisite and human-approval-review shapes stay out of this slice. `runtime_receipt_json_builder_tests` proves credential-handle and endpoint-handle have exact fixture/formatter byte parity, while signed-human has exactly one already-existing `Node v314\u0027s` fixture spelling versus `Node v314's` runtime-surface spelling difference. That single difference is normalized by name and then must compare equal; all read-only/no-router/no-write/no-WAL/no-network/no-credential/no-execution boundary fragments remain required.
- Post-K6 v1639 migrates only the credential-handle approval contract receipt's closed-boundary flag block onto the ordered builder. This is not a full formatter-family migration: the legacy domain sections remain in place, while the repeated boolean tail is now an ordered `OrderedJsonField` list rendered by `json_object` and stripped back to the historical inline fragment. The v1638 byte-parity oracle proves the formatter output remains identical to the frozen fixture subobject, so the change reduces comma/order risk in the no-router/no-write/no-WAL/no-network/no-credential/no-execution field cluster without changing public evidence bytes.
- Post-K6 v1640 mirrors the v1639 boundary-flag pattern onto the endpoint-handle allowlist approval contract receipt. The migration is intentionally the same size: only the closed-boundary boolean cluster moves to ordered builder fields, while the domain sections and fixture remain unchanged. The v1638 byte-parity oracle proves endpoint-handle formatter output remains identical to the frozen fixture subobject, and the focused receipt test plus SMOKEJSON/runtime-smoke/release-manifest checks prove no raw-endpoint, network, router, write, WAL, credential, schema, restore/load/compact, auto-start, audit-authority, order-authority, or execution boundary expands.
- Post-K6 v1641 completes the first approval-contract boundary-flag pass by applying the same ordered-builder migration to the signed-human approval artifact contract receipt. This slice keeps the v1638 `Node v314\u0027s` versus `Node v314's` canonical runtime-surface rule single-use and explicit; it does not rewrite the frozen fixture and does not generalize the waiver to any other field. The legacy domain sections stay in place, while the repeated signed-artifact/credential/raw-endpoint/network/write/schema/restore/load/compact/auto-start/audit/order authority boolean tail becomes ordered `OrderedJsonField` data rendered by `json_object`.
- Post-K6 v1642 takes the next approval-contract slice only for credential-handle: the whole top-level formatter moves from one long string concatenation to ordered builder objects/arrays, while digest parts, fixture bytes, field order, compact JSON, SMOKEJSON, runtime smoke evidence, release manifest, and no-router/no-write/no-WAL/no-network/no-credential/no-execution boundaries stay unchanged. This makes credential-handle the first Slice 2 approval receipt with both top-level and boundary flag builder ownership.
- Post-K6 v1643 completes the endpoint-handle top-level builder slice without copying the credential field table: endpoint/raw-endpoint/allowlist authority fields remain locally owned, the old strip-and-splice boundary helper is removed, and the frozen fixture subobject remains byte-identical. The 503-to-558 line change is recorded as structured readability growth, not a shrink failure; no router/write/WAL/network/credential/execution boundary expands.
- Post-K6 v1644 completes the full Slice 2 approval trio by migrating signed-human top-level assembly and deleting its boundary strip-and-splice bridge. The named Node v314 fixture apostrophe rule remains single-use in the parity harness and absent from production builder code; signed-artifact/credential/raw-endpoint/network/write/execution authorities remain closed.
- Post-K6 v1645 freezes the next approval-lifecycle batch before production edits: four fixture sizes/versioned digests/field counts, three named apostrophe rules, and one separate post-echo `} },` runtime whitespace drift. A new CMake census gate fixes 28 sources/27 formatter owners/5 builder-backed/22 pending/1 named no-formatter waiver; post-echo is blocked from v1646 rather than granted a second parity waiver.
- Post-K6 v1646 migrates the three v1645 migration-ready approval-lifecycle formatters to ordered top-level builders, deduplicates their genuinely identical disabled-runtime-shell boundary suffix, and raises the mechanical census floor from 5 to 8. Frozen fixture bytes/tests stay unchanged; approval-required remains exact, the Node v306/v308 named apostrophe rules remain single-family, and post-echo remains untouched and blocked on its separate `} },` whitespace drift.
- Post-K6 v1647 resolves the isolated post-echo `} },` runtime defect before migration: the unchanged v1645 drift assertion first fails against the one-byte production correction, then the test is tightened to require compact whole-object parity with only the named Node v310 apostrophe rule. The full post-echo formatter moves to the ordered builder and census tightens 8→9; no fixture rewrite or generic whitespace waiver.
- Post-K6 v1648 pauses production migration to freeze exact fixture parity for five consecutive runtime-shell receipts: versioned sizes/digests/field counts, zero apostrophe waivers, whole-object equality, and core no-execution boundaries. Shared parity helpers move into a test-only header and the family gets its own CTest; census correctly remains 9/18/1.
- Post-K6 v1649 migrates the two runtime-shell chain-head formatters under the unchanged v1648 exact-byte gate, replaces duplicated boundary strings with two parameter-free family field appenders, and keeps the candidate-only three-field insertion explicit. Census tightens 9→11 with fixtures/digests/110+114 fields unchanged.
- Post-K6 v1650 completes the five-receipt runtime-shell family by migrating decision record, post-decision plan intake, and stop-or-prerequisite formatters under the unchanged v1648 exact-byte gate. Three duplicated boundary fragments now reuse the same parameter-free readiness/tail appenders while decision-specific fields stay local; census tightens 11→14 with fixtures/digests/113+113+115 fields unchanged.
- Post-K6 v1651 freezes an independent exact-byte oracle for four consecutive v117-v120 legacy resolver receipts before production migration. Fixture/runtime bytes, versioned digests, field counts, zero canonical waivers, receipt-only markers, and eight closed safety boundaries are fixed; production formatters stay untouched and census correctly remains 14/13/1.
- Post-K6 v1652 migrates the four v1651 legacy resolver owners to ordered top-level builders, names source/decision/plan/interface/fake-wiring objects, removes four fragment-based boundary tails, and raises the census floor 14→18. Exact bytes/digests/field counts remain unchanged; 329 lines of honest structured readability growth keep every file below 500 lines.
- Post-K6 v1653 freezes all 13 public formatters owned by the remaining nine files before any production migration. Eleven are raw exact; base-resolver has one named field-order rule and manual dry-run has one named three-field frozen-metadata rule. Both are single-use and canonicalize only to the already-public runtime surface; ten common read-only/no-store/no-write/no-execution/no-authority markers remain mandatory. Census honestly stays 18/9/1.
- Post-K6 v1654 migrates the base-resolver decision, disabled precheck, and test-only shell formatters under the unchanged v1653 oracle. A 37-field parameter-free ordered appender replaces the last shared brace-less boundary fragment; domain objects remain locally owned. Outputs stay 7944/10291/11201 bytes and 104/118/133 fields; census tightens 18→20 with all touched files below 800 lines.

## Progress

| Milestone | Version(s) | State | Evidence |
| --------- | ---------- | ----- | -------- |
| K0 | v1609 | completed | START_HERE refresh; CMake-configured archive hint with unchanged `c/102/` output; `metrics_file.hpp` pragma once; `.clang-format`; changed-file clang-format CI check; local configure/build/focused CTest/full CTest/CLI smoke |
| K1 | v1610-v1611 | completed | v1610 added `MINIKV_SANITIZE` and proved the Ubuntu sanitizer diagnostic job green; v1611 removes `continue-on-error: true`, tests that the sanitizer job cannot silently weaken, keeps Windows MinGW local sanitizer limitation as a fast configure failure, and preserves local default configure/build/334 CTest |
| K2 | v1612-v1614 | completed; planner reviewed 2026-06-12 | v1612 added `MINIKV_COVERAGE`, required Ubuntu coverage CI job, `gcovr` core-module report artifact, local Windows MinGW coverage fast-fail, and local default configure/build/334 CTest; v1613 handles the observed gcov negative-hit parse warning; v1614 freezes the green Ubuntu artifact baseline at 90% and enforces `--fail-under-line 88`; review PASS confirmed the baseline and floor from fresh CI evidence |
| K3 | v1615-v1616 | completed; planner reviewed 2026-06-12 | v1615 handles the post-K2 GitHub Actions Node 20 deprecation advisory before K3 logging starts; v1616 adds `include/minikv/log.hpp` + `src/log.cpp`, `--log-level` for `minikv_cli`/`minikv_server`, stderr runtime log routing, logger filtering tests, CLI/server smoke tests, real TCP smoke, and full 337 CTest; K3 review PASS confirmed zero-dependency logger behavior and stdout evidence boundaries |
| K4 | v1617-v1619 | completed; planner reviewed 2026-06-12 PASS | v1617 moves `CommandProcessor::execute_trimmed` into `src/command_dispatch.cpp`, moves shared parser/error helpers into `src/command_parse_helpers.*`, moves the shared WAL command/compaction gate into `src/command_wal_gate.*`, updates CMake, extends the coverage filter/CI contract test for the new core command files, and proves byte-equivalent intent with command fixture tests, full CTest, CLI smoke, and TCP smoke. v1618 continues the same K4 slice by moving string, expiry, persistence, and introspection command execution into `src/command_string_ops.cpp`, `src/command_expiry_ops.cpp`, `src/command_persistence_ops.cpp`, and `src/command_introspection_ops.cpp`; CMake and coverage gates include every new command-family file; command fixture tests, full 337-test CTest, format dry-run, CLI smoke, and real TCP smoke remain green. v1619 completes the required shard-readiness split by reducing `src/shard_readiness.cpp` to the public fixture-path shim and moving SHARDJSON formatting, digest construction, and boundary/catalog helper rendering into focused translation units; shard readiness focused tests, full CTest, format/diff hygiene, CLI smoke, and real TCP smoke remain green. Review accepted the `shard_readiness_boundary_field_names.cpp` E9 waiver and advised dropping the gcov workaround next |
| K5 | v1620 | completed | Adds `docs/archive-retention-index.md`, stdlib-only `scripts/archive_inventory.py`, and warning-only CI archive inventory; updates AGENTS forward archive policy; re-tests the old gcovr negative-hit parse workaround after K4 and retains it after run `27428515080` proves the parser issue still reproduces in `src/command_catalog.cpp`; local strict inventory, focused CI-contract CTest, full 337-test CTest, and whitespace checks pass |
| K6 | v1621-v1622 | completed; v1622 quality repair ready for program-end review | v1621 moves the long history out of `README.md` into `docs/CHANGELOG.md` (verified no test/src parses README), adds `docs/SECURITY.md`, `docs/TESTING.md`, `docs/CAPABILITY-SNAPSHOT.md`, documents the CMake-0.102.0-vs-tag-vNNNN dual version scheme, and adds opt-in `ServerOptions::client_idle_timeout` with idle/command-timeout connection reclaim plus `tests/tcp_server_timeout_limit_tests.cpp`. v1622 closes the v1621 red CI/quality gaps by clang-formatting the changed C++ files, removing Markdown EOF blank-line issues, replacing the Windows-local CLI stdin waiver with `cmake/run_cli_log_level_smoke.cmake` + `tests/cli_quit_input.txt`, and proving local configure/build, focused CTest, format dry-run, whitespace check, and full 338/338 CTest. Archive `f/1621/` and `f/1622/`. |
| Post-K6 orientation | v1623 | completed | Formalizes `项目通俗说明/mini-kv-项目机制与价值说明.md` plus a folder README, adds `cmake/check_project_orientation_doc.cmake` and the `project_orientation_doc_contract` CTest, updates project entry points, and proves focused CTest plus full local CTest 339/339. Remote CI is checked as part of normal version closeout. |
| Post-K6 executable orientation | v1624 | completed | Adds `tests/project_orientation_examples_tests.cpp`, registers it through CMake, extends the orientation guide and doc contract with executable examples, and proves focused CTest plus full local CTest 340/340. Remote CI is checked as part of normal version closeout. This is a maintainability/test hardening version only; no runtime command output, archive path, WAL/snapshot behavior, router authority, write authority, managed-audit participation, or execution authority is expanded. |
| K4 optional receipts stretch design gate | v1625 | completed | Adds `docs/receipts-consolidation-note.md` with the 28-file/788,811-byte runtime receipt census and 26-file/706,166-byte credential-resolver census; specifies an ordered zero-dependency JSON builder, fixture-subobject byte-parity harness, four migration slices, acceptance gates, and stop conditions; registers `receipts_consolidation_note_contract`. Runtime receipt source, fixtures, digests, public commands, WAL/snapshot, router/write/credential/network/execution boundaries remain unchanged. |
| K4 optional receipts stretch Slice 0 | v1626 | completed | Adds `src/runtime_receipt_json_builder.hpp/.cpp`, `tests/receipt_fixture_parity.hpp`, and `tests/runtime_receipt_json_builder_tests.cpp`; registers the new CTest; proves ordered JSON object/array assembly, escaping, raw nested values, fail-closed fixture extraction, frozen fixture subobject bytes, abort/rollback formatter parity, and no-network pre-migration drift visibility. Full local CTest is 342/342. No runtime receipt migration, fixture rewrite, command output change, router/write/WAL/network/credential/execution expansion. |
| K4 optional receipts stretch Slice 1a | v1627 | completed | Migrates `runtime_credential_resolver_abort_rollback_semantics_receipts.cpp` to the internal ordered JSON builder after the v1626 fixture parity proof; keeps the abort/rollback public formatter byte-for-byte equal to the frozen fixture subobject; adds no-network drift diagnosis to `runtime_receipt_json_builder_tests` before touching no-network; proves the drift is only the `\u0027` versus apostrophe spelling in the source phrase and not a semantic, field-order, digest, boundary, network, write, WAL, or execution change. Full local CTest is 342/342. |
| K4 optional receipts stretch Slice 1a quality correction | v1628 | completed | Corrects the false shrink metric in `docs/receipts-consolidation-note.md` and protects it with `receipts_consolidation_note_contract`: line-count shrink is a review metric, not a stop-condition. Hardens `runtime_receipt_json_builder_tests` so no-network drift is pinned to one `boundary` phrase and one apostrophe escaping spelling, with 118 top-level fields on both sides and unchanged receipt digest/read-only/no-network/no-write/WAL/credential/execution boundaries. No additional formatter migration, fixture rewrite, public command change, router/write/WAL/network/credential/execution expansion. |
| K4 optional receipts stretch canonical surface decision and no-network migration | v1637 | completed | Selects the current no-network runtime formatter apostrophe spelling as canonical runtime surface while keeping committed fixtures frozen; turns the one `Node v323\u0027s` vs `Node v323's` historical spelling difference into an explicit, single-use compatibility rule in `runtime_receipt_json_builder_tests`; migrates `runtime_credential_resolver_no_network_safety_fixture_receipts.cpp` onto the ordered builder under that rule, so abort/rollback and no-network are now both builder-backed. No fixture rewrite, public command change, router/write/WAL/network/credential/execution expansion. |
| K4 optional receipts stretch Slice 2 approval-contract baseline | v1638 | completed | Adds executable pre-migration parity coverage for `signed_human_approval_artifact`, `credential_handle_approval`, and `endpoint_handle_allowlist_approval` receipt formatters. Credential-handle and endpoint-handle are byte-for-byte equal to frozen fixture subobjects; signed-human is equal after exactly one named `Node v314\u0027s` to `Node v314's` canonical runtime-surface normalization. No formatter migration, fixture rewrite, public command change, router/write/WAL/network/credential/execution expansion. |
| K4 optional receipts stretch credential-handle boundary flag builder slice | v1639 | completed | Moves the credential-handle approval contract receipt's closed-boundary boolean field cluster from a hand-written JSON fragment to ordered builder fields rendered by `json_object`, then embedded at the same historical formatter position. Fixture/formatter byte parity, SMOKEJSON, runtime smoke evidence, and release manifest checks remain green. No fixture rewrite, endpoint/signed-human migration, public command change, router/write/WAL/network/credential/execution expansion. |
| K4 optional receipts stretch endpoint-handle boundary flag builder slice | v1640 | completed | Moves the endpoint-handle allowlist approval contract receipt's closed-boundary boolean field cluster from a hand-written JSON fragment to ordered builder fields rendered by `json_object`, then embedded at the same historical formatter position. Fixture/formatter byte parity, SMOKEJSON, runtime smoke evidence, release manifest checks, and full local CTest 344/344 remain green. No fixture rewrite, signed-human migration, public command change, router/write/WAL/network/credential/raw-endpoint/execution expansion. |
| K4 optional receipts stretch signed-human boundary flag builder slice | v1641 | completed | Moves the signed-human approval artifact contract receipt's closed-boundary boolean field cluster from a hand-written JSON fragment to ordered builder fields rendered by `json_object`, then embedded at the same historical formatter position. The v1638 single `Node v314\u0027s` fixture spelling normalization remains explicit and unbroadened; focused receipt/parity tests, SMOKEJSON, runtime smoke evidence, release manifest checks, and full local CTest 344/344 remain green. No fixture rewrite, full approval-family completion claim, public command change, router/write/WAL/network/credential/raw-endpoint/execution expansion. |
| K4 optional receipts stretch credential-handle top-level builder slice | v1642 | completed | Migrates the credential-handle approval contract receipt's full top-level JSON assembly to ordered builder objects/arrays after v1638-v1639 parity/boundary preparation. The formatter still matches the frozen fixture subobject byte-for-byte; focused receipt/parity tests, SMOKEJSON/runtime-smoke/release-manifest checks, CLI SMOKEJSON/CHECKJSON smoke, and full local CTest 344/344 are green. No fixture rewrite, digest drift, public command change, router/write/WAL/network/credential/raw-endpoint/execution expansion. |
| K4 optional receipts stretch endpoint-handle top-level builder slice | v1643 | completed | Migrates the endpoint-handle allowlist approval receipt's full top-level JSON assembly to ordered objects/arrays, directly appends its endpoint-specific closed-boundary field table, and removes the legacy strip-and-splice helper. Frozen fixture parity and five focused receipt surfaces are green; full CTest/CLI/archive gates are recorded in `f/1643/证据/summary.txt`. |
| K4 optional receipts stretch signed-human top-level builder slice | v1644 | completed | Migrates the signed-human approval artifact receipt's full top-level JSON assembly while preserving exactly one named Node v314 fixture spelling rule. Five focused receipt surfaces are green; final full CTest/CLI/archive evidence is recorded in `f/1644/证据/summary.txt`. Slice 2 approval trio top-level migration is complete. |
| K4 optional receipts stretch approval lifecycle baseline/census gate | v1645 | completed | Adds a four-receipt frozen parity baseline and `receipt_builder_census_contract`. Three candidates are migration-ready; post-echo is mechanically blocked by an additional one-byte runtime whitespace drift. Census floor is 5 builder-backed among 27 formatter owners plus one named no-formatter waiver. Evidence: `f/1645/证据/summary.txt`. |
| K4 optional receipts stretch approval lifecycle builder batch | v1646 | completed | Migrates approval-required boundary, approval-prerequisite artifact, and human approval review top-level formatters to ordered builders; extracts their shared disabled-runtime-shell boundary suffix; raises census to 8 builder-backed/19 pending/1 waiver. Focused 8/8, full CTest 345/345, CLI no-execution smoke and archive evidence: `f/1646/证据/summary.txt`. |
| K4 optional receipts stretch post-echo compact repair/builder slice | v1647 | completed | Removes the isolated runtime `} },` defect, records the old drift gate's expected failure, tightens post-echo to compact whole-object parity under its one named apostrophe rule, migrates the formatter to ordered objects/arrays, and raises census to 9/18/1. Evidence: `f/1647/证据/summary.txt`. |
| K4 optional receipts stretch runtime-shell family parity/test split | v1648 | completed | Adds an independent five-receipt exact-byte baseline with frozen sizes/digests/field counts and zero canonical exceptions; extracts reusable test-only parity helpers so the generic builder test does not absorb another domain family. Production formatters/fixtures stay unchanged and census remains 9/18/1. Evidence: `f/1648/证据/summary.txt`. |
| K4 optional receipts stretch runtime-shell chain-head builder batch | v1649 | completed | Migrates disabled-runtime-shell and candidate-gate full formatters to ordered objects/arrays, shares only their exact readiness/tail fields, preserves the candidate-only insertion point, and raises census to 11/16/1 under unchanged exact fixture parity. Evidence: `f/1649/证据/summary.txt`. |
| K4 optional receipts stretch runtime-shell decision-chain builder batch | v1650 | completed | Migrates decision record, post-decision plan intake, and stop-or-prerequisite full formatters to ordered objects/arrays; reuses only their exact family readiness/tail fields, preserves local decision/prerequisite ownership, and raises census to 14/13/1 under unchanged 14208/14084/16306-byte parity. Evidence: `f/1650/证据/summary.txt`. |
| K4 optional receipts stretch legacy resolver chain parity baseline | v1651 | completed | Freezes four v117-v120 fixture/runtime objects at 12335/10623/11114/15160 bytes, 95/99/101/106 fields and four versioned digests with exact equality and zero canonical waivers. Production formatters remain pending; census stays 14/13/1. Evidence: `f/1651/证据/summary.txt`. |
| K4 optional receipts stretch legacy resolver chain builder batch | v1652 | completed; Claude review checkpoint | Migrates all four v1651 owners to ordered builders with local exact boundary ownership; byte/digest/field parity remains fixed and the mechanical census reaches 18 builder-backed/9 pending/1 waiver. Evidence: `f/1652/证据/summary.txt`. |
| K4 optional receipts stretch remaining-owner migration baseline | v1653 | completed | Freezes 13 formatter outputs across the final 9 owners: 11 exact plus two single-use named historical compatibility rules; focused 19/19, full CTest 348/348, CLI 13/13 markers, census 18/9/1. Evidence: `f/1653/证据/summary.txt`. |
| K4 optional receipts stretch base-resolver builder batch | v1654 | completed | Migrates 3 formatter outputs across 2 owners, removes the shared comma-fragment boundary helper, preserves the v1653 byte/field/digest oracle and raises census to 20/7/1. Evidence: `f/1654/证据/summary.txt`. |
| K4 optional receipts stretch implementation-chain builder batch | v1655 | completed | Migrates five exact-parity implementation-chain formatter owners to ordered builders, preserves 16889/19163/13870/15264/21562-byte and 112/121/135/138/113-field oracles, and tightens census to 25/2/1. Evidence: `f/1655/证据/summary.txt`. |
| K4 receipts consolidation completion | v1656 | completed | Migrates the final five sandbox-chain formatters, preserves four exact outputs plus the one named manual-dry-run metadata rule, splits session support from the 700-line evidence owner, and closes the mechanical census at 27 builder-backed/0 pending/1 no-formatter waiver. Evidence: `f/1656/证据/summary.txt`. |
| Stage 2 project-document honesty refresh | v1657 | completed | Reconciles README/START_HERE/capability/changelog/archive/OSFS brief claims with the live tree, accounts for post-K5 OSFS and coursework-package growth, records the two-brief count deviation, and adds `project_docs_honesty_contract`; current default inventory is 349 registered tests. Evidence: `docs/project-docs-honesty-matrix.md` and `f/1657/证据/summary.txt`. |
| Stage 3 E1-E10 track evidence | v1658 | externally reviewed PASS; Stage-1 track closed 2026-07-11 | Adds `docs/minikv-track-final-evidence.md` and `minikv_track_final_evidence_contract`, tightens the coverage floor to 90%, changes the default TCP bind to loopback, constrains the program ledger to exactly two reviewable deviations, and raises the default inventory to 350 tests. Claude independently reproduced the claims; `f/1658/` remains the frozen pre-review archive and the maturity label remains bounded pending capstone. |
| Elegance E-M1 name census gate | v1659 | local green; remote Windows portability finding | Adds a standalone 40-character name census, freezes 740 long filenames and 883 public-header identifiers, and proves the red path with an injected 741st filename. Run `29176190797` passed six jobs but Windows exposed CRLF baseline parsing; v1660 owns the immediate repair. |
| Elegance E-M2 top-five path cleanup | v1660 | local green; remote format finding | Renames five compared-package intake-audit `.cpp` paths and tightens 740→735; fixes CRLF parsing. Run `29176799196` then correctly exposes inherited formatting debt because renamed files enter the changed-files gate; v1661 owns the immediate repair. |
| Elegance E-M4 bounded-program closeout | v1661 | externally reviewed PASS 2026-07-12 | Formats the five touched translation units, freezes 735 filenames (218 include/241 src/276 tests) and 883 identifiers, and records both CI repair chains; Claude reproduced the evidence and returned PASS with the cross-repo LF rule promoted. |
| Elegance Round 2 E2-M1 pin audit | v1662 | externally reviewed PASS 2026-07-12 | Mechanically ranks the combined top 10 as 6 filenames/4 public identifiers, verifies 9 public-header pins and 1 safe test path, fixes baseline/TSV to LF, and closes because `safe=1<5`; runs `29189480986` and `29189891795` prove all seven lanes, while 735/883 and runtime contracts stay fixed. Claude closes retro-elegance with no third round. |
| OSFS teaching clarity and resilience | v1663 | completed; CI run 29217994712 green | Adds eight mechanism-first Chinese tutorials, shared OSFS test support and a resilience target for fd reset/modes, group permissions, UFD growth/reuse, sparse zero-fill and inode-bitmap corruption. The later evidence-only commit `901408e8` preserves the pre-seeded-user disclosure and records completed FinalShell Step 08/09 real runs. |
| README visitor exhibit | v1664 | local closeout complete; final CI is version evidence | Reorders the GitHub landing page around sourced capabilities, architecture, runnable verification, evidence links and honest boundaries; pins 352 tests, the 90% core floor source, 27/0/1 receipts, 735/883 naming debt and the exact reviewed read-only capstone label. Focused 4/4, real CLI and full 352/352 are green; evidence: `f/1664/`. |
| Test architecture command decomposition | v1665 | local closeout green; remote CI is version evidence | Adds the 1,000-nonblank-line shrink-only test ratchet and manifest/entry/assertion census contract; preserves one shared command fixture and original execution order while splitting 3,805 nonblank lines into nine parts capped at 576. MinGW runtime helper, focused 7/7, full 353/353, CLI boundaries and archive budget are green; walkthrough `1033`, evidence `f/1665/`. |
| Test architecture zero-baseline closeout | v1666 | local closeout green; remote CI is version evidence | Splits 4,328 nonblank shard-readiness lines into five contract, two historical fixture and one real-command part capped at 906; preserves 8/3710/2 assertion census and both fixture/runtime contract calls. Clean build 1,030/1,030, full CTest 353/353, real CLI boundaries and zero product/fixture diff are green; walkthrough `1034`, evidence `f/1666/`. |
| CMake test target path budget | v1667 | local closeout green; final remote CI is version evidence | Keeps the v1662 source-path stop condition and full public CTest name, shortens only the warning-producing private target, freezes 277 historical long targets, and ratchets target-plus-source path score 200→197 with three injected red paths. Configure/build warning count 0, full CTest 353/353 and CLI boundaries are green; walkthrough `1035`, evidence `f/1667/`. |
| Receipt Boolean profile foundation | v1668 | local closeout green; remote CI is version evidence | Separates ordered Boolean boundary data from builder behavior, migrates four shared helper families, and reduces manual receipt field appends 1056→814. Exact 814/6 bidirectional ratchets, 813/815 red paths, full CTest 353/353 and real CLI boundaries are green without changing 27/0/1 ownership, fixtures, public output, or execution authority. Evidence: `f/1668/`; walkthrough `1036`. |
| Approval profile unification | v1669 | local focused gates green; remote CI is version evidence | Reuses six ordered boundary profiles across three approval owners while retaining local insertion points, reduces manual appends 814→613 and local digest wrappers 6→1, and proves both ratchets with 612/614 plus 0/2 red probes. Exact approval oracles and 57 receipt/release/runtime tests stay green without fixture, public-output or authority changes. Evidence: `f/1669/`; walkthrough `1037`. |
| Atomic file-write unification | v1670 | local closeout green; remote CI is version evidence | Replaces duplicate WAL/Snapshot temp guards, path factories, flush/close flow and platform replacement helpers with one private writer while preserving owner serialization, formats and APIs. Direct plus owner failure tests cover commit, abandonment, bad streams, parent paths, replacement failure and zero temp leakage; focused 17/17, full CTest 354/354 and real SAVE/LOAD/COMPACT/reopen smoke are green. Evidence: `f/1670/`; walkthrough `1038`. |
| Stable test link shards | v1671 | local closeout green; final remote CI is review checkpoint | Keeps 354 CTest names/order/properties and one-case-per-process isolation while 342 ordinary object targets link through 8 stable runners; coverage/sanitizer and explicit legacy builds retain one executable per test. Core-touch links 347→13, time 56.87→21.36 seconds and all test exe 345/3,983,351,060 bytes→11/415,219,566 bytes; full CTest 354/354, legacy/default parity and runner red paths are green. Evidence: `f/1671/`; walkthrough `1039`. |
| Evidence command table | v1672 | local closeout green; remote CI checked at v1673 start | Replaces 62 homogeneous no-argument evidence branches with one compile-time table, reducing `command.cpp` 732→258 nonblank lines while the 2,093,273-byte CLI oracle stays identical. Adds exact table/catalog/branch/size gates and keeps assertions active for bundled, legacy and standalone Release tests. Full CTest 354/354; evidence `f/1672/`, walkthrough `1040`. |
| Validated test manifest | v1673 | local closeout green; v1672 CI 29733051543 success | Moves all 342 ordinary linked/source-dir identities into one LF manifest (8 pre-smoke + 334 main), shrinking root CMake 2597→436 while the ordered 354-name SHA-256 stays `f4ecf104…3310e` in bundled and legacy configurations. Parser red paths and generated-order checks are mechanical; touched release test split 1090→820+280 with no waiver. Evidence `f/1673/`, walkthrough `1041`. |
