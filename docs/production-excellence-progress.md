# Production Excellence Progress

Source plan: `D:\nodeproj\orderops-node\docs\plans\production-excellence-minikv-playbook.md`.

This file is the mini-kv local progress ledger. The Node playbook is read-only from this repo; mini-kv records milestone state here and keeps all edits inside `D:\C\mini-kv`.

## Deviations

- K0 keeps the configured `MINIKV_CURRENT_ARCHIVE_VERSION` default at `c/102/`. That value is not treated as the latest git tag; it is the frozen current-runtime fixture hint consumed by existing runtime receipts and tests. Moving it to CMake removes the hard-coded template value without changing evidence output or breaking downstream path/digest references.
- K1 starts with a diagnostic sanitizer lane instead of making sanitizer required in the same commit. The local CLion Windows MinGW toolchain does not ship `-lasan`/`-lubsan`; `MINIKV_SANITIZE=ON` therefore fails fast on that local toolchain with an explicit message, while the actual ASan/UBSan signal is collected by the Ubuntu CI job. Once that job is green, a follow-up version should remove `continue-on-error: true`.
- K2 also uses Ubuntu as the source of truth for coverage. The local CLion Windows MinGW toolchain can compile default Debug builds, but gcov instrumentation writes `.gcno` sidecar paths long enough to exceed the bundled Windows MinGW object-path limit in this repository. `MINIKV_COVERAGE=ON` therefore fails fast on Windows MinGW with an explicit message, while the Ubuntu coverage CI job builds, runs CTest, and publishes the core coverage report artifact.
- K2 v1612 proved the Ubuntu coverage build and full CTest path, but `gcovr 7.0` failed while parsing GCC/gcov negative branch hits in `src/command.cpp` (`branch 7 taken -2`). v1613 follows the tool's own recommendation and adds `--gcov-ignore-parse-errors negative_hits.warn_once_per_file`; this keeps the job strict for build/test/report generation while treating the known gcov negative-hit parser issue as a warning.
- K2 v1614 freezes the first green Ubuntu coverage artifact from GitHub Actions run `27408621850`: selected core modules report 2,292 total lines, 2,082 executed lines, and 90% line coverage. The enforced floor is `--fail-under-line 88`, exactly the measured baseline minus two points, and the next step is the Claude review checkpoint before K3.
- Post-K2 review advisory: Claude reviewed K2 as PASS on 2026-06-12 and flagged the GitHub Actions Node 20 runtime deprecation before GitHub's June 16, 2026 Node 24 enforcement. v1615 closes that advisory inside mini-kv by bumping `actions/checkout` and `actions/upload-artifact` to the latest GitHub release tags verified via `gh api`.
- K3 v1616 intentionally keeps the logger in mini-kv code, not in a third-party dependency. The only stdout/stderr behavior change is that server callback runtime logs now use stderr through the new logger; startup, WAL, metrics-file exports, command responses, and JSON evidence outputs remain on their existing channels.
- K4 v1617 expands the Ubuntu core coverage filter when command dispatch logic is split into new translation units. The floor remains `--fail-under-line 88`; the next Ubuntu coverage artifact should be treated as the refreshed command-split baseline, while the existing negative-hit parser workaround stays in place until the whole command-side split is reviewed.
- K4 v1618 keeps the command split contract-preserving: `src/command_dispatch.cpp` becomes a thin router, while string/expiry/persistence/introspection command families move to separate translation units. The coverage filter follows the new files so the K2 floor still protects moved command logic. K4 is not ready for planner review yet; the shard-readiness split remains the next required slice.
- K4 v1619 keeps the shard-readiness split contract-preserving: `src/shard_readiness.cpp` is only the public fixture-path shim, while SHARDJSON formatting, digest construction, and boundary/catalog helper rendering move into focused translation units. The required K4 split is now ready for planner review. `src/shard_readiness_boundary_field_names.cpp` is intentionally not converted in this slice: it is a large path-stable boundary-name registry with existing tests, and converting it into a different table representation would be a separate E9 maintenance migration that needs its own byte-for-byte fixture proof.
- K4 review PASS was confirmed on 2026-06-12. The program-end E9 gate should cite the accepted `src/shard_readiness_boundary_field_names.cpp` waiver. K5 v1620 re-tested the K2/K4 coverage advisory by removing the `gcovr --gcov-ignore-parse-errors negative_hits.warn_once_per_file` workaround in pushed commit `f35dd0d3`; GitHub Actions run `27428515080` failed in `ubuntu coverage` while generating the core report because `src/command_catalog.cpp` still produced `branch 4 taken -32`. Final v1620 restores and protects the workaround as a documented gcov/gcovr tool limitation.
- K5 v1620 adds warning-only archive retention measurement. Historical archive roots remain path-stable evidence; the new inventory index and script only measure, warn, and document forward policy. They do not move, delete, compress, or rewrite `a/` through `f/` or `fixtures/release/*.json`.
- K6 v1621 closeout was completed by Claude acting as executor (operating-model change: the user asked Claude to take over the three projects on 2026-06-14). Full local CTest is 337/338; the only failure, `cli_log_level_flag_smoke`, times out because its `add_test` runs `minikv_cli --log-level debug` with no stdin redirection — on Windows ctest does not feed EOF so the REPL blocks, whereas Linux CI connects stdin to /dev/null and it passes. The test runs the `minikv_cli` binary (command.cpp), not the `tcp_server.cpp` changed here, and was not recompiled; piping input to `minikv_cli --log-level debug` exits 0 in ~0s. Recorded as a Windows-local limitation; advisory: a future quick-win should redirect that smoke test's stdin for cross-platform robustness. Screenshots were substituted with text evidence (`f/1621/证据/full-ctest-summary.txt`) since the executor cannot capture CLI screenshots.
- K6 closeout repair v1622 closes the v1621 quality gaps: the changed C++ files are formatted with the CLion-bundled clang-format and pass `--dry-run --Werror`; the three Markdown EOF blank-line findings from `git show --check v1621` are removed; `cli_log_level_flag_smoke` now feeds `QUIT` through a CMake script instead of relying on platform-specific stdin behavior; focused CTest and full local CTest are both green (338/338).

## Progress

| Milestone | Version(s) | State | Evidence |
| --------- | ---------- | ----- | -------- |
| K0 | v1609 | completed | START_HERE refresh; CMake-configured archive hint with unchanged `c/102/` output; `metrics_file.hpp` pragma once; `.clang-format`; changed-file clang-format CI check; local configure/build/focused CTest/full CTest/CLI smoke |
| K1 | v1610-v1611 | completed | v1610 added `MINIKV_SANITIZE` and proved the Ubuntu sanitizer diagnostic job green; v1611 removes `continue-on-error: true`, tests that the sanitizer job cannot silently weaken, keeps Windows MinGW local sanitizer limitation as a fast configure failure, and preserves local default configure/build/334 CTest |
| K2 | v1612-v1614 | completed; planner reviewed 2026-06-12 | v1612 added `MINIKV_COVERAGE`, required Ubuntu coverage CI job, `gcovr` core-module report artifact, local Windows MinGW coverage fast-fail, and local default configure/build/334 CTest; v1613 handles the observed gcov negative-hit parse warning; v1614 freezes the green Ubuntu artifact baseline at 90% and enforces `--fail-under-line 88`; review PASS confirmed the baseline and floor from fresh CI evidence |
| K3 | v1615-v1616 | completed; planner reviewed 2026-06-12 | v1615 handles the post-K2 GitHub Actions Node 20 deprecation advisory before K3 logging starts; v1616 adds `include/minikv/log.hpp` + `src/log.cpp`, `--log-level` for `minikv_cli`/`minikv_server`, stderr runtime log routing, logger filtering tests, CLI/server smoke tests, real TCP smoke, and full 337 CTest; K3 review PASS confirmed zero-dependency logger behavior and stdout evidence boundaries |
| K4 | v1617-v1619 | completed; planner reviewed 2026-06-12 PASS | v1617 moves `CommandProcessor::execute_trimmed` into `src/command_dispatch.cpp`, moves shared parser/error helpers into `src/command_parse_helpers.*`, moves the shared WAL command/compaction gate into `src/command_wal_gate.*`, updates CMake, extends the coverage filter/CI contract test for the new core command files, and proves byte-equivalent intent with command fixture tests, full CTest, CLI smoke, and TCP smoke. v1618 continues the same K4 slice by moving string, expiry, persistence, and introspection command execution into `src/command_string_ops.cpp`, `src/command_expiry_ops.cpp`, `src/command_persistence_ops.cpp`, and `src/command_introspection_ops.cpp`; CMake and coverage gates include every new command-family file; command fixture tests, full 337-test CTest, format dry-run, CLI smoke, and real TCP smoke remain green. v1619 completes the required shard-readiness split by reducing `src/shard_readiness.cpp` to the public fixture-path shim and moving SHARDJSON formatting, digest construction, and boundary/catalog helper rendering into focused translation units; shard readiness focused tests, full CTest, format/diff hygiene, CLI smoke, and real TCP smoke remain green. Review accepted the `shard_readiness_boundary_field_names.cpp` E9 waiver and advised dropping the gcov workaround next |
| K5 | v1620 | completed | Adds `docs/archive-retention-index.md`, stdlib-only `scripts/archive_inventory.py`, and warning-only CI archive inventory; updates AGENTS forward archive policy; re-tests the old gcovr negative-hit parse workaround after K4 and retains it after run `27428515080` proves the parser issue still reproduces in `src/command_catalog.cpp`; local strict inventory, focused CI-contract CTest, full 337-test CTest, and whitespace checks pass |
| K6 | v1621-v1622 | completed; v1622 quality repair ready for program-end review | v1621 moves the long history out of `README.md` into `docs/CHANGELOG.md` (verified no test/src parses README), adds `docs/SECURITY.md`, `docs/TESTING.md`, `docs/CAPABILITY-SNAPSHOT.md`, documents the CMake-0.102.0-vs-tag-vNNNN dual version scheme, and adds opt-in `ServerOptions::client_idle_timeout` with idle/command-timeout connection reclaim plus `tests/tcp_server_timeout_limit_tests.cpp`. v1622 closes the v1621 red CI/quality gaps by clang-formatting the changed C++ files, removing Markdown EOF blank-line issues, replacing the Windows-local CLI stdin waiver with `cmake/run_cli_log_level_smoke.cmake` + `tests/cli_quit_input.txt`, and proving local configure/build, focused CTest, format dry-run, whitespace check, and full 338/338 CTest. Archive `f/1621/` and `f/1622/`. |
