# v791 command archive explanation

This directory records key command evidence for v791 `SHARDROUTEVALUESUPPLYSIGNEDAPPROVALCAPTUREARTIFACTDRAFTTEXTPACKAGEREVIEWPREFLIGHTJSON` at `route-preview-value-supply-signed-approval-capture-artifact-draft-text-package-review-preflight-template-digest-binding-criterion`. PNG files are rendered terminal transcripts because this verification target is command output, not a browser page.

- `01-cmake-build.png`: CMake incremental build for the current build tree using CLion bundled CMake and MinGW.
- `02-focused-ctest.png`: focused CTest for text package review preflight, instruction preflight, command catalog, line editor, SHARDJSON, artifact preflight, and authoring readiness compatibility.
- `03-full-ctest.png`: full repository CTest.
- `04-cli-smoke.png`: CLI smoke for the new command and SHARDJSON; confirms v791, no draft package parsing/acceptance, no grant, no write routing, no WAL touch, and no execution.
- `05-tcp-smoke.png`: loopback TCP smoke through an owned `minikv_server.exe` on `127.0.0.1:17791` and `minikv_client.exe`; the owned server was stopped after validation.
- `06-git-diff-check.png`: `git diff --check` quality gate summary after source, fixture, docs, archive, and walkthrough updates.

Files kept: final archive screenshots under `e/791/图片/`, this explanation under `e/791/解释/`, the frozen fixture, rolling fixture, source/tests/docs/walkthrough changes.

Files deleted later: temporary command transcript logs used only to render PNG evidence.

Processes stopped: the owned `minikv_server.exe` process used for TCP smoke.
