# mini-kv v920 command archive

This archive records the final verification evidence for mini-kv v896-v920 candidate request package integrity hardening.

Screenshots were generated with Playwright MCP from temporary local HTML pages served by a short-lived read-only `python -m http.server` process on `127.0.0.1:6920`. The server was stopped after screenshots were captured. No text-rendered PNG fallback was used.

| Screenshot | Command or check | Result |
|---|---|---|
| `01-cmake-configure-build.png` | `cmake -S . -B cmake-build-v896-v920 -G Ninja`, then `cmake --build cmake-build-v896-v920` using the CLion bundled toolchain. | Configure and build completed successfully. CMake still reports existing long object path warnings for older long-named tests; the build succeeds. |
| `02-focused-ctest.png` | `ctest --test-dir cmake-build-v896-v920 -R "candidate_request_package_integrity|candidate_request_package|command_tests|line_editor_tests|shard_readiness_tests" --output-on-failure`. | Focused tests passed, including the new integrity modules, fixture check, command catalog, line editor completion, and SHARDJSON readiness assertions. |
| `03-full-ctest.png` | `ctest --test-dir cmake-build-v896-v920 --output-on-failure`. | Full CTest passed: 159/159 tests. |
| `04-cli-smoke.png` | CLI stdin smoke: `COMMANDS`, `SHARDROUTECANDIDATEREQUESTPACKAGEINTEGRITYJSON`, `SHARDJSON`, `QUIT`. | Verified `command_count=60`, read-only command catalog entry, v895 source package linkage, v920 integrity release, `additionalRequestEchoCreated=false`, `documentIntakeOpened=false`, no router, no write, no WAL touch, no execution, and SHARDJSON current status. |
| `05-tcp-smoke.png` | Started `minikv_server.exe 6520 127.0.0.1`, then used `minikv_client.exe 127.0.0.1 6520 5000 --connect-retries 10 --retry-delay-ms 100` for `COMMANDS`, `SHARDROUTECANDIDATEREQUESTPACKAGEINTEGRITYJSON`, `QUIT`. | TCP path verified the new short read-only command and command catalog. The server process was stopped after validation. SHARDJSON was verified through CLI because the current response is close to 1 MB and exceeds the bundled client's single-line response limit. |

Final retained files:

- `e/920/图片/*.png`
- `e/920/解释/说明.md`
- `fixtures/release/shard-readiness-v895.json` through `fixtures/release/shard-readiness-v919.json`
- `fixtures/release/shard-readiness.json` as current v920

Temporary logs, HTML render pages, the temporary HTTP PID file, and the build directory are cleanup candidates after final review and commit.
