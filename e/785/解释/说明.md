# mini-kv v785 command archive

Screenshot method: these artifacts are local CMake, CTest, CLI, and TCP command outputs rather than browser pages. There is no stable Playwright/MCP page context for these logs, so the logs are rendered to PNG images. Temporary text logs are deleted by the cleanup gate; only the PNG evidence is kept.

## 01-cmake-build.png

Command: cmake --build cmake-build-v761.

Result: the build passes and the current source/tests compile.

Meaning: v785 stage/count/status changes did not introduce compile failures.

## 02-focused-ctest.png

Command: focused CTest covering instruction preflight, authoring readiness, artifact preflight, command catalog, line editor, and shard readiness.

Result: focused tests pass.

Meaning: the advanced stage remains read-only and keeps no-write, no-execution, and no-draft-text boundaries.

## 03-full-ctest.png

Command: full CTest.

Result: the full test suite passes.

Meaning: this version does not regress WAL, TCP, restore, runtime receipt, fixture guard, or other existing behavior.

## 04-cli-smoke.png

Command: minikv_cli.exe runs the instruction preflight command, SHARDJSON, and QUIT.

Result: output includes instructionPreflightReleaseVersion=v785, stage $Stage, draftInstructionPreflightOnly=true, draftInstructionTextGenerated=false, writeRoutingAllowed=false, 	ouchesWal=false, and executionAllowed=false.

Meaning: real CLI reads v785 evidence without instruction text generation, grants, write routing, WAL touch, or execution authority.

## 05-tcp-smoke.png

Command: minikv_server.exe listens on 127.0.0.1:17685; minikv_client.exe reads instruction preflight evidence and exits.

Result: client connection succeeds; the server is stopped after verification.

Meaning: TCP can read the new command without leaving a background service process.