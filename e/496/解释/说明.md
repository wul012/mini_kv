# mini-kv v496 command archive

Stage: route-preview-archive-maintenance-verification-tcp-smoke-contract

This archive captures the version closeout commands for the read-only route preview archive maintenance verifier.
The PNG files are rendered from captured command logs because this batch needs high-volume repeatable command
archives; no browser or UI state was required for these terminal-only checks.

- 01-cmake-configure.png: CMake configure for the MinGW build tree.
- 02-cmake-build.png: incremental build after the source and fixture updates.
- 03-focused-tests.png: fixture regeneration plus verifier, command, shard readiness, and completion focused checks.
- 04-full-ctest.png: full CTest suite for the version.
- 05-cli-smoke.png: real CLI reads of SHARDROUTEARCHIVEMAINTVERIFYJSON, CHECKJSON, EXPLAINJSON, and SHARDJSON.
- 06-tcp-smoke.png: owned TCP server smoke; the server is stopped before the version is committed.

Boundary result: read-only evidence only; no active router, no write routing, no WAL mutation, no archive filesystem
walk, no restore/load/compact execution, and no Java order authority.