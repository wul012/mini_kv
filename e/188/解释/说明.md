# mini-kv v188 command archive

This archive records the v188 closeout commands for freezing the v187 Node route catalog expected integrity snapshot audit evidence.

## 01-cmake-configure-build.png

Command: configure and build with the CLion CMake/Ninja/MinGW toolchain.

Result: CMake configured `cmake-build-v188` and built the CLI, server, library, and tests successfully.

Meaning: the v188 freeze evidence and tests compile before fixture generation.

## 02-shardjson-fixture.png

Command: run `SHARDJSON` through `minikv_cli.exe` and write the result to `fixtures/release/shard-readiness.json`.

Result: the rolling fixture reports `releaseVersion=v188`, `status=node-route-catalog-expected-integrity-snapshot-freeze-read-only`, digest `fnv1a64:565e5ce1fdb5f308`, frozen source `fixtures/release/shard-readiness-v187.json`, boundary catalog `read-only-boundary-fields.v16`, `fieldCount=525`, `groupCount=27`, and `executionAllowed=false`.

Meaning: v188 now has a versioned frozen baseline for the v187 audit, with no runtime permission changes.

## 03-targeted-ctest.png

Command: run focused CTest for `command_tests` and `shard_readiness_tests`.

Result: 2/2 focused tests passed.

Meaning: command-level and fixture-level assertions agree on the v188 freeze contract.

## 04-full-ctest.png

Command: run full CTest from `cmake-build-v188`.

Result: 71/71 tests passed.

Meaning: the freeze did not regress storage, WAL, TCP, restore, runtime evidence, or receipt behavior.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6461`, run `minikv_client.exe` with `SHARDJSON` and `QUIT`, validate the JSON response, and stop the server.

Result: TCP smoke returned `releaseVersion=v188`, frozen fixture path `fixtures/release/shard-readiness-v187.json`, `splitRouteGroupCount=39`, `freezeMode=node-route-catalog-expected-integrity-snapshot-freeze-read-only`, `readOnly=true`, and `executionAllowed=false`.

Meaning: the network path publishes the same frozen read-only evidence and leaves no service running.
