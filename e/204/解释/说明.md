# mini-kv v204 command archive

## 01-cmake-configure-build.png

Command: configure and build `cmake-build-v204` with the CLion bundled CMake/Ninja toolchain and MinGW runtime on PATH.

Result and meaning: the full C++ target graph rebuilt after the v204 source/test change, so fixture generation and CTest use fresh binaries.

## 02-shardjson-fixture.png

Command: run `minikv_cli.exe`, issue `SHARDJSON` through stdin, write `fixtures/release/shard-readiness.json`, and summarize the parsed JSON.

Result and meaning: the rolling fixture is v204 with digest `fnv1a64:670b62f7c203b814`. The continuity stage is `post-closeout-archive-readiness-audit`, sequence `4`, and the frozen source is v203; fallback points at `fixtures/release/shard-readiness-v203.json`. `readOnly=True` and `executionAllowed=False`.

## 03-targeted-ctest.png

Command: run `ctest -R "command_tests|shard_readiness_tests" --output-on-failure` from `cmake-build-v204`.

Result and meaning: the two focused suites passed 2/2 and prove the public command plus in-process formatter publish the same v204 read-only archive-readiness audit.

## 04-full-ctest.png

Command: run the full CTest suite from `cmake-build-v204`.

Result and meaning: all 71 tests passed, covering the broader WAL, TCP, restore, fixture, runtime evidence, approval-boundary, and command surfaces.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6477`, request `SHARDJSON` through `minikv_client.exe`, summarize the response, and stop the server.

Result and meaning: real TCP smoke returned v204 with fallback v203, `readOnly=True`, `executionAllowed=False`, `routerActivationAllowed=False`, and `writeRoutingAllowed=False`. The temporary smoke server was stopped after verification.
