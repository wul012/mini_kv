# mini-kv v195 command archive

## 01-cmake-configure-build.png

Command: configure and build `cmake-build-v195` with the CLion bundled CMake, Ninja, and MinGW runtime on PATH.

Result: configuration completed and all v195 binaries were rebuilt from the updated sources.

Meaning: the release catalog audit implementation, boundary catalog update, frozen v194 fixture, and test changes were verified from a fresh build.

## 02-shardjson-fixture.png

Command: run `minikv_cli.exe`, issue `SHARDJSON`, write `fixtures/release/shard-readiness.json`, and summarize the generated fixture.

Result: the fixture reports `releaseVersion=v195`, status `node-route-catalog-cleanup-closeout-release-catalog-audit-read-only`, digest `fnv1a64:36463a895469c4bc`, Node window end `Node v472`, 40 covered Node route/catalog versions, audit source `v194`, boundary catalog `read-only-boundary-fields.v23`, 693 fields, 34 groups, `readOnly=True`, and `executionAllowed=False`.

Meaning: the rolling fixture now audits the v194 cleanup closeout release catalog without adding runtime authority.

## 03-targeted-ctest.png

Command: run focused CTest for `command_tests` and `shard_readiness_tests`.

Result: both targeted suites passed.

Meaning: the public command contract, frozen v194 fixture parity, release catalog audit fields, route/catalog window, and boundary catalog counts are covered before broader regression testing.

## 04-full-ctest.png

Command: run the full CTest suite from `cmake-build-v195`.

Result: all 71 tests passed.

Meaning: v195 did not regress existing storage, WAL, snapshot, restore, TCP, command, retention, or release-evidence behavior.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6468`, request `SHARDJSON` through `minikv_client.exe`, summarize the response, and stop the server.

Result: TCP smoke returned v195 with the same read-only release catalog audit, `executionAllowed=False`, and the server was stopped after verification.

Meaning: the externally visible TCP command path matches the fixture and still opens no write, router, admin, LOAD, RESTORE, COMPACT, or execution behavior.

## Process Notes

Playwright MCP produced the PNG screenshots from temporary local HTML pages served by a short-lived `python -m http.server` process. That render server was stopped after screenshots were captured.
