# mini-kv v1010 command archive

This archive records the key closeout command evidence for mini-kv v986-v1010 candidate material submission precheck integrity. Screenshots were generated through Playwright MCP from local temporary HTML pages. The temporary logs and HTTP pages were used only to produce the PNG evidence and are removed during cleanup.

## 01-cmake-configure.png

Command:

```text
cmake -S . -B cmake-build-v986-v1010 -G Ninja
```

Result: CMake configuration and generation completed successfully. Existing long-object-path warnings remain warnings only and did not block build or tests.

## 02-cmake-build.png

Command:

```text
cmake --build cmake-build-v986-v1010
```

Result: The archive-pass build completed successfully. The build was already up to date after fixture generation and test runs.

## 03-focused-ctest.png

Command:

```text
ctest --test-dir cmake-build-v986-v1010 -R "candidate_material_submission_precheck_integrity|candidate_material_submission_precheck|command_tests|line_editor_tests|shard_readiness_tests" --output-on-failure
```

Result: 13 focused tests passed. This covers the v986-v1010 integrity modules, the v985 source precheck, command catalog wiring, line-editor completion, and `SHARDJSON` aggregation.

## 04-full-ctest.png

Command:

```text
ctest --test-dir cmake-build-v986-v1010 --output-on-failure
```

Result: 191 tests passed. This proves the v1010 integrity chain did not regress WAL, snapshot, TCP, command, fixture, runtime boundary, or historical shard-preview tests.

## 05-cli-smoke.png

Command:

```text
COMMANDS
SHARDROUTECANDIDATEMATERIALSUBMISSIONPRECHECKINTEGRITYJSON
SHARDJSON
QUIT
```

Result: CLI smoke verified that `SHARDROUTECANDIDATEMATERIALSUBMISSIONPRECHECKINTEGRITYJSON` is exposed and returns contract `shard-route-preview-candidate-material-submission-precheck-fixture-integrity.v1`, source precheck `v985`, current release `v1010`, 25 integrity checks, 41 gates, `nodeStopConditionRespected=true`, and disabled material/router/write/WAL/execution fields. The screenshot uses a viewport capture because the full-page capture timed out; the visible content includes the key verification fragments.

## 06-tcp-smoke.png

Command:

```text
minikv_server.exe 6610 127.0.0.1
minikv_client.exe 127.0.0.1 6610 5000
SHARDROUTECANDIDATEMATERIALSUBMISSIONPRECHECKINTEGRITYJSON
QUIT
```

Result: TCP smoke returned the same v1010 read-only integrity evidence through the client/server path. The temporary TCP server was stopped after verification.

## Cleanup note

The archive intentionally keeps only this explanation and the six PNG screenshots under `e/1010/图片/`. The temporary `_logs` folder, temporary HTTP server, Playwright snapshot directory, and `cmake-build-v986-v1010` build directory are cleanup-gate artifacts and are removed before final handoff.
