# mini-kv v1195 command archive

## Scope

v1171-v1195 adds `SHARDROUTEPRODUCTIONLIVECAPTURENONPARTICIPATIONJSON`, a read-only evidence command proving that mini-kv treats Node v1607-v1631 production live-capture module split as sibling maintenance context only.

The source fixture is `fixtures/release/shard-readiness-v1170.json`. The final rolling fixture is `fixtures/release/shard-readiness.json` at v1195. The command and embedded `SHARDJSON` section prove no live capture startup, no live endpoint or raw endpoint read, no credential or secret read, no runtime payload import, no router, no write routing, no WAL touch, and no execution authority.

## Screenshots

| Screenshot | Command | Result and meaning |
| --- | --- | --- |
| `01-cmake-configure.png` | `cmake -S . -B cmake-build-v1195 -G Ninja` | Configure succeeded. CMake repeated the existing long object-path warning for a historical long test filename, but generated build files successfully. |
| `02-cmake-build.png` | `cmake --build cmake-build-v1195` | Build succeeded; Ninja reported no additional work after the final rebuild. |
| `03-targeted-ctest.png` | `ctest --test-dir cmake-build-v1195 -R production_live_capture_non_participation|... --output-on-failure` | 29/29 targeted tests passed, covering the new command, stage/check/validation modules, fixture parity, command catalog exposure, line-editor completion, SHARDJSON embedding, and related rolling profile-section fixture checks. |
| `04-full-ctest.png` | `ctest --test-dir cmake-build-v1195 --output-on-failure` | 231/231 tests passed. The full suite confirms the new v1195 evidence did not regress WAL, snapshot, TCP, command catalog, prior shard readiness evidence, or historical non-participation receipts. |
| `05-cli-smoke.png` | `minikv_cli.exe` with `SHARDROUTEPRODUCTIONLIVECAPTURENONPARTICIPATIONJSON`, `SHARDJSON`, `QUIT` | Real CLI smoke passed. Assertions found v1195, v1170 source integrity fixture, Node v1631 context, no fresh mini-kv evidence requirement, no live capture, no raw endpoint read, no write routing, no WAL touch, no execution, and the embedded SHARDJSON section. |
| `06-tcp-smoke.png` | `minikv_server.exe 17995 127.0.0.1` plus `minikv_client.exe 127.0.0.1 17995 5000 --connect-retries 10 --retry-delay-ms 100` | Real TCP smoke passed for the new read-only command and command catalog. The owned `minikv_server.exe` process was stopped after validation. This was normal mini-kv protocol verification, not live-capture startup. |

## Process Cleanup

- Started and stopped one owned `minikv_server.exe` process for TCP smoke on `127.0.0.1:17995`.
- Started and stopped one temporary Python HTTP server on `127.0.0.1:18195` only to render Playwright screenshot pages.
- No sibling Node or Java service was started.
- No live-capture service was started.

## Boundary Summary

The v1195 archive demonstrates a stable read-only evidence closeout:

- `productionLiveCaptureNonParticipationReleaseVersion=v1195`
- `sourceIntegrityFixturePath=fixtures/release/shard-readiness-v1170.json`
- `sourceNodeRequiresFreshMiniKvEvidence=false`
- `miniKvStartsLiveCapture=false`
- `miniKvReadsRawEndpoint=false`
- `liveEndpointRead=false`
- `rawEndpointStored=false`
- `credentialValueRead=false`
- `secretValueRead=false`
- `activeRouterInstalled=false`
- `writeRoutingAllowed=false`
- `touchesWal=false`
- `executionAllowed=false`

Temporary logs and HTML screenshot pages under `e/1195/_logs/` are cleanup-only intermediates and can be deleted after this archive is committed.
