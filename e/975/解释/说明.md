# mini-kv v975 command archive

本目录归档 mini-kv v966-v975 candidate material request integrity closeout 的关键命令截图。截图通过 Playwright MCP 从本地临时 HTTP 页面生成；原始日志只用于生成截图，最终保留 PNG 和本说明。

## 01-cmake-configure.png

命令：

```text
cmake -S . -B cmake-build-v966-v975 -G Ninja
```

结果：CMake 配置和生成成功，构建目录为 `cmake-build-v966-v975`。配置阶段仍出现既有长路径 warning，属于当前测试目标命名较长导致的历史提示；后续 build 和 CTest 均通过，未形成失败。

## 02-cmake-build.png

命令：

```text
cmake --build cmake-build-v966-v975
```

结果：Ninja 返回 `no work to do`，说明归档前代码已经完成构建，当前构建产物与源代码一致。

## 03-focused-ctest.png

命令：

```text
ctest --test-dir cmake-build-v966-v975 -R "candidate_material_request_integrity|candidate_material_request|command_tests|line_editor_tests|shard_readiness_tests" --output-on-failure
```

结果：定向 14 条测试全部通过。它覆盖新增 integrity 模块、旧 material request 回归、命令目录、补全行为和 `SHARDJSON` 聚合，证明新增 `SHARDROUTECANDIDATEMATERIALREQUESTINTEGRITYJSON` 没有破坏 v965 material request 行为。

## 04-full-ctest.png

命令：

```text
ctest --test-dir cmake-build-v966-v975 --output-on-failure
```

结果：全量 181 条 CTest 全部通过，证明 v975 顶层 readiness 状态、版本化 fixture、命令注册和历史证据链回归均稳定。

## 05-cli-smoke.png

命令：

```text
COMMANDS
SHARDROUTECANDIDATEMATERIALREQUESTINTEGRITYJSON
SHARDJSON
QUIT
```

结果：CLI smoke 验证新命令出现在命令目录中，属性为 read-only、`mutates_store=no`、`touches_wal=no`、`stable=yes`。同时验证 integrity JSON 和 `SHARDJSON` 均包含 v975、source v965、15 条完整性检查、Node v1446 stop condition，以及 `additionalMaterialRequestEchoCreated=false`、`externalReviewedMaterialConsumed=false`、`documentRouterInstalled=false`、`writeRoutingAllowed=false`、`touchesWal=false`、`executionAllowed=false`。

## 06-tcp-smoke.png

命令：

```text
minikv_server 6575 127.0.0.1
minikv_client 127.0.0.1 6575 5000
SHARDROUTECANDIDATEMATERIALREQUESTINTEGRITYJSON
QUIT
```

结果：TCP smoke 在 `127.0.0.1:6575` 读取 integrity JSON 成功，验证 contract、v975 release、Node stop condition 和 `executionAllowed=false`。服务端仅用于本轮 smoke，验证结束后已停止。

## Cleanup note

截图生成期间启动过一个本地 HTTP server 用于 Playwright MCP 访问 HTML 日志页，PID 25080，截图完成后已停止。临时 `_logs`、Playwright snapshot 目录和 build 目录已在提交前 cleanup gate 中删除；最终保留本目录说明和 `e/975/图片/` 下的 PNG 证据。
