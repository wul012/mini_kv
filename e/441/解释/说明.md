# mini-kv v441 command archive

本目录记录 v441 `SHARDROUTEVERIFYREPORTARCHIVECLOSEOUTVERIFYAUDITCLOSEOUTARCHIVEVERIFYJSON` 的关键命令证据。截图是终端 transcript 的文本渲染 PNG；本轮验证对象不是浏览器页面，因此没有用页面截图包装命令输出。

## 01-cmake-configure.png

命令：`cmake -S . -B cmake-build-v441-v460 -G "MinGW Makefiles" -DCMAKE_CXX_COMPILER=...`

结果：配置通过，生成 `cmake-build-v441-v460`。输出中仍有历史长路径 warning，但 CMake 退出码为 0，不影响本轮构建。

意义：证明 v441 新增源文件、测试文件和 CMake 注册可以被本机 CLion/MinGW 工具链识别。

## 02-cmake-build.png

命令：`cmake --build cmake-build-v441-v460 --parallel 6`

结果：全量构建通过，包含 `minikv` 静态库、CLI/server/client、以及新增 `minikv_shard_route_preview_audit_closeout_archive_verification_tests.exe`。

意义：证明新 archive verification 模块、命令入口、命令契约、行编辑补全和 SHARDJSON 嵌入都能一起编译。

## 03-focused-tests.png

命令：运行新增 focused test，并重跑 `command_tests`、`shard_readiness_tests`、`line_editor_tests`。

结果：全部通过。

意义：证明新命令是 read-only metadata evidence；`EXPLAINJSON`/`CHECKJSON` 不允许执行；当前 fixture 与 formatter 一致；客户端补全能够发现新命令。

## 04-full-ctest.png

命令：`ctest --test-dir cmake-build-v441-v460 --output-on-failure`

结果：82/82 通过。

意义：证明 v441 没有破坏 WAL、snapshot、TCP、RESP、fixture、runtime evidence、credential resolver receipt、restore/release evidence 等既有行为。

## 05-cli-smoke.png

命令：通过 `minikv_cli.exe` 读取 `SHARDROUTEVERIFYREPORTARCHIVECLOSEOUTVERIFYAUDITCLOSEOUTARCHIVEVERIFYJSON`，再读取对应 `CHECKJSON`。

结果：CLI 返回 v441 archive verification JSON；`CHECKJSON` 返回 `read_only=true` 和 `execution_allowed=false`。

意义：证明命令行真实路径只读可用，没有写 store、没有触 WAL、没有执行 restore/load/compact。

## 06-tcp-smoke.png

命令：启动本轮 owned `minikv_server.exe` 到 `127.0.0.1:6641`，通过 TCP 发送新命令、对应 `CHECKJSON` 和 `QUIT`。

结果：TCP 返回 archive verification JSON、`CHECKJSON` 返回 `execution_allowed=false`，`QUIT` 返回 `BYE`。验证结束后已停止本轮启动的 server 进程。

意义：证明新命令在真实 TCP inline 协议下同样只是读证据，不安装 active router、不允许 write routing、不启动额外服务、不留下后台进程。
