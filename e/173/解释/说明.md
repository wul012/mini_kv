# mini-kv v173 command archive

This archive records the final closure evidence for mini-kv v173. The version freezes v172 as `fixtures/release/shard-readiness-v172.json` and extends the read-only `nodeRouteSplitCompatibilityWindow` through Node v447 for the managed audit dry-run adapter route split.

The screenshots were generated with Playwright MCP from temporary local HTML render pages served on `127.0.0.1:8173`. That HTTP server is temporary capture infrastructure and should be stopped during cleanup.

## 01-cmake-configure-build.png

Command:

```powershell
cmake -S . -B cmake-build-v173 -G Ninja
cmake --build cmake-build-v173
```

Result: CMake configure/generate succeeded and Ninja confirmed the v173 build graph was up to date.

Meaning: v173 code and tests are present in the CMake build graph, including the Node v447 route split compatibility window extension and the v172 frozen fixture.

## 02-generate-shard-readiness-fixture.png

Command:

```powershell
"SHARDJSON`nQUIT`n" | .\cmake-build-v173\minikv_cli.exe
```

Result: `fixtures/release/shard-readiness.json` was regenerated from real CLI output. Key fields:

- `releaseVersion=v173`
- `status=node-route-split-window-dry-run-adapter-read-only`
- `windowEndNodeVersion=Node v447`
- `splitRouteGroupCount=15`
- `observedWindowVersionCount=15`
- `sourceFrozenFixturePath=fixtures/release/shard-readiness-v172.json`
- `catalogVersion=read-only-boundary-fields.v9`
- `fieldCount=420`
- `groupCount=22`
- `evidenceDigest=fnv1a64:33a60bc1d66fd286`
- `executionAllowed=false`
- `writeRoutingAllowed=false`
- `routerActivationAllowed=false`

Meaning: v173 extends the Node route split compatibility window without adding catalog fields or enabling routers, writes, runtime probes, service startup, restore/load/compact, or execution.

## 03-targeted-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v173 -R "^(command_tests|shard_readiness_tests)$" --output-on-failure
```

Result: targeted tests passed 2/2.

Meaning: the direct `SHARDJSON` contract tests verify v173 runtime/fixture parity, the frozen v172 fixture, the Node v447 window extension, the 15-version audit count, boundary catalog v9 counts, and read-only boundary flags.

## 04-full-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v173 --output-on-failure
```

Result: full CTest passed 71/71.

Meaning: the v173 compatibility update does not regress WAL, snapshots, restore evidence, TCP tests, runtime smoke evidence, managed-audit receipts, credential-resolver receipts, route split compatibility evidence, or existing shard-readiness fixtures.

## 05-tcp-smoke.png

Command:

```powershell
minikv_server.exe 6446 127.0.0.1
minikv_client.exe 127.0.0.1 6446
```

Client input:

```text
SHARDJSON
GET missing:v173
QUIT
```

Result: TCP smoke read v173 `SHARDJSON`, confirmed `windowEndNodeVersion=Node v447`, confirmed `observedWindowVersionCount=15`, confirmed `executionAllowed=false`, `writeRoutingAllowed=false`, and `routerActivationAllowed=false`, observed `(nil)` for the missing key, stopped the server, and confirmed port `6446` was released.

Meaning: the real TCP path can consume the v447 compatibility window as evidence only. It does not activate routing, writes, probes, service startup, restore/load/compact, or execution.

## Cleanup

Temporary render/log directories for this task are `e/173/_render/` and `e/173/_logs/`. They are intermediate capture inputs and should be deleted after final verification. The temporary render HTTP server on port `8173` should be stopped. The final kept archive is `e/173/图片/` plus this explanation file.
