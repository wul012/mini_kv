# mini-kv v1160 command archive

This archive records the final verification evidence for mini-kv v1136-v1160, which closes the read-only `SHARDROUTEVALUESUPPLYPROFILESECTIONJSON` evidence chain at v1160.

The screenshots were generated with Playwright MCP from temporary localhost HTML pages rendered from the final command logs. The temporary HTTP server was stopped after screenshots were captured.

## Screenshots

1. `01-cmake-configure.png`
   - Command: `cmake -S . -B cmake-build-v1160 -G Ninja -DCMAKE_C_COMPILER=...gcc.exe -DCMAKE_CXX_COMPILER=...g++.exe`
   - Result: configure completed with exit code 0 using the CLion MinGW/Ninja toolchain.
   - Meaning: the v1160 source tree is accepted by CMake before any final build or test evidence is used.

2. `02-cmake-build.png`
   - Command: `cmake --build cmake-build-v1160`
   - Result: build completed with exit code 0.
   - Meaning: the new profile-section renderer split sources, command wiring, and tests compile together.

3. `03-targeted-ctest.png`
   - Command: targeted `ctest` over command, line-editor, SHARDJSON, value-supply source lineage, and the new profile-section tests.
   - Result: 23/23 targeted tests passed.
   - Meaning: the new `SHARDROUTEVALUESUPPLYPROFILESECTIONJSON` command, fixtures, validation split, source lineage, and rolling status are internally consistent.

4. `04-full-ctest.png`
   - Command: `ctest --test-dir cmake-build-v1160 --output-on-failure`
   - Result: 221/221 tests passed, 0 failed, exit code 0.
   - Meaning: the v1160 current fixture and command addition did not regress storage, WAL, TCP, recovery, release, shard readiness, or historical evidence tests.

5. `05-cli-smoke.png`
   - Command: `minikv_cli.exe` with `SHARDROUTEVALUESUPPLYPROFILESECTIONJSON`, `SHARDJSON`, and `QUIT`.
   - Result: CLI output contains contract `shard-route-preview-operator-value-supply-profile-section-renderer-split.v1`, command `SHARDROUTEVALUESUPPLYPROFILESECTIONJSON`, `executionAllowed=false`, `writeRoutingAllowed=false`, `releaseVersion=v1160`, and the embedded `shardPreviewOperatorValueSupplyProfileSection`.
   - Meaning: the command and aggregate SHARDJSON expose the new evidence as read-only metadata only.

6. `06-tcp-smoke.png`
   - Command: owned `minikv_server.exe 6760 127.0.0.1` plus `minikv_client.exe 127.0.0.1 6760 5000 --connect-retries 10 --retry-delay-ms 100`.
   - Result: TCP output for `SHARDROUTEVALUESUPPLYPROFILESECTIONJSON` contains the same contract and false execution/write-routing boundaries. The owned server process was stopped after smoke.
   - Meaning: the short read-only command works through the real TCP path without using aggregate SHARDJSON over the client line reader and without leaving a server running.

## Boundary Statement

The v1160 evidence remains read-only. It records Node v1581 renderer ownership context and versioned mini-kv fixtures, but it does not import Node renderers, import operator values, import fresh sibling evidence, accept runtime payloads, accept synthetic evidence, read credential values, parse raw endpoints, install routers, route writes, touch WAL, start sibling services, or execute runtime behavior.
