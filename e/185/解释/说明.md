# mini-kv v185 command archive

v185 freezes v184 as a versioned fixture and adds `nodeRouteCatalogCloseoutSnapshot` for the Node v464 final closeout validation result. It remains read-only evidence only: no router, no write path, no service startup, no runtime probe, and no execution permission.

## 01-cmake-configure-build.png

- Command: configure and build `cmake-build-v185` with the CLion CMake/MinGW toolchain.
- Result: build completed successfully after adding the route catalog closeout snapshot module.
- Meaning: confirms the new formatter, CMake wiring, and tests compile and link.

## 02-generate-shard-readiness-fixture.png

- Command: regenerate `fixtures/release/shard-readiness.json` through `minikv_cli.exe` and parse the result.
- Result: `releaseVersion=v185`, status `node-route-catalog-closeout-snapshot-read-only`, digest `fnv1a64:6a6b4cbdac4bdb46`, boundary catalog `read-only-boundary-fields.v12`, field/group count `448/24`.
- Meaning: proves the rolling fixture matches the new read-only snapshot contract.

## 03-targeted-ctest.png

- Command: run targeted `command_tests` and `shard_readiness_tests`.
- Result: 2/2 passed.
- Meaning: verifies SHARDJSON command behavior, fixture parity, frozen v184 evidence, snapshot fields, and closed runtime boundaries.

## 04-full-ctest.png

- Command: run full CTest.
- Result: 71/71 passed.
- Meaning: confirms the snapshot does not regress storage, WAL, TCP, recovery, credential-resolver receipts, or restore evidence tests.

## 05-tcp-smoke.png

- Command: start `minikv_server.exe` on `127.0.0.1:6458`, read `SHARDJSON` through `minikv_client.exe`, then stop the server.
- Result: response contained v185, the closeout snapshot, `readOnly=true`, `executionAllowed=false`, and digest `fnv1a64:6a6b4cbdac4bdb46`.
- Meaning: real client/server read path remains read-only and non-executing.

## Cleanup Record

- Stopped: v185 TCP smoke server.
- Kept: final archive screenshots, this explanation, v185 rolling fixture, frozen `fixtures/release/shard-readiness-v184.json`, source/test/docs changes, and walkthrough.
- Deleted before final handoff: `cmake-build-v185`, `e/185/_logs`, `e/185/_render`, and temporary screenshot render files.
