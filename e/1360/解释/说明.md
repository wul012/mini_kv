# v1360 归档说明

本轮收尾 `v1331-v1360` 的 catalog entry group split non-participation follow-up，把 `v1359` 冻结为版本化来源 fixture，并将当前 `shard-readiness.json` 滚到 `v1360`。本版只发布只读证据，不读取 Node catalog endpoint，不导入 catalog records，不安装 router，不打开 write routing，不触碰 WAL，不执行 LOAD/RESTORE/COMPACT，也不启动 Java 或 Node 服务。

## 截图对应

1. `01-cmake-configure-build.png`
   - 命令：`cmake -S . -B cmake-build-v1360 -G "MinGW Makefiles"` 和 `cmake --build cmake-build-v1360 --parallel 4`
   - 结果：配置与构建退出码均为 `0`；CMake 仅保留一个非致命的 `CMAKE_OBJECT_PATH_MAX` 长对象路径 warning。
   - 意义：确认 v1360 的新 stage/check/validation 拆分、fixture 消费测试和命令面都能通过 MinGW/CMake 编译链接。

2. `02-targeted-ctest.png`
   - 命令：定向 `ctest`，覆盖 `catalog_entry_group_split_non_participation`、`shard_readiness`、`command_tests`、production live capture fixture 消费者，以及 operator value supply 相关消费者。
   - 结果：`38/38` 通过，退出码 `0`。
   - 意义：先确认本轮直接改动面和 current rolling status 的消费面没有回归。

3. `03-full-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v1360 --output-on-failure`
   - 结果：`251/251` 通过，退出码 `0`。
   - 意义：确认旧 WAL、TCP、恢复、fixture、receipt、read-only boundary 和历史命令行为没有被 v1360 带偏。

4. `04-cli-tcp-smoke.png`
   - 命令：本地 CLI 通过 stdin 执行 `SHARDROUTECATALOGENTRYGROUPSPLITNONPARTICIPATIONJSON`；TCP smoke 启动 `minikv_server.exe 17660 127.0.0.1 --accept-poll-ms 50`，再用 `minikv_client.exe 127.0.0.1 17660 2000` 读取同一命令。
   - 结果：CLI 和 TCP 两条路径退出码均为 `0`，均观察到 `release=v1360`、`range=v1331-v1360`、`plannedStageCount=30`、`completedCheckCount=30`。
   - 边界字段：`readOnly=true`、`executionAllowed=false`、`writeRoutingAllowed=false`、`touchesWal=false`、`activeRouterInstalled=false`、`catalogRouterInstalled=false`、`miniKvReadsCatalogEndpoint=false`、`miniKvConsumesCatalogRecords=false`、`nodeCatalogEntryRecordsImportedByMiniKv=false`、`nodeCatalogRegistryExecutedByMiniKv=false`、`nodeCatalogEndpointReadByMiniKv=false`、`loadRestoreCompactAllowed=false`。
   - 意义：真实运行层确认 v1360 只发布证据，不打开 router、写入、WAL 或执行能力。

## 清理

- 归档保留：`e/1360/图片/` 和本说明文档。
- 临时日志目录：`e/1360/_logs/` 仅用于生成截图，最终收尾时删除。
- 构建目录：`cmake-build-v1360` 用于本轮构建和测试，最终收尾时删除。
- TCP smoke 启动过的 `minikv_server.exe` 已在验证结束后停止，最终收尾会再次确认无残留进程。
