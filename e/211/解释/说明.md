# mini-kv v211 command archive

## 01-cmake-configure-build.png

Command: configure and build cmake-build-v211 with the CLion bundled toolchain.

Result and meaning: fresh binaries were built after the v211 source/test change.

## 02-shardjson-fixture.png

Command: run minikv_cli.exe, issue SHARDJSON through stdin, extract the JSON block, write fixtures/release/shard-readiness.json, and summarize the parsed JSON.

Result and meaning: the rolling fixture is v211. The stage is `post-closeout-continuity-release-catalog-retention`, sequence is `11`, source is `v210`, fallback is `fixtures/release/shard-readiness-v210.json`, readOnly=True, executionAllowed=False.

## 03-targeted-ctest.png

Command: ctest -R command_tests|shard_readiness_tests --output-on-failure.

Result and meaning: focused suites passed 2/2.

## 04-full-ctest.png

Command: full CTest.

Result and meaning: all 71 tests passed.

## 05-tcp-smoke.png

Command: start minikv_server.exe on 127.0.0.1:6484, request SHARDJSON through minikv_client.exe, summarize the response, and stop the server.

Result and meaning: TCP smoke returned v211 with fallback `v210`, readOnly=True, executionAllowed=False, routerActivationAllowed=False, and writeRoutingAllowed=False.
