# mini-kv v473 command archive

This directory records key command evidence for v473 $commandName. PNG files are rendered terminal transcripts because this verification target is command output, not a browser page.

## 01-cmake-configure.png

Configures $build. Existing long-path warnings do not change the successful exit code.

## 02-cmake-build.png

Builds all targets and proves the maintenance ledger module, command entry point, fixture dependency, and tests compile together.

## 03-focused-tests.png

Runs the maintenance focused test plus command, shard readiness, previous archive verification, and line editor tests. This proves audits source fixture index continuity without filesystem reads at runtime.

## 04-full-ctest.png

Runs full CTest; all tests pass.

## 05-cli-smoke.png

Reads $commandName and matching CHECKJSON through CLI. CHECKJSON remains ead_only=true and execution_allowed=false.

## 06-tcp-smoke.png

Starts an owned loopback minikv_server.exe, reads $commandName and matching CHECKJSON through TCP, sends QUIT, and stops the owned process after validation.