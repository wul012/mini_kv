# mini-kv v456 command archive

This directory records key command evidence for v456 `SHARDROUTEVERIFYREPORTARCHIVECLOSEOUTVERIFYAUDITCLOSEOUTARCHIVEVERIFYJSON`. PNG files are rendered terminal transcripts because this verification target is command output, not a browser page.

## 01-cmake-configure.png

Configures `cmake-build-v441-v460`. Historical long-path warnings do not change the successful exit code.

## 02-cmake-build.png

Builds all targets and proves archive verification code, command entry points, fixture dependencies, and tests compile together.

## 03-focused-tests.png

Runs the archive verification focused test plus `command_tests`, `shard_readiness_tests`, and `line_editor_tests`. This proves audits readOnly, executionAllowed, router, write, WAL, and service-start fields.

## 04-full-ctest.png

Runs full CTest; all 82 tests pass.

## 05-cli-smoke.png

Reads `SHARDROUTEVERIFYREPORTARCHIVECLOSEOUTVERIFYAUDITCLOSEOUTARCHIVEVERIFYJSON` and matching `CHECKJSON` through CLI. CHECKJSON remains `read_only=true` and `execution_allowed=false`.

## 06-tcp-smoke.png

Starts an owned loopback `minikv_server.exe`, reads `SHARDROUTEVERIFYREPORTARCHIVECLOSEOUTVERIFYAUDITCLOSEOUTARCHIVEVERIFYJSON` and matching `CHECKJSON` through TCP, sends `QUIT`, and stops the owned process after validation.