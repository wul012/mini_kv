# mini-kv v1490 command archive

本归档使用 Playwright MCP 对临时 HTML 命令摘要页截图，截图完成后删除 `_logs` 临时目录，仅保留 PNG 和本说明。

- `01-cmake-configure-build.png`: CMake configure/build 成功。日志中仍有既有 CMAKE_OBJECT_PATH_MAX 路径长度 warning，但命令退出码为 0。
- `02-targeted-ctest.png`: v1476-v1490 新模块、command、line editor、SHARDJSON 和 source fixture 相关定向测试 9/9 通过。
- `03-full-ctest.png`: 全量 CTest 286/286 通过，证明新增 evidence 不影响 WAL/TCP/restore/runtime receipt 等既有行为。
- `04-cli-smoke.png`: CLI 真实读取 `SHARDROUTERELEASEWINDOWREADINESSPACKETSPLITNONPARTICIPATIONJSON` 和 `SHARDJSON`，返回 v1490 read-only evidence。
- `05-tcp-smoke.png`: TCP server/client 真实 smoke 返回同一只读 evidence；验证后已停止 server。
- `06-git-diff-check.png`: `git diff --check` 退出码为 0，无 trailing whitespace 等格式问题。