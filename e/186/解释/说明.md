# mini-kv v186 command archive

v186 freezes v185 and adds a read-only audit for Node v467 route catalog anchor removal. It extends the compatibility window to Node v433-v467 while keeping mini-kv free of route-anchor dependency, router activation, writes, service startup, runtime probes, and execution.

## 01-cmake-configure-build.png

- Command: configure and build `cmake-build-v186` with the CLion CMake/MinGW toolchain.
- Result: build completed successfully.
- Meaning: confirms the v467 window and anchor-removal audit compile and link.

## 02-generate-shard-readiness-fixture.png

- Command: regenerate `fixtures/release/shard-readiness.json` through `minikv_cli.exe` and parse the result.
- Result: `releaseVersion=v186`, status `node-route-catalog-anchor-removal-audit-read-only`, digest `fnv1a64:a5d69dff949088ee`, window `Node v433-v467`, count/span `35/35`, boundary catalog `read-only-boundary-fields.v13`, field/group count `472/25`.
- Meaning: proves the rolling fixture reflects the latest Node v467 read-only audit boundary.

## 03-targeted-ctest.png

- Command: run targeted `command_tests` and `shard_readiness_tests`.
- Result: 2/2 passed.
- Meaning: verifies SHARDJSON command behavior, frozen v185 fixture, v467 window, anchor-removal audit fields, and closed runtime boundaries.

## 04-full-ctest.png

- Command: run full CTest.
- Result: 71/71 passed.
- Meaning: confirms the v467 audit does not regress other mini-kv behavior.

## 05-tcp-smoke.png

- Command: start `minikv_server.exe` on `127.0.0.1:6459`, read `SHARDJSON` through `minikv_client.exe`, then stop the server.
- Result: response contained v186, Node v467 window, count 35, the anchor-removal audit, `readOnly=true`, `executionAllowed=false`, and digest `fnv1a64:a5d69dff949088ee`.
- Meaning: real client/server path remains a read-only evidence read.

## Cleanup Record

- Stopped: v186 TCP smoke server.
- Kept: final archive screenshots, this explanation, v186 rolling fixture, frozen `fixtures/release/shard-readiness-v185.json`, source/test/docs changes, and walkthrough.
- Deleted before final handoff: `cmake-build-v186`, `e/186/_logs`, `e/186/_render`, and temporary screenshot render files.
