# mini-kv v213 command archive explanation

## Scope

v213 freezes the v212 shard-readiness fixture as fixtures/release/shard-readiness-v212.json and advances the post-closeout continuity lane to post-closeout-continuity-downstream-consumption-handshake. The evidence remains metadata-only and read-only: no router activation, no write routing, no admin/write command, no LOAD/RESTORE/COMPACT execution, no runtime probe, and no service startup as part of the evidence contract.

## Screenshots

1. 01-cmake-build.png shows the v213 incremental CMake build after source and test updates. It proves the modified C++ and tests were rebuilt rather than using stale binaries.
2. 02-targeted-ctest.png shows command_tests and shard_readiness_tests passing. These tests cover releaseVersion v213, frozen v212 source fixture, digest pinning, fallback text, fixture parity, and no-router/no-write/no-execution fields.
3. 03-full-ctest.png shows all 71 CTest tests passing. This checks that the v213 evidence update did not regress WAL, store, TCP, restore, runtime receipt, or historical fixture behavior.
4. 04-tcp-smoke.png shows a real loopback minikv_client SHARDJSON read from minikv_server on port 6486. The parsed response was v213 with executionAllowed=false, routerActivationAllowed=false, and writeRoutingAllowed=false.
5. 05-evidence-summary.png summarizes the frozen source, digest, stage sequence, post-closeout range, and read-only boundary flags from fixtures/release/shard-readiness.json.

## Cleanup

The TCP server started for the smoke test was stopped after validation. Temporary build, log, and render folders are removed during the final cleanup gate for this version; the final kept archive is e/213/图片 plus this explanation file.