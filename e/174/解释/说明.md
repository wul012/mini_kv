# mini-kv v174 command archive

This archive records the final closure evidence for mini-kv v174. The version freezes v173 as `fixtures/release/shard-readiness-v173.json` and extends the read-only `nodeRouteSplitCompatibilityWindow` through Node v448 for the managed audit route quality route split.

The screenshots were generated with Playwright MCP from temporary local HTML render pages served on `127.0.0.1:8174`. That HTTP server is temporary capture infrastructure and should be stopped during cleanup.

## 01-cmake-configure-build.png

Command:

```powershell
cmake -S . -B cmake-build-v174 -G Ninja
cmake --build cmake-build-v174
```

Result: CMake configure/generate succeeded and Ninja rebuilt the v174 test target after the focused assertion fix.

Meaning: v174 code and tests are present in the CMake build graph, including the Node v448 route split compatibility window extension and the v173 frozen fixture.

## 02-generate-shard-readiness-fixture.png

Command:

```powershell
"SHARDJSON`nQUIT`n" | .\cmake-build-v174\minikv_cli.exe
```

Result: `fixtures/release/shard-readiness.json` was regenerated from real CLI output. Key fields:

- `releaseVersion=v174`
- `status=node-route-split-window-route-quality-read-only`
- `windowEndNodeVersion=Node v448`
- `splitRouteGroupCount=16`
- `observedWindowVersionCount=16`
- `sourceFrozenFixturePath=fixtures/release/shard-readiness-v173.json`
- `catalogVersion=read-only-boundary-fields.v9`
- `fieldCount=420`
- `groupCount=22`
- `evidenceDigest=fnv1a64:d582c3d04aed99e1`
- `executionAllowed=false`
- `writeRoutingAllowed=false`
- `routerActivationAllowed=false`

Meaning: v174 extends the Node route split compatibility window without adding catalog fields or enabling routers, writes, runtime probes, service startup, restore/load/compact, or execution.

## 03-targeted-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v174 -R "^(command_tests|shard_readiness_tests)$" --output-on-failure
```

Result: targeted tests passed 2/2.

Meaning: the direct `SHARDJSON` contract tests verify v174 runtime/fixture parity, the frozen v173 fixture, the Node v448 window extension, the 16-version audit count, boundary catalog v9 counts, and read-only boundary flags.

## 04-full-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v174 --output-on-failure
```

Result: full CTest passed 71/71.

Meaning: the v174 compatibility update does not regress WAL, snapshots, restore evidence, TCP tests, runtime smoke evidence, managed-audit receipts, credential-resolver receipts, route split compatibility evidence, or existing shard-readiness fixtures.

## 05-tcp-smoke.png

Command:

```powershell
minikv_server.exe 6447 127.0.0.1
minikv_client.exe 127.0.0.1 6447
```

Client input:

```text
SHARDJSON
GET missing:v174
QUIT
```

Result: TCP smoke read v174 `SHARDJSON`, confirmed `windowEndNodeVersion=Node v448`, confirmed `observedWindowVersionCount=16`, confirmed `executionAllowed=false`, `writeRoutingAllowed=false`, and `routerActivationAllowed=false`, observed `(nil)` for the missing key, stopped the server, and confirmed port `6447` had no listener.

Meaning: the real TCP path can consume the v448 compatibility window as evidence only. It does not activate routing, writes, probes, service startup, restore/load/compact, or execution.

## Cleanup

Temporary render/log directories for this task are `e/174/_render/` and `e/174/_logs/`. They are intermediate capture inputs and should be deleted after final verification. The temporary render HTTP server on port `8174` should be stopped. The final kept archive is `e/174/图片/` plus this explanation file.
