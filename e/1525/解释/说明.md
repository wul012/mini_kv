# mini-kv v1506-v1525 命令归档说明

本目录记录 v1506-v1525 blocked execution rehearsal split non-participation 版本链的最终验证截图。截图通过 Playwright MCP 打开本地临时归档页面生成；页面服务只用于截图，完成后已停止。

## 截图清单

1. `01-cmake-configure-build.png`
   - 命令：`cmake -S . -B cmake-build-v1525 -G Ninja`，随后 `cmake --build cmake-build-v1525`
   - 结果：配置和构建通过。CMake 保留了历史长目标路径 warning，但构建成功。
   - 意义：证明新增 core/stage/check/validation 源文件、命令接线和测试目标都能被本机 CLion MinGW 工具链编译链接。

2. `02-targeted-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v1525 -R "(blocked_rehearsal_split_np|command_tests|line_editor_tests|shard_readiness_tests|dfh_echo_split_np_fixture_tests|release_window_readiness_packet_split_non_participation_fixture_tests)" --output-on-failure`
   - 结果：10/10 通过。
   - 意义：证明本版新增 v1506-v1525 stage/check/validation/core/fixture 测试通过，同时命令目录、补全、rolling SHARDJSON、v1505 来源 fixture 兼容都保持正确。

3. `03-full-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v1525 --output-on-failure`
   - 结果：296/296 通过。
   - 意义：证明本版没有破坏既有 store、WAL、snapshot、TCP、runtime receipt、历史 shard readiness 证据链和全部旧 fixture 测试。

4. `04-cli-smoke.png`
   - 命令：通过 `minikv_cli.exe` 输入 `COMMANDS`、`SHARDROUTEBLOCKEDEXECUTIONREHEARSALSPLITNONPARTICIPATIONJSON`、`SHARDJSON`、`EXIT`
   - 结果：CLI smoke 通过，检查到 `command_count=87`、v1525 新段落、Node v1982 来源、以及 no Node checks/no service/no endpoint/no credential/no receipt parse/no Java scan/no WAL replay/no execution 字段。
   - 意义：证明 standalone 命令和 rolling SHARDJSON 都在真实 CLI 路径可读。

5. `05-tcp-smoke.png`
   - 命令：临时启动 `minikv_server.exe 6424 127.0.0.1`，通过 `minikv_client.exe 127.0.0.1 6424` 输入 `COMMANDS`、`SHARDROUTEBLOCKEDEXECUTIONREHEARSALSPLITNONPARTICIPATIONJSON`、`EXIT`
   - 结果：TCP smoke 通过，临时 server 在验证后已停止。
   - 意义：证明新 read-only command 经真实 TCP 读路径可见，并且不会要求发送超长 SHARDJSON、不会启动额外服务或打开写/执行边界。

6. `06-git-diff-check.png`
   - 命令：`git diff --check`
   - 结果：exit code 0，通过；输出只有 Git 的 CRLF 规范化 warning，没有 whitespace 错误。
   - 意义：证明本版代码、fixture、README、讲解和归档说明没有 trailing whitespace 等基础格式瑕疵；CRLF warning 属于本仓库 Windows 换行规范化提示。

## 边界结论

v1506-v1525 只发布版本化证据：mini-kv 不导入 Node modules，不执行 Node checks，不启动 Node/Java/mini-kv 额外服务，不读取 endpoint 或 credential，不解析历史 receipt，不扫描 Java v90，不回放 mini-kv v99 WAL regression，不安装 active router，不写 store，不触碰 WAL，不允许 execution。

## 进程清理

- TCP smoke 的 `minikv_server.exe` 已在 smoke 脚本 finally 中停止。
- Playwright 截图用的临时 `python -m http.server 8768 --bind 127.0.0.1` 已停止。
