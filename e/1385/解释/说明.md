# mini-kv v1361-v1385 命令归档说明

本轮对齐只读查看的 Node 最新计划：

```text
D:\nodeproj\orderops-node\docs\plans3\v1846-controlled-read-only-shard-preview-type-barrel-split-roadmap.md
```

mini-kv 侧没有修改 Node 或 Java，只新增 `v1361-v1385` 的 controlled read-only shard preview type barrel split non-participation 证据链。证据证明 mini-kv 只记录 Node 类型桶拆分维护上下文，不导入 TypeScript type/barrel/profile boundary，不读取 profile/evidence endpoint，不安装 router，不写 WAL，不执行 Node 测试/build/Vitest。

## 截图 01：CMake 配置与构建

文件：

```text
e/1385/图片/01-cmake-configure-build.png
```

命令：

```powershell
cmake -S . -B cmake-build-v1385 -G Ninja -DCMAKE_CXX_COMPILER=D:\CLION\CLion 2025.1.1\bin\mingw\bin\g++.exe
cmake --build cmake-build-v1385
```

结果：CMake 配置完成，Ninja 构建图稳定，当前构建无新增编译工作。截图中的 CMake path-length warning 是既有超长测试目标路径提示，不是本轮编译失败；后续 CTest 已完整通过。

## 截图 02：定向测试

文件：

```text
e/1385/图片/02-targeted-ctest.png
```

命令：

```powershell
ctest --test-dir cmake-build-v1385 -R "type_barrel_split_non_participation|command_tests|line_editor_tests|shard_readiness_tests|catalog_entry_group_split_non_participation_fixture|production_live_capture_(archive_verification|release_evidence_review)_non_participation_fixture" --output-on-failure
```

结果：11/11 通过。覆盖新增 stage/check/validation/module/fixture 测试，命令目录、补全、`SHARDJSON` 当前状态、旧 source fixture 兼容断言全部通过。

## 截图 03：全量 CTest

文件：

```text
e/1385/图片/03-full-ctest.png
```

命令：

```powershell
ctest --test-dir cmake-build-v1385 --output-on-failure
```

结果：256/256 通过。说明本轮新增 section、命令、fixtures 和旧历史证据测试没有互相破坏。

## 截图 04：CLI/TCP smoke

文件：

```text
e/1385/图片/04-cli-tcp-smoke.png
```

命令：

```powershell
minikv_cli.exe
SHARDROUTETYPEBARRELSPLITNONPARTICIPATIONJSON
SHARDJSON
COMMANDSJSON

minikv_server.exe 17687 127.0.0.1
minikv_client.exe 127.0.0.1 17687 5000 --connect-retries 10 --retry-delay-ms 100
SHARDROUTETYPEBARRELSPLITNONPARTICIPATIONJSON
COMMANDS
QUIT
```

结果：CLI 验证新增独立命令、当前 `SHARDJSON` 的 v1385 status、命令目录暴露均正确。TCP smoke 验证新增独立命令和 `COMMANDS` 目录可经 TCP 读取，随后停止本轮启动的 server。完整 `SHARDJSON` 当前体量超过 bundled client 单行读取上限，因此 TCP smoke 使用新增独立只读命令覆盖本版行为，`SHARDJSON` 由 CLI 与 CTest 覆盖。

## 截图 05：git diff --check

文件：

```text
e/1385/图片/05-git-diff-check.png
```

命令：

```powershell
git diff --check
```

结果：exit code 为 0，没有 trailing whitespace 错误。截图中 Git 的 LF/CRLF 提示是本地换行规范提示，不是 whitespace gate 失败。

## 清理记录

- 截图临时 HTTP server 已停止，端口 `18085` 无保留进程。
- TCP smoke server 已停止，端口 `17687` 无保留进程。
- 截图由 Playwright MCP 生成，临时 HTML 和文本日志位于 `e/1385/_logs/`，最终 cleanup gate 会删除该临时目录。
