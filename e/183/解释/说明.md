# mini-kv v183 命令归档说明

v183 冻结 v182 route catalog test migration window，并把只读 Node route/catalog 兼容窗口扩展到 Node v463 route quality catalog integrity。它只发布版本化证据，不改变运行、路由、写入或执行边界。

## 01-cmake-configure-build.png

- 命令：配置并构建 `cmake-build-v183`，测试更新后追加 rebuild。
- 结果：构建成功。
- 意义：确认 v183 源码与测试可编译链接。

## 02-generate-shard-readiness-fixture.png

- 命令：通过 `minikv_cli.exe` 生成 `SHARDJSON` 并刷新 rolling fixture。
- 结果：`releaseVersion=v183`，status 为 `node-route-quality-catalog-integrity-window-read-only`，窗口覆盖 Node v433-v463，count/span 为 31，digest 为 `fnv1a64:8b252a90cb150a99`。
- 意义：证明 v463 route quality catalog integrity 只作为读证据进入窗口。

## 03-targeted-ctest.png

- 命令：定向运行 `command_tests` 和 `shard_readiness_tests`。
- 结果：2/2 通过。
- 意义：验证当前 JSON、冻结 v182 fixture、v463 window、31 span 和 no-execution 边界。

## 04-full-ctest.png

- 命令：运行全量 CTest。
- 结果：71/71 通过。
- 意义：确认窗口扩展没有影响其他模块。

## 05-tcp-smoke.png

- 命令：在 `127.0.0.1:6456` 启动服务端并通过客户端读取 `SHARDJSON`。
- 结果：TCP 响应包含 v183、v433-v463 window、count/span 31、`readOnly=true`、`executionAllowed=false` 和 digest `fnv1a64:8b252a90cb150a99`；服务端已停止。
- 意义：真实客户端路径仍只读、无执行。

## 清理记录

- 已停止：v183 TCP smoke 服务端、归档截图临时 HTTP 服务。
- 已删除：`cmake-build-v183`、`e/183/_logs`、`e/183/_render`、`.playwright-mcp`。
- 保留：`e/183/图片/`、本说明文档、v183 rolling fixture、冻结的 `fixtures/release/shard-readiness-v182.json`、源码/测试/README/代码讲解记录。
