# mini-kv v1417 command archive

本目录归档 mini-kv v1406-v1417 type barrel split follow-up fixture audit 的关键命令证据。

## 01-cmake-configure-build.png

命令：

```powershell
$cmake -S . -B cmake-build-v1417 -G Ninja -DCMAKE_BUILD_TYPE=Debug
$cmake --build cmake-build-v1417
```

结果：Debug 配置成功，最终构建为 `ninja: no work to do.`，说明截图前代码已经处于最新构建状态。CMake 输出包含 Windows 长路径 warning；这是既有长测试文件名带来的路径长度提示，不是本轮编译失败。

意义：证明新增 fixture audit 模块、命令注册、SHARDJSON 接入和测试目标已经进入 Debug 构建图。

## 02-full-ctest.png

命令：

```powershell
$ctest --test-dir cmake-build-v1417 -C Debug --output-on-failure
```

结果：`100% tests passed, 0 tests failed out of 266`。

意义：证明新增 5 条 fixture audit 测试、命令/line editor/SHARDJSON 测试更新、历史 fixture 读取测试和全量回归一起通过。

## 03-cli-smoke.png

命令：

```text
SHARDROUTETYPEBARRELSPLITFOLLOWUPFIXTUREAUDITJSON
COMMANDS
COMMANDSJSON
EXPLAINJSON SHARDROUTETYPEBARRELSPLITFOLLOWUPFIXTUREAUDITJSON
CHECKJSON SHARDROUTETYPEBARRELSPLITFOLLOWUPFIXTUREAUDITJSON
QUIT
```

结果：CLI 返回 `shard-route-preview-type-barrel-split-follow-up-fixture-audit.v1`，命令目录显示 `command_count=81`，`EXPLAINJSON` 显示 `mutates_store=false`、`touches_wal=false`，`CHECKJSON` 显示 `read_only=true`、`execution_allowed=false`。

意义：证明新增命令是独立可读的 metadata command，不写 store、不触 WAL、不执行 Node 检查。

## 04-tcp-smoke.png

命令：

```text
minikv_server.exe 6547 127.0.0.1
minikv_client.exe 127.0.0.1 6547
SHARDROUTETYPEBARRELSPLITFOLLOWUPFIXTUREAUDITJSON
COMMANDS
QUIT
```

结果：TCP 客户端读到同一份 fixture audit JSON 和 `command_count=81`，随后返回 `BYE`。本轮启动的 `minikv_server` 已在验证结束后停止。

意义：证明新命令不仅 CLI 可读，也能通过 TCP read-only command surface 读取；该 smoke 没有启动 Node/Java，没有打开 router/write/WAL/execution。

## 05-fixture-field-check.png

命令：读取 `fixtures/release/shard-readiness-v1417.json` 并检查核心字段。

结果：`releaseVersion=v1417`、`auditRelease=v1417`、`sourceFollowUpRelease=v1405`、`publishedStageCount=12`、`completedCheckCount=12`，并确认 `miniKvRuntimeReadsSourceFixturePayload=False`、`miniKvImportsNodeModules=False`、`miniKvExecutesNodeChecks=False`、`writeRoutingAllowed=False`、`touchesWal=False`、`executionAllowed=False`。

意义：证明 v1417 current fixture 已经把 v1405 follow-up 证据冻结为 source，同时继续保持只读、no Node import、no write、no WAL、no execution。

## 06-git-diff-check.png

命令：

```powershell
git diff --check
```

结果：退出码为 0。输出中只有 Windows 工作区的 LF/CRLF 提示，没有 trailing whitespace 或 whitespace error。

意义：证明本轮文档和代码没有复现此前提到的 trailing whitespace 类质量瑕疵。

## Cleanup note

本轮截图使用 Playwright MCP 访问临时 HTTP 页面生成 PNG。临时 HTTP server、`e/1417/_logs`、`.playwright-mcp` 和 `cmake-build-v1417` 会在最终 cleanup gate 中清理；保留本目录 PNG 与说明文档作为最终归档证据。
