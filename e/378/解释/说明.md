# mini-kv v378 command archive

Plan source: D:\nodeproj\orderops-node\docs\plans3\README.md and latest plans3 v549 roadmap were read before this run. The plan keeps mini-kv as recommended parallel work and does not require Java or mini-kv services to start.

Screenshot fallback note: command outputs are non-browser terminal logs, so this archive uses deterministic text-rendered PNGs instead of a browser screenshot session.

## 01-cmake-build.png

Command: CLion CMake configure when needed, then cmake --build bb --parallel 2.

Meaning: proves the C++ source, command wiring, and tests compile after v378 changes.

## 02-focused-tests.png

Command: focused CTest for archive closeout, archive, command, shard readiness, post-closeout continuity, and line editor completion tests.

Meaning: proves the new read-only archive closeout evidence surface and command metadata stay aligned.

## 03-full-ctest.png

Command: full CTest suite.

Meaning: proves v378 did not regress WAL, TCP, recovery, runtime evidence, credential resolver receipt, or existing shard readiness behavior.

## 04-cli-smoke.png

Command: minikv_cli.exe with SHARDJSON, SHARDROUTEVERIFYREPORTARCHIVEJSON, SHARDROUTEVERIFYREPORTARCHIVECLOSEOUTJSON, EXPLAINJSON, CHECKJSON, QUIT.

Meaning: proves the CLI exposes the archive closeout evidence as read-only metadata with execution_allowed=false.

## 05-tcp-smoke.png

Command: start minikv_server.exe on loopback, query through minikv_client.exe, then stop the server.

Meaning: proves TCP can read the same closeout evidence without enabling router, write, WAL, restore/load/compact, or execution authority. The smoke server was stopped during cleanup.
