# mini-kv v1585 命令归档说明

本归档对应 v1566-v1585：runtime execution packet approval gate archive verification non-participation。

归档根：

```text
e/1585/图片/
e/1585/解释/说明.md
```

截图通过 Playwright MCP 从临时本地只读 HTTP 页面生成。HTTP 页面只承载本轮命令日志渲染，不是产品服务；截图完成后应删除 `_logs` 并停止临时服务器。

## 01-cmake-configure.png

命令：

```powershell
$cmake -S . -B cmake-build-v1585 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug -DCMAKE_MAKE_PROGRAM="D:\CLION\CLion 2025.1.1\bin\mingw\bin\mingw32-make.exe"
```

结果：CMake configure/generate 完成，build files 写入 `cmake-build-v1585`。

意义：确认本版新增 core/stage/check/validation 源文件、五组测试目标和命令入口都能被 CMake 项目识别。截图中出现的 long object path warning 是既有 Windows 路径长度提示，本轮后续 build 和 308 条 CTest 已证明没有造成构建失败。

## 02-cmake-build.png

命令：

```powershell
$cmake --build cmake-build-v1585 -- -j 4
```

结果：构建完成，包含 `minikv`、`minikv_cli`、`minikv_server`、`minikv_client`、新增 `minikv_runtime_approval_gate_archive_np_*` 测试目标以及既有 WAL/TCP/restore 等测试目标。

意义：证明 v1566-v1585 的新增模块和旧模块测试 helper 收敛都能通过真实 MinGW/CMake 编译链。

## 03-targeted-tests.png

命令：

```powershell
$ctest --test-dir cmake-build-v1585 -R "runtime_approval_gate_archive_np|command_catalog_tests|command_tests|line_editor_tests|shard_readiness_tests|precheck_receipt_split_np_fixture_tests|blocked_rehearsal_split_np_fixture_tests|command_catalog_refactor_fixture_tests|..." --output-on-failure
```

结果：定向测试通过。

意义：覆盖新增 5 组 runtime approval gate archive non-participation 测试、命令目录、命令入口、当前 SHARDJSON、历史 fixture、line editor completion，以及旧 route-preview 测试中被修正的当前 status/source status 断言。

## 04-full-ctest.png

命令：

```powershell
$ctest --test-dir cmake-build-v1585 --output-on-failure
```

结果：308/308 tests passed。

意义：确认本版只读证据链没有影响 store、WAL、snapshot、TCP、restore、runtime smoke、历史 fixture 与当前 rolling guard。之前第一次全量 CTest 暴露出 12 个旧测试仍假设当前 status 必须以 `route-preview-` 开头；本轮已改为 helper 断言当前 v1585 status 和历史 v1545 source status，并经全量测试验证。

## 05-cli-smoke.png

命令：

```text
SHARDROUTERUNTIMEEXECUTIONPACKETAPPROVALGATEARCHIVEVERIFYNONPARTICIPATIONJSON
CHECKJSON SHARDROUTERUNTIMEEXECUTIONPACKETAPPROVALGATEARCHIVEVERIFYNONPARTICIPATIONJSON
COMMANDSJSON
QUIT
```

结果：CLI smoke 确认新增命令输出 v1 contract、`archiveVerificationReleaseVersion=v1585`、`readOnly=true`、`executionAllowed=false`，`CHECKJSON` 中 `execution_allowed=false`，并且 `COMMANDSJSON` 中存在新增 read command 条目。

意义：证明新增命令在真实 CLI 入口可读，且仍是 metadata read；没有启动服务、没有连接 sibling 项目、没有读取 raw endpoint/credential、没有写 store、没有写 WAL、没有执行 approval/runtime artifact。
