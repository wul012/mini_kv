# mini-kv v160 命令截图说明

本轮先只读确认 Node 当前有效计划为 `D:\nodeproj\orderops-node\docs\plans3\v418-post-sandbox-endpoint-credential-resolver-route-group-split-roadmap.md`。该计划是 Node 路由注册拆分维护版，不新增 evidence gate、不改 API path/response、不启动 Java 或 mini-kv，因此 mini-kv v160 选择做契约保持型维护：冻结 v159 证据，并把 `readOnlyBoundaryFields` 目录拆到独立 C++ 模块。

截图通过 Playwright MCP 对本地临时渲染页生成。`file://` 被 MCP 阻止后，本轮临时启动 `python -m http.server 8160 --bind 127.0.0.1` 只服务 `e/160/_render/`，截图完成后已停止该进程，并确认 8160 不再监听。

## 01-cmake-configure-generate-fixture.png

对应命令：

```powershell
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
& $cmake -S . -B cmake-build-v160 -G Ninja
"SHARDJSON`nQUIT`n" | .\cmake-build-v160\minikv_cli.exe
```

结果与意义：CMake 配置和生成成功；随后通过交互式 `SHARDJSON` 重新生成 `fixtures/release/shard-readiness.json`。新 current fixture 为 v160，包含 `boundaryCatalogMaintenance`，摘要 digest 为 `fnv1a64:c96ae9497cfd6386`。

## 02-target-build-ctest.png

对应命令：

```powershell
& $cmake --build cmake-build-v160 --target minikv_cli minikv_command_tests minikv_shard_readiness_tests
& $ctest --test-dir cmake-build-v160 -R "command_tests|shard_readiness_tests" --output-on-failure
```

结果与意义：定向构建通过，`command_tests` 与 `shard_readiness_tests` 共 2/2 通过。它们证明 v160 current fixture、`SHARDJSON` 命令输出、v159 冻结 fixture、`boundaryCatalogMaintenance`、只读/no-router/no-write/no-execution 字段都保持一致。

## 03-full-build-ctest.png

对应命令：

```powershell
& $cmake --build cmake-build-v160
& $ctest --test-dir cmake-build-v160 --output-on-failure
```

结果与意义：全量构建通过，71 条 CTest 全部通过。该结果证明 v160 的字段目录拆分没有影响 WAL、snapshot、restore、TCP、RESP、runtime smoke、managed audit non-participation receipts 等既有行为。

## 04-tcp-smoke-cleanup.png

对应命令：

```powershell
.\cmake-build-v160\minikv_server.exe 6431 127.0.0.1
# TCP 发送 SHARDJSON / GET missing:v160 / QUIT
```

结果与意义：临时启动的 `minikv_server` 在 6431 上返回了 v160 `SHARDJSON`，响应包含 `boundaryCatalogMaintenance` 和 `executionAllowed=false`；`GET missing:v160` 返回 `(nil)`。验证结束后已停止该 server，并确认 6424 与 6431 均无监听，说明本轮没有启动声明的批准包端口，也没有留下 mini-kv 后台进程。
