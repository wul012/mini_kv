# mini-kv v985 command archive

This archive records the key closeout command evidence for mini-kv v976-v985 candidate material submission precheck. Screenshots were generated through Playwright MCP from local temporary HTML pages. The temporary logs and HTTP pages were used only to produce the PNG evidence and are removed during cleanup.

## 01-cmake-configure.png

Command:

```text
cmake -S . -B cmake-build-v976-v985 -G Ninja
```

Result: CMake configuration and generation completed successfully. The configure phase still reports the existing long-object-path warnings for older long test names, but the build and CTest evidence below prove the generated build remains valid.

## 02-cmake-build.png

Command:

```text
cmake --build cmake-build-v976-v985
```

Result: The C++ implementation and the new tests compiled successfully. The archived build output reports `ninja: no work to do` because the tree had already been built before the final archive pass.

## 03-focused-ctest.png

Command:

```text
ctest --test-dir cmake-build-v976-v985 -R "candidate_material_submission_precheck|candidate_material_request_integrity|command_tests|line_editor_tests|shard_readiness_tests" --output-on-failure
```

Result: 13 focused tests passed. This covers the new material submission precheck modules, the v975 material request integrity source, command catalog wiring, line-editor completion, and `SHARDJSON` aggregation.

## 04-full-ctest.png

Command:

```text
ctest --test-dir cmake-build-v976-v985 --output-on-failure
```

Result: 186 tests passed. This proves the v985 read-only evidence chain did not regress WAL, snapshot, TCP, command, fixture, runtime boundary, or historical shard-preview tests.

## 05-cli-smoke.png

Command:

```text
COMMANDS
SHARDROUTECANDIDATEMATERIALSUBMISSIONPRECHECKJSON
SHARDJSON
QUIT
```

Result: CLI smoke verified that `SHARDROUTECANDIDATEMATERIALSUBMISSIONPRECHECKJSON` is exposed and returns contract `shard-route-preview-candidate-document-material-submission-precheck.v1`, source integrity `v975`, current release `v985`, 10 checkpoints, 10 validators, 41 gates, `nodeStopConditionRespected=true`, and disabled material/router/write/WAL/execution fields.

## 06-tcp-smoke.png

Command:

```text
minikv_server.exe 6585 127.0.0.1
minikv_client.exe 127.0.0.1 6585 5000
SHARDROUTECANDIDATEMATERIALSUBMISSIONPRECHECKJSON
QUIT
```

Result: TCP smoke returned the same v985 read-only material submission precheck evidence through the client/server path. The temporary TCP server was stopped after verification.

## Cleanup note

The archive intentionally keeps only this explanation and the six PNG screenshots under `e/985/图片/`. The temporary `_logs` folder, temporary HTTP server, Playwright snapshot directory, and `cmake-build-v976-v985` build directory are cleanup-gate artifacts and are removed before final handoff.
