# v1330 归档说明

本轮把 `v1296-v1330` 的 `catalog entry group split non-participation` 证据收口到版本化 fixture，并确认整条链路仍然是只读、无 router、无写入、无执行。

## 截图对应

1. `01-cmake-configure-build.png`
   - 命令：`cmake -S . -B cmake-build-v1330 -G "MinGW Makefiles"` 和 `cmake --build cmake-build-v1330 --parallel 4`
   - 结果：配置与构建成功；保留了一个非致命的长对象路径警告。
   - 意义：证明新模块、测试和命令面都能正常编译链接。

2. `02-targeted-ctest.png`
   - 命令：定向 `ctest`，覆盖新模块、`command_tests`、`shard_readiness_tests`、`line_editor_tests` 和相关 fixture。
   - 结果：38/38 通过。
   - 意义：先验证本版改动的主路径和受影响面。

3. `03-full-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v1330 --output-on-failure`
   - 结果：251/251 通过。
   - 意义：确认没有把旧的存储、WAL、TCP、恢复和受限命令行为带偏。

4. `04-cli-tcp-smoke.png`
   - 命令：`CHECKJSON SHARDROUTECATALOGENTRYGROUPSPLITNONPARTICIPATIONJSON` 先走 CLI，再走 TCP client。
   - 结果：两条路径都显示 `read_only=true`、`execution_allowed=false`、`write_command=false`、`touches_wal=false`、`append_when_enabled=false`；主 evidence 也保持 `readOnly=true`、`executionAllowed=false`、`touchesWal=false`。
   - 意义：真实运行层面确认这版只发布证据，不打开执行权。

## 清理

- 已生成并保留：`e/1330/图片/`、`e/1330/解释/说明.md`
- 临时文件：`e/1330/_logs/`、`tmp-http.pid`、`tmp-http-out.log`、`tmp-http-err.log`、`tmp-v1330-smoke.wal` 及其他临时日志将在收尾时删除
- 后续会停止本轮临时 HTTP 服务，再做 `git diff --check`、提交、打 tag、推送
