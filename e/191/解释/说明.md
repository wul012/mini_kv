# mini-kv v191 command archive

## 01-cmake-configure-build.png

Command: configure and build `cmake-build-v191` with the CLion bundled CMake, Ninja, and MinGW runtime on PATH.

Result: configuration completed and all v191 binaries were rebuilt from the updated sources.

Meaning: the cleanup closeout handoff manifest was verified from a fresh build.

## 02-shardjson-fixture.png

Command: run `minikv_cli.exe`, issue `SHARDJSON`, write `fixtures/release/shard-readiness.json`, and summarize the generated fixture.

Result: the generated fixture reports `releaseVersion=v191`, status `node-route-catalog-cleanup-closeout-handoff-read-only`, digest `fnv1a64:e6a06c9207144d9d`, Node window end `Node v472`, 40 covered Node route/catalog versions, handoff source `v190`, boundary catalog `read-only-boundary-fields.v19`, 597 fields, 30 groups, `readOnly=True`, and `executionAllowed=False`.

Meaning: the rolling fixture now exposes a downstream handoff over the frozen v190 evidence chain audit without adding runtime authority.

## 03-targeted-ctest.png

Command: run focused CTest for `command_tests` and `shard_readiness_tests`.

Result: both targeted suites passed.

Meaning: the direct command contract, frozen v190 fixture parity, handoff fields, route/catalog window, and boundary catalog counts are covered before broader regression testing.

## 04-full-ctest.png

Command: run the full CTest suite from `cmake-build-v191`.

Result: all 71 tests passed.

Meaning: v191 did not regress existing storage, WAL, snapshot, restore, TCP, command, retention, or release-evidence behavior.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6464`, request `SHARDJSON` through `minikv_client.exe`, summarize the response, and stop the server.

Result: TCP smoke returned v191 with the same read-only cleanup closeout handoff, `executionAllowed=False`, and the server was stopped after verification.

Meaning: the externally visible TCP command path matches the fixture and still opens no write, router, admin, LOAD, RESTORE, COMPACT, or execution behavior.
