# mini-kv v1491-v1505 命令归档说明

本轮归档使用 Playwright MCP 截图。`file://` 页面被浏览器环境拦截后，临时启动 `python -m http.server 8767 --bind 127.0.0.1` 服务 `_logs/pages`，截图完成后已停止该进程。

| 截图 | 命令/验证 | 结果和意义 |
|---|---|---|
| `01-cmake-configure-build.png` | `cmake -S . -B cmake-build-v1505 -G Ninja -DCMAKE_BUILD_TYPE=Debug` 与 `cmake --build cmake-build-v1505` | Debug 配置和构建通过。新增测试 target 已缩短为 `dfh_echo_split_np_*`，避免 Windows/Ninja 路径长度问题。 |
| `02-targeted-ctest.png` | `ctest --test-dir cmake-build-v1505 -R "(dfh_echo_split_np|command_tests|line_editor_tests|shard_readiness_tests|release_window_readiness_packet_split_non_participation_fixture_tests)" --output-on-failure` | 针对性 9/9 通过，覆盖新模块、命令目录、line editor 补全、SHARDJSON 聚合和 v1490 source fixture 冻结关系。 |
| `03-full-ctest.png` | `ctest --test-dir cmake-build-v1505 --output-on-failure` | 全量 291/291 通过，确认 v1505 current status 与旧证据链、旧 fixture guard、runtime evidence tests 不冲突。 |
| `04-cli-smoke.png` | CLI stdin 发送 `COMMANDS`、`SHARDROUTEDISABLEDFAKEHARNESSCONTRACTUPSTREAMECHOVERIFICATIONSPLITNONPARTICIPATIONJSON`、`SHARDJSON`、`EXIT` | CLI 真实读到 `command_count=86`、新 command contract、v1505 release、current status，并确认 endpoint/credential/history receipt parse、router/write/WAL/execution 都保持 false。 |
| `05-tcp-smoke.png` | 启动 `minikv_server.exe 6577 127.0.0.1`，TCP client 发送 `COMMANDS`、新 standalone command、`EXIT` | TCP 真实命令面读到 `command_count=86` 与 v1505 standalone evidence。`SHARDJSON` 当前约 2MB，TCP client 单行回显不适合作为 smoke，root `SHARDJSON` 由 CLI smoke 和 CTest 覆盖。服务端验证后已停止。 |
| `06-git-diff-check.png` | `git diff --check` | exit code 为 0，无 trailing whitespace。输出里的 CRLF conversion warning 是 Git 工作区换行提示，不是 whitespace 失败。 |

保留文件：`e/1505/图片/*.png` 与本说明文件。中间 `_logs` 和临时页面只用于截图，最终清理时删除。
