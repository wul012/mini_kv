# mini-kv v220 command archive explanation

## Version goal

v220 freezes the v219 continuity batch package as `fixtures/release/shard-readiness-v219.json`, then audits the v211-v219 post-closeout continuity package in the current rolling SHARDJSON fixture.

This version stays inside the evidence boundary requested by the Node v498 plan. It does not add a route, start Node or Java, open an active shard router, allow write routing, execute LOAD/RESTORE/COMPACT, read credential values, or claim fresh Node runtime consumption.

## Screenshots

1. `01-cmake-configure-build.png`
   - Command: CLion CMake configure plus Ninja build for `cmake-build-v220`.
   - Result: configure/generate completed and the build target was up to date after the v220 source and fixture generation pass.
   - Meaning: the v220 audit source is buildable with the local C++ toolchain before test evidence is accepted.

2. `02-targeted-ctest.png`
   - Command: targeted CTest filter `command_tests|shard_readiness_tests`.
   - Result: 2/2 tests passed.
   - Meaning: the SHARDJSON command path and fixture contract both accepted the v220 release, digest, frozen v219 source baseline, audit stage, sequence 20, range end v220, and the read-only/no-execution boundaries.

3. `03-full-ctest.png`
   - Command: full CTest run.
   - Result: 71/71 tests passed.
   - Meaning: the audit-only SHARDJSON update did not regress storage, WAL, snapshot, TCP, recovery, restore, runtime evidence, credential resolver, no-network, abort/rollback, or input-hardening tests.

4. `04-tcp-smoke.png`
   - Command: real `minikv_server.exe` plus `minikv_client.exe 127.0.0.1 6493`, sending `SHARDJSON` and `QUIT`.
   - Result: the parsed TCP output reported `releaseVersion=v220`, `evidenceDigest=fnv1a64:d9ecc44211f0fe02`, `continuityStage=post-closeout-continuity-v211-v219-batch-package-audit`, `sourceFrozenReleaseVersion=v219`, and `sourceFrozenDigest=fnv1a64:78b10360a474df83`.
   - Meaning: the v220 audit package is available through the real server/client read path, not only through the generated fixture file.

5. `05-evidence-summary.png`
   - Command: rendered summary of the parsed TCP smoke evidence.
   - Result: `readOnly=true`, `executionAllowed=false`, `routerActivationAllowed=false`, and `writeRoutingAllowed=false`.
   - Meaning: v220 is still a versioned evidence audit. It is not a router activation, write path, execution command, or runtime integration claim.

## Cleanup note

The temporary render server, TCP smoke server, `cmake-build-v220`, `e/220/_logs`, `e/220/_render`, and Playwright scratch files are removed during cleanup. The retained deliverables are the source changes, `fixtures/release/shard-readiness-v219.json`, the rolling fixture, screenshots in `e/220/图片/`, this explanation file, and the v220 code walkthrough.
