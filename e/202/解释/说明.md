# mini-kv v202 command archive

## 01-cmake-configure-build.png

Command: build `cmake-build-v202` with the CLion bundled CMake/Ninja toolchain and MinGW runtime on PATH.

Result and meaning: the full C++ target graph rebuilt after the source/test change. The final build links `minikv_cli.exe`, `minikv_server.exe`, command tests, shard readiness tests, and the rest of the test executables, so later fixture and CTest results are not using stale binaries.

## 02-shardjson-fixture.png

Command: run `minikv_cli.exe`, issue `SHARDJSON` through stdin, write `fixtures/release/shard-readiness.json`, and summarize the parsed JSON.

Result and meaning: the rolling fixture is v202 with digest `fnv1a64:cd0c634b2fc44eff`. The new continuity stage is `post-closeout-source-freeze-audit`, `stageSequence=2`, and the frozen source is v201. `historicalFallback` now points at `fixtures/release/shard-readiness-v201.json`; `readOnly=True` and `executionAllowed=False`.

## 03-targeted-ctest.png

Command: run `ctest -R "command_tests|shard_readiness_tests" --output-on-failure` from `cmake-build-v202`.

Result and meaning: the two focused suites passed 2/2. They prove the public `SHARDJSON` command and in-process formatter both publish the v202 source-freeze audit, frozen v201 fixture path, v201 fallback, unchanged boundary catalog v29 counts, and no router/write/execution fields.

## 04-full-ctest.png

Command: run the full CTest suite from `cmake-build-v202`.

Result and meaning: all 71 tests passed. This checks that the v202 read-only evidence update does not regress WAL, TCP, restore, fixture retention, runtime evidence, approval-boundary, or command behavior.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6475`, request `SHARDJSON` through `minikv_client.exe`, summarize the response, and stop the server.

Result and meaning: real TCP smoke returned v202, digest `fnv1a64:cd0c634b2fc44eff`, fallback v201, `readOnly=True`, `executionAllowed=False`, `routerActivationAllowed=False`, and `writeRoutingAllowed=False`. The temporary smoke server was stopped after verification.
