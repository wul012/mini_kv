# mini-kv v163 截图说明

本轮只读确认 Node 当前有效计划为 `D:\nodeproj\orderops-node\docs\plans3\v426-post-credential-resolver-runtime-shell-decision-route-group-split-roadmap.md`。该计划仍是 Node route registration 维护拆分，不新增 evidence gate、不改变 API path/response、不启动 Java 或 mini-kv，因此 mini-kv v163 继续按并行模式推进自己的只读证据：冻结 v162，并新增 `slotTablePreviewAudit`。

截图通过 Playwright MCP 对本地临时渲染页生成。`file://` 在本环境不可用，因此临时启动 `python -m http.server 8163 --bind 127.0.0.1` 服务 `e/163/_render/`；截图完成后该服务会在 cleanup gate 中停止并删除 `_render` 与 `_logs`。

## 01-cmake-configure-generate-fixture.png

对应命令：
```powershell
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
& $cmake -S . -B cmake-build-v163 -G Ninja
& $cmake --build cmake-build-v163 --target minikv_cli
"SHARDJSON`nQUIT`n" | .\cmake-build-v163\minikv_cli.exe
```

结果与意义：CMake 配置和 `minikv_cli` 构建可用，随后通过交互式 `SHARDJSON` 重新生成 `fixtures/release/shard-readiness.json`。新 current fixture 为 v163，状态为 `slot-table-preview-audit-read-only`，digest 为 `fnv1a64:f81c5b47cb396df6`；`slotTablePreviewAudit` 证明 16 槽连续、无重复、`executionAllowed=false`，`boundaryCatalogIndex` 为 `fieldCount=319`、`groupCount=17`。

## 02-targeted-ctest.png

对应命令：
```powershell
$ctest = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir cmake-build-v163 -R "^(command_tests|shard_readiness_tests)$" --output-on-failure
```

结果与意义：定向测试 2/2 通过。`command_tests` 与 `shard_readiness_tests` 证明 v163 current fixture、v162 frozen fixture、`slotTablePreviewAudit`、catalog v4/count、no-router/no-write/no-execution 字段在命令层和 fixture 层一致。

## 03-full-build-ctest.png

对应命令：
```powershell
& $cmake --build cmake-build-v163
& $ctest --test-dir cmake-build-v163 --output-on-failure
```

结果与意义：全量构建无新增工作，71 条 CTest 全部通过。说明 v163 的 slot preview audit 没有影响 WAL、snapshot、restore、TCP、RESP、runtime smoke、credential resolver non-participation receipts 等既有行为。

## 04-tcp-smoke-cleanup.png

对应命令：
```powershell
.\cmake-build-v163\minikv_server.exe 6436 127.0.0.1
# TCP 发送 SHARDJSON / GET missing:v163 / QUIT
```

结果与意义：临时启动的 `minikv_server` 在 6436 上返回 v163 `SHARDJSON`，响应包含 `slotTablePreviewAudit`、`observedSlotCount=16`、`contiguousSlotRange=true`、`duplicateSlotsDetected=false` 和 `executionAllowed=false`；`GET missing:v163` 返回 `(nil)`。验证结束后已停止该 server；本轮没有启动 6424 声明审批端口，没有打开 active router，也没有留下 mini-kv 后台进程。
