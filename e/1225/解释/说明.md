# mini-kv v1225 命令归档说明

本轮归档对应 v1196-v1225 production live capture archive non-participation 证据链。截图通过 Playwright MCP 临时页面生成，未保留中间 HTML 文件。

## 01-cmake-build.png

命令：

```powershell
$cmake -S . -B cmake-build-v1225 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
$cmake --build cmake-build-v1225 --config Debug --parallel 4
```

结果：配置和构建通过，`minikv`、`minikv_cli`、`minikv_server`、`minikv_client` 以及新增 production live capture archive non-participation 测试目标均被编译。

意义：证明新增 core/stages/checks/validation 模块已经进入 CMake 构建图，不是孤立文件。

## 02-targeted-ctest.png

命令：

```powershell
$ctest --test-dir cmake-build-v1225 -C Debug --output-on-failure -R "production_live_capture_archive_non_participation|..."
```

结果：13 个 targeted 测试全部通过。

意义：覆盖新命令、`SHARDJSON` 嵌入、line editor completion、v1196-v1225 fixture 链、新旧 production live capture 状态承接，以及相邻 rolling-status consumer。

## 03-full-ctest.png

命令：

```powershell
$ctest --test-dir cmake-build-v1225 -C Debug --output-on-failure
```

结果：236 个 CTest 全部通过。

意义：证明 v1225 archive non-participation 没有破坏既有 storage、WAL、TCP、RESP、recovery、runtime safety、approval boundary 和历史 fixture 测试。

## 04-cli-tcp-smoke.png

命令：

```powershell
minikv_cli.exe
SHARDROUTEPRODUCTIONLIVECAPTUREARCHIVENONPARTICIPATIONJSON

minikv_server.exe 41726 127.0.0.1 tmp-v1225-smoke.wal
minikv_client.exe 127.0.0.1 41726 5000 --connect-retries 10 --retry-delay-ms 100
SHARDROUTEPRODUCTIONLIVECAPTUREARCHIVENONPARTICIPATIONJSON
```

结果：CLI 和 TCP client 均读到 `productionLiveCaptureArchiveNonParticipationReleaseVersion=v1225`，并保留 `executionAllowed=false`、`writeRoutingAllowed=false`、`touchesWal=false`、`archiveEndpointRead=false`、`archivePayloadImported=false` 等边界字段。

意义：证明新 evidence 命令不只在单测中可用，真实 CLI/TCP 路径也能读取相同的只读证据。

## 清理记录

临时 TCP server 已停止，`tmp-v1225-smoke.wal` 已删除。本轮截图没有创建中间 HTML 文件。
