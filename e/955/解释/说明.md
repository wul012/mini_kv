# mini-kv v955 command evidence archive

## Scope

v931-v955 closes the read-only candidate intake packet handoff after the v930 candidate submission precheck fixture.  The archived evidence proves that the new packet remains a versioned contract surface only:

- no reviewed material is read or accepted;
- no document intake is opened;
- no document router is installed;
- no store write or WAL write is authorized;
- no runtime execution is allowed.

The cross-project planning input was read from:

- `D:\nodeproj\orderops-node\docs\plans\README.md`
- `D:\nodeproj\orderops-node\docs\plans3\v1421-controlled-read-only-shard-preview-candidate-document-intake-packet-closeout-roadmap.md`

## Screenshots

1. `01-cmake-configure-build.png`
   - Command: CLion bundled CMake configure and Ninja build for `cmake-build-v931-v955`.
   - Result: configure exit code 0 and build exit code 0.  CMake still reports the historical long-object-path warning for older long test target names; Ninja reports no work to do and no build failure.

2. `02-focused-ctest.png`
   - Command: focused CTest filter for `candidate_intake_packet`, `candidate_submission_precheck`, `command_tests`, `line_editor_tests`, and `shard_readiness_tests`.
   - Result: 14/14 tests passed.  This covers the new packet module, source precheck lineage, command catalog contract, shell completion, and SHARDJSON aggregation.

3. `03-full-ctest.png`
   - Command: full CTest run for the v955 build directory.
   - Result: 170/170 tests passed.  Existing storage, WAL, TCP, recovery, runtime safety, and all previous read-only evidence tests stayed green.

4. `04-cli-smoke.png`
   - Command: `minikv_cli.exe` with `COMMANDS`, `SHARDROUTECANDIDATEINTAKEPACKETJSON`, `SHARDJSON`, and `QUIT`.
   - Result: the command catalog includes 62 commands, the new command is read-only, and both direct packet output and SHARDJSON expose v955 with closed intake, no router, no write routing, no WAL touch, and no execution.

5. `05-tcp-smoke.png`
   - Command: temporary loopback `minikv_server.exe` on `127.0.0.1:6555`, queried by `minikv_client.exe` with `COMMANDS`, `SHARDROUTECANDIDATEINTAKEPACKETJSON`, and `QUIT`.
   - Result: the TCP path returns the same read-only packet evidence.  The temporary server was stopped after validation.

## Cleanup Note

The raw logs and temporary HTML files used to generate screenshots live under `e/955/_logs/` during the task and are removed during the cleanup gate.  The final preserved archive is `e/955/图片/` plus this explanation file.
