# mini-kv v1405 命令归档说明

本目录记录 v1386-v1405 type barrel split follow-up non-participation 批次的关键命令证据。截图由 Playwright MCP 对临时本地 HTTP 页面生成，HTTP server 仅用于截图，完成后已停止。

## 01-cmake-configure-build-debug.png

对应命令：

```text
cmake -S . -B cmake-build-v1405-debug -G Ninja -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_COMPILER=...
cmake --build cmake-build-v1405-debug
```

结果：配置成功，构建显示 `ninja: no work to do.`，说明 Debug 构建产物已是最新。日志里保留了既有 CMake 长路径 warning，但退出码为 0，没有编译失败。

意义：确认新增 follow-up 模块、命令接线、测试目标和 fixture 相关测试都能进入 Debug 构建。

## 02-debug-full-ctest.png

对应命令：

```text
ctest --test-dir cmake-build-v1405-debug --output-on-failure
```

结果：Debug 全量 CTest `261/261` 通过，截图使用压缩视图展示开头、重点 follow-up 测试和结尾汇总。完整日志在生成截图时使用，最终只保留截图证据。

意义：Debug 构建下 `assert` 生效，能真实覆盖 current fixture parity、命令目录、stage/check/validation、旧 v1385 source fixture 和新增 v1386-v1405 follow-up fixtures。

## 03-cli-smoke.png

对应命令：

```text
minikv_cli.exe
SHARDROUTETYPEBARRELSPLITFOLLOWUPNONPARTICIPATIONJSON
SHARDJSON
COMMANDSJSON
```

结果：检查项全部为 `True`，包括 follow-up contract、follow-up command、根 `releaseVersion=v1405`、根 section、COMMANDSJSON 目录项和 `executionAllowed=false`。

意义：证明新增 standalone read command、根 `SHARDJSON` 嵌入和命令目录在真实 CLI 路径可读，且没有打开执行能力。

## 04-tcp-smoke.png

对应命令：

```text
minikv_server.exe 6455 127.0.0.1
minikv_client.exe 127.0.0.1 6455
SHARDROUTETYPEBARRELSPLITFOLLOWUPNONPARTICIPATIONJSON
COMMANDS
QUIT
```

结果：临时 server PID 31072，follow-up contract、command、COMMANDS catalog entry 和 `executionAllowed=false` 全部为 `True`，验证后已停止 PID 31072。

意义：证明 TCP 服务路径可以读取 standalone follow-up 证据和命令目录。没有通过 TCP 读取完整 `SHARDJSON`，因为当前根 payload 很大，容易触发客户端行长限制，和本版行为无关。

## 05-fixture-field-check.png

对应命令：PowerShell 读取 `fixtures/release/shard-readiness-v1386.json` 到 `shard-readiness-v1405.json`，检查每个版本的 releaseVersion、follow-up section 和边界字段。

结果：v1386-v1405 的 `HasRelease`、`HasSection`、`HasBoundary` 全部为 `True`，并且 `shard-readiness.json` 与 `shard-readiness-v1405.json` 完全一致。

意义：证明版本化 fixture 链和 current fixture parity 已落地，且每个版本都保留 `executionAllowed=false`、`typeRouterInstalled=false`、`touchesWal=false` 等边界。

## 06-git-diff-check.png

对应命令：

```text
git diff --check
```

结果：退出码为 0。日志中的 LF/CRLF 是 Git 换行提示，不是 trailing whitespace 或 patch whitespace 错误。

意义：回应本批次质量提醒，确保新增代码讲解和文档没有 trailing whitespace 质量瑕疵。

## 进程清理

本批次启动过两个临时服务：

```text
minikv_server.exe PID 31072: TCP smoke 后已停止
python http.server: Playwright 截图用临时服务均已停止
```

没有留下需要继续运行的后台进程。
