# mini-kv v161 命令截图说明

本轮先只读确认 Node 当前有效计划为 `D:\nodeproj\orderops-node\docs\plans3\v420-post-credential-resolver-pre-implementation-readiness-route-group-split-roadmap.md`。该计划仍是 Node route registration 维护拆分，不新增 evidence gate、不改变 API path/response、不启动 Java 或 mini-kv，因此 mini-kv v161 继续做只读证据硬化：冻结 v160，并给边界字段目录增加结构化 index/count。

截图通过 Playwright MCP 对本地临时渲染页生成。`file://` 不可用，因此本轮临时启动 `python -m http.server 8161 --bind 127.0.0.1` 服务 `e/161/_render/`；截图完成后已停止该进程，并确认 8161 不再监听。

## 01-cmake-configure-generate-fixture.png

对应命令：

```powershell
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
& $cmake -S . -B cmake-build-v161 -G Ninja
& $cmake --build cmake-build-v161 --target minikv_cli
"SHARDJSON`nQUIT`n" | .\cmake-build-v161\minikv_cli.exe
```

结果与意义：CMake 配置、`minikv_cli` 构建和 current fixture 生成成功。新 `SHARDJSON` 为 v161，状态为 `boundary-field-catalog-index-read-only`，包含 `boundaryCatalogIndex`，`fieldCount=290`、`groupCount=15`，digest 为 `fnv1a64:2193962195ba633a`。

## 02-target-build-ctest.png

对应命令：

```powershell
& $cmake --build cmake-build-v161 --target minikv_command_tests minikv_shard_readiness_tests
& $ctest --test-dir cmake-build-v161 -R "command_tests|shard_readiness_tests" --output-on-failure
```

结果与意义：定向构建通过，`command_tests` 与 `shard_readiness_tests` 共 2/2 通过。它们证明 v161 current fixture、v160 frozen fixture、`boundaryCatalogIndex`、字段数量、分组数量、共享 vector 标记以及 no-router/no-write/no-execution 字段一致。

## 03-full-build-ctest.png

对应命令：

```powershell
& $cmake --build cmake-build-v161
& $ctest --test-dir cmake-build-v161 --output-on-failure
```

结果与意义：全量构建通过，71 条 CTest 全部通过。说明 v161 的 index/count 硬化没有影响 WAL、snapshot、restore、TCP、RESP、runtime smoke、managed audit non-participation receipts 等既有行为。

## 04-tcp-smoke-cleanup.png

对应命令：

```powershell
.\cmake-build-v161\minikv_server.exe 6432 127.0.0.1
# TCP 发送 SHARDJSON / GET missing:v161 / QUIT
```

结果与意义：临时启动的 `minikv_server` 在 6432 上返回 v161 `SHARDJSON`，响应包含 `boundaryCatalogIndex`、`fieldCount=290` 和 `executionAllowed=false`；`GET missing:v161` 返回 `(nil)`。验证结束后已停止该 server，并确认 6424 与 6432 均无监听，说明没有启动声明的批准包端口，也没有留下 mini-kv 后台进程。
