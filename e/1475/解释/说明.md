# mini-kv v1475 命令归档说明

本目录归档 v1473-v1475 implementation-plan upstream echo closeout non-participation 的关键命令证据。截图由 Playwright MCP 打开本地临时 HTTP 页面后生成；临时 HTTP server 只用于渲染日志截图，截图结束后已停止。

## 01-cmake-configure-build.png

命令：

```powershell
& $cmake -S . -B cmake-build-v1475 -G Ninja -DCMAKE_BUILD_TYPE=Debug
& $cmake --build cmake-build-v1475
```

结果：CMake configure/generate 成功，Ninja build 成功。日志中保留了既有长路径 warning，它不影响本次构建结果。

意义：证明新增 implementation-plan upstream echo closeout non-participation 模块、命令接线、测试目标和 fixture 读取测试都能在本地 Debug 构建中编译链接。

## 02-targeted-ctest.png

命令：

```powershell
& $ctest --test-dir cmake-build-v1475 -R "implementation_plan_upstream_echo_closeout_non_participation" --output-on-failure
& $ctest --test-dir cmake-build-v1475 -R "command_tests|line_editor_tests|shard_readiness_tests" --output-on-failure
```

结果：新增 5 个 implementation-plan upstream echo closeout non-participation 测试全部通过；命令目录、自动补全和 SHARDJSON 当前发布测试全部通过。

意义：证明 v1473-v1475 的 stage/check/validation/standalone JSON/fixture 链闭合，并且新命令被正确声明为 read/no mutate/no WAL。

## 03-full-ctest.png

命令：

```powershell
& $ctest --test-dir cmake-build-v1475 --output-on-failure
```

结果：281 条 CTest 全部通过。

意义：证明本轮 current SHARDJSON 从 v1472 滚到 v1475 后，没有破坏旧的 WAL、TCP、恢复、runtime receipt、历史 fixture 和旧 non-participation 模块测试。

## 04-cli-smoke.png

命令：

```text
SHARDROUTEIMPLEMENTATIONPLANUPSTREAMECHOCLOSEOUTNONPARTICIPATIONJSON
SHARDJSON
EXIT
```

结果：CLI 返回 `shard-route-implementation-plan-upstream-echo-closeout-non-participation.v1` JSON，并在 current `SHARDJSON` 中发布 `releaseVersion=v1475` 和 v1475 status。

意义：证明 standalone CLI 只读命令可用，且输出继续声明 `miniKvReadsEndpoints=false`、`miniKvReadsCredentials=false`、`writeRoutingAllowed=false`、`touchesWal=false`、`executionAllowed=false`。

## 05-tcp-smoke.png

命令：

```powershell
.\cmake-build-v1475\minikv_server.exe 6575 127.0.0.1
.\cmake-build-v1475\minikv_client.exe 127.0.0.1 6575
```

客户端输入：

```text
SHARDROUTEIMPLEMENTATIONPLANUPSTREAMECHOCLOSEOUTNONPARTICIPATIONJSON
EXIT
```

结果：TCP client 成功读取同一份只读 JSON，验证结束后记录 `serverStopped=true`。

意义：证明 TCP 路径也只是读取 metadata evidence，没有打开 router/write/WAL/execution 能力。server 仅为本地 smoke test 临时启动，归档后不保留后台进程。

## 06-git-diff-check.png

命令：

```powershell
git diff --stat
git diff --check
```

结果：`git diff --check` 退出码为 0；日志只有 Git 在 Windows 下提示 LF/CRLF 转换，没有 trailing whitespace 报错。

意义：证明本轮代码、fixture、文档和归档说明没有新增 whitespace 质量瑕疵。
