# mini-kv v184 命令归档说明

v184 冻结 v183 route quality catalog integrity window，并把只读 Node route/catalog 兼容窗口扩展到 Node v464 final closeout。它只发布版本化证据，不改变运行、路由、写入或执行边界。

## 01-cmake-configure-build.png

- 命令：配置并构建 `cmake-build-v184`，测试更新后追加 rebuild。
- 结果：构建成功。
- 意义：确认 v184 源码与测试可编译链接。

## 02-generate-shard-readiness-fixture.png

- 命令：通过 `minikv_cli.exe` 生成 `SHARDJSON` 并刷新 rolling fixture。
- 结果：`releaseVersion=v184`，status 为 `node-route-catalog-final-closeout-window-read-only`，窗口覆盖 Node v433-v464，count/span 为 32，digest 为 `fnv1a64:5b7cd9ee9a9f2524`。
- 意义：证明 Node v464 final closeout 已作为只读兼容证据归档。

## 03-targeted-ctest.png

- 命令：定向运行 `command_tests` 和 `shard_readiness_tests`。
- 结果：2/2 通过。
- 意义：验证当前 JSON、冻结 v183 fixture、v464 window、32 span 和 no-execution 边界。

## 04-full-ctest.png

- 命令：运行全量 CTest。
- 结果：71/71 通过。
- 意义：确认最终窗口扩展没有影响其他模块。

## 05-tcp-smoke.png

- 命令：在 `127.0.0.1:6457` 启动服务端并通过客户端读取 `SHARDJSON`。
- 结果：TCP 响应包含 v184、v433-v464 window、count/span 32、`readOnly=true`、`executionAllowed=false` 和 digest `fnv1a64:5b7cd9ee9a9f2524`；服务端已停止。
- 意义：真实客户端路径仍只读、无执行。

## 清理记录

- 已停止：v184 TCP smoke 服务端、归档截图临时 HTTP 服务。
- 已删除：`cmake-build-v184`、`e/184/_logs`、`e/184/_render`、`.playwright-mcp`。
- 保留：`e/184/图片/`、本说明文档、v184 rolling fixture、冻结的 `fixtures/release/shard-readiness-v183.json`、源码/测试/README/代码讲解记录。
