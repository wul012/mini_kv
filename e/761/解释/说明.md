# mini-kv v761 command archive

截图生成方式：本轮关键证据是本地 CMake/CTest/CLI/TCP 命令输出，不是浏览器页面。Playwright/MCP 没有稳定页面上下文可截图，因此退回为文本日志渲染 PNG；原始临时日志在 cleanup gate 中删除，只保留 PNG 证据。

## 01-cmake-configure.png

命令：使用 CLion 捆绑 CMake、Ninja 和 MinGW g++ 配置 `cmake-build-v761`。

结果：配置和生成通过，构建文件写入 `D:/C/mini-kv/cmake-build-v761`。

意义：确认新增 draft instruction preflight 模块、测试注册和生成头文件路径可以被 CMake 正确识别。

## 02-cmake-build.png

命令：`cmake --build cmake-build-v761`。

结果：归档时构建已是最新状态，Ninja 报告 `no work to do`；此前同一 build dir 已完成完整编译。

意义：确认 v761 源码与测试目标处于已构建状态，没有遗留未编译改动。

## 03-focused-ctest.png

命令：运行 `instruction_preflight|authoring_readiness|artifact_preflight|command_tests|line_editor_tests|shard_readiness_tests` focused CTest。

结果：21/21 通过。

意义：验证新增命令、拆分模块、SHARDJSON 嵌入、命令目录、补全，以及上游 artifact/authoring readiness 旧证据仍保持只读边界。

## 04-full-ctest.png

命令：运行完整 CTest。

结果：125/125 通过。

意义：验证 v761 没有破坏 WAL、TCP、restore、runtime receipt、fixture guard 等既有行为。

## 05-cli-smoke.png

命令：通过 `minikv_cli.exe` 执行 `SHARDROUTEVALUESUPPLYSIGNEDAPPROVALCAPTUREARTIFACTDRAFTINSTRUCTIONPREFLIGHTJSON`、`SHARDJSON`、`QUIT`。

结果：输出包含 `instructionPreflightReleaseVersion=v761`、`draftInstructionPreflightOnly=true`、`draftInstructionTextGenerated=false`、`draftInstructionTextMaterialized=false`、`approvalGrantEmitted=false`、`writeRoutingAllowed=false`、`touchesWal=false`、`executionAllowed=false`。

意义：证明真实 CLI 可以读取 v761 evidence，同时没有指令文本生成、grant、写路由、WAL touch 或执行授权。

## 06-tcp-smoke.png

命令：启动 `minikv_server.exe` 监听 `127.0.0.1:17611`，通过 `minikv_client.exe` 发送 instruction preflight 命令和 `QUIT`。

结果：客户端连接成功并读取 v761 只读证据；验证后 server PID 已停止，`server_stopped=True`。

意义：证明 TCP 路径也能读取新命令，同时本轮未留下后台服务进程。
