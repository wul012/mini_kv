# mini-kv v189 command archive

This archive records the v189 closeout commands for the versioned Node route catalog evidence chain.

## 01-cmake-configure-build.png

Command: configure and build with the CLion CMake/Ninja/MinGW toolchain.

Result: CMake configured `cmake-build-v189` and built the new route catalog evidence-chain module, binaries, and tests successfully.

Meaning: the new module is part of the compiled mini-kv library and not merely documentation.

## 02-shardjson-fixture.png

Command: run `SHARDJSON` through `minikv_cli.exe` and write the result to `fixtures/release/shard-readiness.json`.

Result: the rolling fixture reports `releaseVersion=v189`, `status=node-route-catalog-evidence-chain-read-only`, digest `fnv1a64:a595f2d9cdcbac8d`, `catalogedReleaseCount=4`, latest cataloged release `v188`, boundary catalog `read-only-boundary-fields.v17`, `fieldCount=547`, `groupCount=28`, and `executionAllowed=false`.

Meaning: v189 publishes a versioned v185-v188 route catalog evidence chain without runtime authority.

## 03-targeted-ctest.png

Command: run focused CTest for `command_tests` and `shard_readiness_tests`.

Result: 2/2 focused tests passed.

Meaning: command-level and fixture-level route catalog chain assertions agree.

## 04-full-ctest.png

Command: run full CTest from `cmake-build-v189`.

Result: 71/71 tests passed.

Meaning: the new evidence-chain module did not regress storage, WAL, TCP, restore, runtime evidence, or receipt behavior.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6462`, run `minikv_client.exe` with `SHARDJSON` and `QUIT`, validate the JSON response, and stop the server.

Result: TCP smoke returned `releaseVersion=v189`, `chainMode=node-route-catalog-evidence-chain-read-only`, `catalogedReleaseCount=4`, boundary catalog v17, `fieldCount=547`, `readOnly=true`, and `executionAllowed=false`.

Meaning: the network path exposes the same read-only route catalog evidence chain and leaves no service running.
