# v1260 归档说明

## 01-cmake-build.png
对应 `cmake -S . -B cmake-build-v1260 -G 'MinGW Makefiles'` 和 `cmake --build cmake-build-v1260 --target minikv_cli -- -j 6`。
结果是配置成功, `minikv_cli` 和基础库都完成构建。

## 02-targeted-ctest.png
对应 targeted CTest。
结果是 17 个相关测试全过, 覆盖新模块、命令目录、补全和受影响的旧回归面。

## 03-full-ctest.png
对应全量 CTest。
结果是 241 个测试全部通过, 说明新版本没有把更广的 route / WAL / TCP / recovery 面带歪。

## 04-cli-tcp-smoke.png
对应 CLI 和 TCP smoke。
结果是 `CHECKJSON SHARDROUTEPRODUCTIONLIVECAPTUREARCHIVEVERIFYNONPARTICIPATIONJSON` 在本地 CLI 和 TCP client 上都能正常返回只读 contract, 且 `read_only=true`、`execution_allowed=false`、`write_command=false`。

## 清理
本轮 smoke 使用的临时 WAL 和 server 进程已在验证后清理。
