# mini-kv v190 command archive

## 01-cmake-configure-build.png

Command: configure and build `cmake-build-v190` with the CLion bundled CMake, Ninja, and MinGW runtime on PATH.

Result: configuration completed and the v190 test/CLI/server binaries were rebuilt from the updated C++ sources.

Meaning: the route catalog evidence chain audit was validated from fresh objects, not a stale executable.

## 02-shardjson-fixture.png

Command: run `minikv_cli.exe SHARDJSON`, write `fixtures/release/shard-readiness.json`, and summarize the generated fixture.

Result: the generated fixture reports `releaseVersion=v190`, status `node-route-catalog-evidence-chain-audit-read-only`, digest `fnv1a64:b24193bae62876e6`, Node window end `Node v472`, 40 covered Node route/catalog versions, `nodeRouteCatalogEvidenceChainAudit`, boundary catalog `read-only-boundary-fields.v18`, 573 fields, 29 groups, `readOnly=True`, and `executionAllowed=False`.

Meaning: the rolling fixture now freezes v189 as the audit source and exposes only versioned read-only evidence.

## 03-targeted-ctest.png

Command: run focused CTest for `command_tests` and `shard_readiness_tests`.

Result: both targeted suites passed.

Meaning: the direct `SHARDJSON` command contract, frozen fixture parity, Node v472 route/catalog window, evidence chain audit fields, and boundary catalog counts are covered before broader regression testing.

## 04-full-ctest.png

Command: run the full CTest suite from `cmake-build-v190`.

Result: all 71 tests passed.

Meaning: v190 did not regress existing storage, WAL, snapshot, restore, TCP, command, retention, or release-evidence behavior.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6463`, request `SHARDJSON` through `minikv_client.exe`, summarize the response, and stop the server.

Result: TCP smoke returned v190 with the same read-only audit evidence, `executionAllowed=False`, and the server was stopped after verification.

Meaning: the externally visible TCP command path matches the fixture and still opens no write, router, admin, LOAD, RESTORE, COMPACT, or execution behavior.
