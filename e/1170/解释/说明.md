# mini-kv v1170 command archive

This archive records the final verification for mini-kv v1161-v1170, which adds read-only `SHARDROUTEVALUESUPPLYPROFILESECTIONINTEGRITYJSON` evidence and embeds `shardPreviewOperatorValueSupplyProfileSectionIntegrity` in `SHARDJSON`.

## 01-cmake-configure.png

Command:

```text
cmake -S . -B cmake-build-v1170 -G Ninja -DCMAKE_C_COMPILER=...gcc.exe -DCMAKE_CXX_COMPILER=...g++.exe
```

Result: CMake configured and generated the v1170 Ninja build directory successfully. The historical long object-path warning remains informational for this repository shape and did not fail configuration.

Meaning: verifies the final source tree can be configured with the local CLion MinGW/Ninja toolchain after the v1170 source, fixture, test, README, and walkthrough updates are in place.

## 02-cmake-build.png

Command:

```text
cmake --build cmake-build-v1170
```

Result: build completed with no work left to do after the final rebuild.

Meaning: confirms the C++ targets, including the new integrity formatter, stage catalog, check catalog, validation module, command wiring, and tests, are buildable in the final state.

## 03-targeted-ctest.png

Command:

```text
ctest --test-dir cmake-build-v1170 -R operator_value_supply_profile_section_integrity|...
```

Result: 24/24 targeted tests passed.

Meaning: verifies the new integrity section, command catalog wiring, line-editor completion, rolling `SHARDJSON`, and related source-profile fixture expectations. This pass also confirms the generated fixtures are byte-for-byte compatible with the formatter and are UTF-8 without BOM.

## 04-full-ctest.png

Command:

```text
ctest --test-dir cmake-build-v1170 --output-on-failure
```

Result: 226/226 tests passed.

Meaning: confirms the v1170 evidence update did not regress the broader mini-kv surface, including store behavior, WAL, snapshot, TCP, RESP, restore/retention evidence, runtime non-participation receipts, and older route-preview evidence chains.

## 05-cli-smoke.png

Command:

```text
minikv_cli.exe
SHARDROUTEVALUESUPPLYPROFILESECTIONINTEGRITYJSON
SHARDJSON
QUIT
```

Result: CLI smoke passed, capturing the v1170 integrity command and aggregate `SHARDJSON` output.

Meaning: proves the executable CLI path exposes the new read-only integrity contract, the v1160 source profile-section fixture, the v1170 release marker, SHARDJSON embedding, and the false boundary fields for Node renderer execution, Node catalog import, operator value import, runtime payload acceptance, secret/raw endpoint handling, router activation, write routing, WAL touch, and execution authority.

## 06-tcp-smoke.png

Command:

```text
minikv_server.exe 6770 127.0.0.1
minikv_client.exe 127.0.0.1 6770 5000 --connect-retries 20 --retry-delay-ms 100
SHARDROUTEVALUESUPPLYPROFILESECTIONINTEGRITYJSON
QUIT
```

Result: TCP smoke passed on `127.0.0.1:6770`.

Meaning: proves the loopback service path returns the new integrity command as read-only metadata without importing operator values, accepting runtime payloads, storing raw endpoints, installing routers, routing writes, touching WAL, or granting execution.

## Process Notes

A temporary HTTP server was started only to let Playwright MCP screenshot the generated log pages:

```text
python -m http.server 8770 --bind 127.0.0.1 --directory e/1170/_logs/pages
```

The server was stopped after screenshot generation. No mini-kv server process or temporary HTTP process was intentionally left running.
