# mini-kv v187 command archive

This archive records the v187 closeout commands for the read-only Node v471 route catalog expected integrity snapshot audit.

## 01-cmake-configure-build.png

Command: configure and build with the CLion CMake/Ninja/MinGW toolchain.

Result: CMake configured `cmake-build-v187` and rebuilt the changed mini-kv sources, CLI/server binaries, and test binaries successfully.

Meaning: the v187 C++ changes compile cleanly before any fixture or test evidence is trusted.

## 02-shardjson-fixture.png

Command: run `SHARDJSON` through `minikv_cli.exe` and write the result to `fixtures/release/shard-readiness.json`.

Result: the rolling fixture reports `releaseVersion=v187`, `status=node-route-catalog-expected-integrity-snapshot-audit-read-only`, digest `fnv1a64:f4459b7beacceedd`, Node window end `Node v471`, `splitRouteGroupCount=39`, boundary catalog `read-only-boundary-fields.v15`, `fieldCount=502`, and `executionAllowed=false`.

Meaning: the generated fixture is the v187 evidence payload and proves the new Node v471 audit remains read-only.

## 03-targeted-ctest.png

Command: run focused CTest for `command_tests` and `shard_readiness_tests`.

Result: 2/2 focused tests passed.

Meaning: the command processor, CLI fixture contract, frozen v186 reference, route/catalog window, v471 audit fields, and boundary catalog assertions agree.

## 04-full-ctest.png

Command: run full CTest from `cmake-build-v187`.

Result: 71/71 tests passed.

Meaning: the v187 shard-readiness changes did not regress WAL, snapshot, TCP, restore, runtime evidence, or receipt tests.

## 05-tcp-smoke.png

Command: start `minikv_server.exe` on `127.0.0.1:6460`, run `minikv_client.exe` with `SHARDJSON` and `QUIT`, validate the JSON response, and stop the server.

Result: TCP smoke returned `releaseVersion=v187`, Node window end `Node v471`, `splitRouteGroupCount=39`, `expectedIntegritySnapshotFactoryMovedByNodeV471=true`, `readOnly=true`, `executionAllowed=false`, and the server was stopped.

Meaning: the network path exposes the same read-only evidence as the CLI path without leaving a service running.
