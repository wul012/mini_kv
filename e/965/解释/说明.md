# mini-kv v965 command evidence archive

## Scope

v956-v965 closes the read-only candidate material request package after the v955 candidate intake packet.  The archived evidence proves that the new request layer is still a contract surface only:

- the external reviewed material is requested but absent;
- no reviewed real candidate document is read;
- no document intake is opened;
- no candidate payload is imported or evaluated;
- no document router is installed;
- no store write or WAL write is authorized;
- no runtime execution is allowed.

The cross-project planning input was read from:

- `D:\nodeproj\orderops-node\docs\plans\README.md`
- `D:\nodeproj\orderops-node\docs\plans3\v1446-controlled-read-only-shard-preview-candidate-document-material-request-closeout-roadmap.md`

## Screenshots

1. `01-cmake-configure-build.png`
   - Command: CLion bundled CMake configure and Ninja build for `cmake-build-v956-v965`.
   - Result: configure exit code 0 and build exit code 0.  CMake still reports the historical long-object-path warning for older long test target names; Ninja reports no work to do and no build failure.

2. `02-focused-ctest.png`
   - Command: focused CTest filter for `candidate_material_request`, `candidate_intake_packet`, `command_tests`, `line_editor_tests`, and `shard_readiness_tests`.
   - Result: 15/15 tests passed.  This covers the new material request module, v955 intake packet lineage, command catalog contract, shell completion, SHARDJSON aggregation, and fixture parity.

3. `03-full-ctest.png`
   - Command: full CTest run for the v965 build directory.
   - Result: 176/176 tests passed.  Existing storage, WAL, TCP, recovery, runtime safety, and all prior read-only evidence tests stayed green.

4. `04-cli-smoke.png`
   - Command: `minikv_cli.exe` with `COMMANDS`, `SHARDROUTECANDIDATEMATERIALREQUESTJSON`, `SHARDJSON`, and `QUIT`.
   - Result: the command catalog includes 63 commands, the new command is read-only, and both direct material request output and SHARDJSON expose v965 with closed intake, no material read, no router, no write routing, no WAL touch, and no execution.

5. `05-tcp-smoke.png`
   - Command: temporary loopback `minikv_server.exe` on `127.0.0.1:6565`, queried by `minikv_client.exe` with `COMMANDS`, `SHARDROUTECANDIDATEMATERIALREQUESTJSON`, and `QUIT`.
   - Result: the TCP path returns the same read-only material request evidence.  The temporary server was stopped after validation.

## Cleanup Note

The raw logs and temporary HTML files used to generate screenshots live under `e/965/_logs/` during the task and are removed during the cleanup gate.  The final preserved archive is `e/965/图片/` plus this explanation file.
