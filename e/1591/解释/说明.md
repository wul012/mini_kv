# v1591 命令归档说明

本目录归档 v1586-v1591 代码讲解质量门禁非参与证据的关键命令截图。截图通过临时本地 HTTP 页面和 Playwright MCP 生成；HTTP server 仅用于渲染命令输出页面，归档完成后关闭。

## 01-cmake-configure.png

命令：`cmake -S . -B cmake-build-v1591 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`

意义：确认 v1591 使用 CLion 捆绑 CMake/MinGW 工具链完成配置，新的 `shard_preview_code_walkthrough_quality_gate_non_participation` 源文件和测试目标已进入构建图。

## 02-cmake-build.png

命令：`cmake --build cmake-build-v1591 -- -j 4`

意义：确认新增 core/stage/check/validation 模块、命令接入、readiness 聚合和测试目标全部可编译。此步骤在 C++ 和测试头文件修改后重新执行，避免旧测试二进制误判。

## 03-targeted-ctest.png

命令：`ctest --test-dir cmake-build-v1591 -R "code_walkthrough_quality_gate|runtime_approval_gate_archive_np|command_catalog|command_tests|line_editor|shard_readiness_tests" --output-on-failure`

意义：定向验证 15 条关键测试全部通过，覆盖新质量门禁模块、v1585 源证据衔接、命令目录、命令执行、补全和 `SHARDJSON` 当前状态滚动。

## 04-full-ctest.png

命令：`ctest --test-dir cmake-build-v1591 --output-on-failure`

意义：全量 313 条 CTest 通过，证明本批没有破坏旧的存储、WAL、snapshot、TCP、restore、receipt、fixture 和历史证据测试。

## 05-cli-smoke.png

命令：通过 `minikv_cli.exe` 依次执行：

```text
SHARDCODEWALKTHROUGHQUALITYGATENONPARTICIPATIONJSON
CHECKJSON SHARDCODEWALKTHROUGHQUALITYGATENONPARTICIPATIONJSON
SHARDJSON
COMMANDSJSON
QUIT
```

意义：真实 CLI smoke 证明新增命令返回 v1591 质量门禁证据、validation 通过、`readOnly=true`、`executionAllowed=false`、`writeRoutingAllowed=false`、`touchesWal=false`；`CHECKJSON` 证明该命令不写 store、不碰 WAL；`SHARDJSON` 证明当前 release/status 和 command count 滚到 v1591/90；`COMMANDSJSON` 证明命令目录包含新增命令。
