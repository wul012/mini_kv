# mini-kv v1526-v1545 命令归档说明

本轮归档使用 Playwright MCP 对本地 HTML 日志页面截图。临时 HTTP server 仅用于截图页面访问，截图完成后已停止。TCP smoke 中临时启动的 `minikv_server.exe` 也已在验证结束后停止。

## 01-cmake-configure-build.png

命令：

```powershell
cmake -S . -B cmake-build-v1545 -G Ninja
cmake --build cmake-build-v1545
```

结果：CMake 配置成功，Ninja 构建成功，最终为 `ninja: no work to do.`。输出中的 CMake 长路径 warning 是本仓库既有 Windows 对象文件路径提示，不影响本次构建退出码。

意义：证明 v1526-v1545 新增 core/stage/check/validation 源文件、命令接线和测试目标都能被当前 CLion/CMake/Ninja 工具链识别并构建。

## 02-targeted-ctest.png

命令：

```powershell
ctest --test-dir cmake-build-v1545 -R "(precheck_receipt_split_np|command_tests|line_editor_tests|shard_readiness_tests|blocked_rehearsal_split_np_fixture_tests)" --output-on-failure
```

结果：9/9 通过。

意义：覆盖新增 precheck upstream receipt verification split non-participation 模块、命令目录、命令执行、补全、SHARDJSON 嵌入，以及 v1525 source fixture 与 v1545 current fixture 的联动。

## 03-full-ctest.png

命令：

```powershell
ctest --test-dir cmake-build-v1545 --output-on-failure
```

结果：301/301 通过，`100% tests passed, 0 tests failed out of 301`。

意义：证明新增 v1526-v1545 证据链没有破坏旧模块、旧 fixture、TCP/RESP、WAL、snapshot、runtime receipt、restore evidence 等既有测试面。原始 full CTest 日志页面过长，Playwright 无法对完整长页面进行 full-page screenshot，因此截图使用摘要页，摘要页保留 301/301 结果和尾部日志；完整原始日志仅作为中间截图材料存放在 `_logs`，后续 cleanup 会删除。

## 04-cli-smoke.png

命令流：

```text
SHARDROUTEPRECHECKUPSTREAMRECEIPTVERIFICATIONSPLITNONPARTICIPATIONJSON
COMMANDS
CHECKJSON SHARDROUTEPRECHECKUPSTREAMRECEIPTVERIFICATIONSPLITNONPARTICIPATIONJSON
EXIT
```

结果：CLI smoke 通过，检查到新合约、新 release version `v1545`、source `v1525`、`miniKvReadsEndpoints=false`、`writeRoutingAllowed=false`、`executionAllowed=false`、COMMANDS 只读目录项，以及 CHECKJSON 的 `read_only=true` / `execution_allowed=false`。

意义：证明新命令不是只在单元测试中可调用，真实 CLI 路径也能读取只读证据，并且命令目录与 CHECKJSON 风险面一致。

## 05-tcp-smoke.png

命令：

```text
minikv_server.exe 6545 127.0.0.1
minikv_client.exe 127.0.0.1 6545 5000 --connect-retries 10 --retry-delay-ms 100
```

客户端命令流：

```text
COMMANDS
SHARDROUTEPRECHECKUPSTREAMRECEIPTVERIFICATIONSPLITNONPARTICIPATIONJSON
EXIT
```

结果：TCP smoke 通过，检查到新命令目录项、合约、`v1545`、source `v1525`、`activeRouterInstalled=false`、`writeRoutingAllowed=false`、`executionAllowed=false` 和 `BYE`。临时 server PID 已记录并停止。

意义：证明新证据命令在 TCP 客户端路径也可读取，且没有启动额外 sibling service、没有写入、没有 router 或 WAL 行为。

## 06-git-diff-check.png

命令：

```powershell
git diff --check
```

结果：退出码为 0。输出只包含 Windows 工作区 CRLF 提示，不包含 trailing whitespace 或 conflict marker。

意义：证明本轮源码、测试、README、讲解和 fixture 更新没有引入 `git diff --check` 会阻断的空白质量问题。CRLF 提示属于 Git 工作区换行提示，不是质量失败。
