# mini-kv v225 command archive explanation

## Version goal

v225 catalogs the v221-v224 Node v505 readiness handoff archive verification route evidence releases from frozen v224 evidence.

This version stays inside the evidence boundary requested by the Node v505 plan. It does not add a route, start Node or Java, open an active shard router, allow write routing, execute LOAD/RESTORE/COMPACT, read credential values, or claim fresh Node runtime consumption.

## Screenshots

1. `01-cmake-configure-build.png`
   - Command: CLion CMake configure plus Ninja build for `cmake-build-v225`.
   - Result: configure/generate/build completed for the v225 source.
   - Meaning: the versioned evidence source is buildable before any test evidence is accepted.

2. `02-targeted-ctest.png`
   - Command: targeted CTest filter `command_tests|shard_readiness_tests`.
   - Result: 2/2 tests passed.
   - Meaning: the command output and fixture contract accepted v225, frozen v224 source evidence, the v505 plan path, stage sequence 25, and no-router/no-write/no-execution flags.

3. `03-full-ctest.png`
   - Command: full CTest run.
   - Result: 71/71 tests passed.
   - Meaning: the evidence-only change did not regress storage, WAL, snapshot, TCP, recovery, restore, runtime evidence, credential resolver, no-network, abort/rollback, or input-hardening tests.

4. `04-tcp-smoke.png`
   - Command: real `minikv_server.exe` plus `minikv_client.exe 127.0.0.1 6498`, sending `SHARDJSON` and `QUIT`.
   - Result: TCP reported `releaseVersion=v225`, `evidenceDigest=fnv1a64:b720e589ca8d9bc7`, `continuityStage=post-closeout-continuity-node-v505-readiness-handoff-archive-verification-route-release-catalog`, `sourceFrozenReleaseVersion=v224`, and `sourceFrozenDigest=fnv1a64:011a92d8d38f3557`.
   - Meaning: the package is exposed through the real server/client read path, not only through a generated fixture file.

5. `05-evidence-summary.png`
   - Command: rendered summary of parsed TCP smoke evidence.
   - Result: `readOnly=true`, `executionAllowed=false`, `routerActivationAllowed=false`, and `writeRoutingAllowed=false`.
   - Meaning: v225 is a versioned evidence package, not an activation switch, write route, execution command, or runtime integration claim.

## Cleanup note

The temporary render server, TCP smoke server, `cmake-build-v225`, `e/225/_logs`, `e/225/_render`, and Playwright scratch files are removed during cleanup. The retained deliverables are the source changes, `fixtures/release/shard-readiness-v224.json`, the rolling fixture, screenshots in `e/225/图片/`, this explanation file, and the v225 code walkthrough.

Screenshot note: `04-tcp-smoke.png` and `05-evidence-summary.png` were captured with the Playwright MCP viewport screenshot path after Chromium rejected full-page capture for the long TCP smoke page. The underlying TCP log and parsed summary passed before screenshotting.
