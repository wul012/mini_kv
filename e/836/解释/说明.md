# mini-kv v836 命令归档说明

本版归档使用 Playwright MCP 截取临时本地 HTML 日志页。临时 HTTP server 仅服务 `e/836/_logs`，截图完成后已停止并删除 `_logs`，最终保留本说明和 `e/836/图片/`。

## 01-cmake-configure.png

命令：`cmake -S . -B cmake-build-v836 -G "MinGW Makefiles"`

结果：配置成功，生成 `cmake-build-v836` 构建目录。

意义：确认 v836 新增 comparison closeout audit 模块、缩短后的测试目标名、CMake 注册块可以被 MinGW/CMake 正确识别。

## 02-cmake-build.png

命令：`cmake --build cmake-build-v836`

结果：构建成功，`minikv` 静态库、CLI、server/client 和全部测试目标均完成编译。

意义：证明 v836 新增源文件、头文件、命令注册、`SHARDJSON` 聚合和压短后的 comparison audit 测试文件在真实工具链下可编译。

## 03-focused-ctest.png

命令：`ctest --test-dir cmake-build-v836 --output-on-failure -R "tp_comp_audit|command_tests|shard_readiness_tests|line_editor_tests"`

结果：focused 测试 9/9 通过。

意义：重点验证新命令、命令目录、补全、`SHARDJSON` 嵌入、comparison checks/blockers/profiles/validation/stages 和 read-only/no-execution 字段。

## 04-full-ctest.png

命令：`ctest --test-dir cmake-build-v836 --output-on-failure`

结果：全量 CTest 143/143 通过。

意义：证明 v836 没有破坏既有 WAL/TCP/restore/runtime/route-preview/credential-resolver 等测试链路。

## 05-cli-smoke.png

命令：通过 `minikv_cli.exe` stdin 依次执行 `SHARDROUTEVALUESUPPLYSIGNEDAPPROVALCAPTUREARTIFACTDRAFTTEXTPACKAGECOMPARISONCLOSEOUTAUDITJSON`、`SHARDJSON`、`QUIT`。

结果：CLI smoke 断言通过，输出包含 v836 comparison closeout audit、v835 source freeze、submitted/detached/uncompared/unacceptable 全部 false，以及 `writeRoutingAllowed=false`、`executionAllowed=false`。

意义：证明本版证据在真实 CLI 路径下可读，且不会开启解析、接收、写路由或执行权限。

## 06-tcp-smoke.png

命令：临时启动 `minikv_server.exe`，通过 `minikv_client.exe` 发送新 comparison closeout audit 命令后退出。

结果：TCP smoke 断言通过，临时 server 已停止，临时 WAL 已删除。

意义：证明新命令经 TCP 路径同样只返回只读证据，不触发写入、WAL 触碰、router 激活或执行。

## 07-git-diff-check.png

命令：`git -c core.autocrlf=false diff --check`

结果：无 whitespace 错误。

意义：确认本版文档和代码没有 trailing whitespace；此前 README 首行 CRLF 导致的 diff-check 提示已修复。
