# mini-kv v176 command archive

This archive records the final closure evidence for mini-kv v176. The version freezes v175 as `fixtures/release/shard-readiness-v175.json` and extends the read-only `nodeRouteSplitCompatibilityWindow` through Node v457 for the managed audit manual sandbox connection route split sequence.

The screenshots were generated with Playwright MCP from temporary local HTML render pages served on `127.0.0.1:8176`. That HTTP server is temporary capture infrastructure and should be stopped during cleanup.

## 01-cmake-configure-build.png

Command:

```powershell
cmake -S . -B cmake-build-v176 -G Ninja
cmake --build cmake-build-v176
```

Result: CMake configure/generate succeeded and Ninja built the v176 code and test targets.

Meaning: v176 code and tests are present in the CMake build graph, including the Node v450-v457 route split compatibility window extension and the v175 frozen fixture.

## 02-generate-shard-readiness-fixture.png

Command:

```powershell
"SHARDJSON`nQUIT`n" | .\cmake-build-v176\minikv_cli.exe
```

Result: `fixtures/release/shard-readiness.json` was regenerated from real CLI output. Key fields:

- `releaseVersion=v176`
- `status=node-route-split-window-manual-sandbox-fake-transport-read-only`
- `windowEndNodeVersion=Node v457`
- `splitRouteGroupCount=25`
- `observedWindowVersionCount=25`
- `sourceFrozenFixturePath=fixtures/release/shard-readiness-v175.json`
- `catalogVersion=read-only-boundary-fields.v9`
- `fieldCount=420`
- `groupCount=22`
- `evidenceDigest=fnv1a64:528073c4315ef89b`
- `executionAllowed=false`
- `writeRoutingAllowed=false`
- `routerActivationAllowed=false`

Meaning: v176 extends the Node route split compatibility window without adding catalog fields or enabling routers, writes, runtime probes, service startup, restore/load/compact, or execution.

## 03-targeted-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v176 -R "^(command_tests|shard_readiness_tests)$" --output-on-failure
```

Result: targeted tests passed 2/2.

Meaning: the direct `SHARDJSON` contract tests verify v176 runtime/fixture parity, the frozen v175 fixture, the Node v457 window extension, the 25-version audit count, boundary catalog v9 counts, and read-only boundary flags.

## 04-full-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v176 --output-on-failure
```

Result: full CTest passed 71/71.

Meaning: the v176 compatibility update does not regress WAL, snapshots, restore evidence, TCP tests, runtime smoke evidence, managed-audit receipts, credential-resolver receipts, route split compatibility evidence, or existing shard-readiness fixtures.

## 05-tcp-smoke.png

Command:

```powershell
minikv_server.exe 6449 127.0.0.1
minikv_client.exe 127.0.0.1 6449
```

Client input:

```text
SHARDJSON
GET missing:v176
QUIT
```

Result: TCP smoke read v176 `SHARDJSON`, confirmed `windowEndNodeVersion=Node v457`, confirmed `observedWindowVersionCount=25`, confirmed `executionAllowed=false`, `writeRoutingAllowed=false`, and `routerActivationAllowed=false`, observed `(nil)` for the missing key, stopped the server, and confirmed port `6449` had no listener.

Meaning: the real TCP path can consume the v457 compatibility window as evidence only. It does not activate routing, writes, probes, service startup, restore/load/compact, or execution.

## Cleanup

Temporary render/log directories for this task are `e/176/_render/` and `e/176/_logs/`. They are intermediate capture inputs and should be deleted after final verification. The temporary render HTTP server on port `8176` should be stopped. The final kept archive is `e/176/图片/` plus this explanation file.
