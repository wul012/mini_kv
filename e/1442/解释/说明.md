# mini-kv v1442 命令归档说明

本目录归档 v1418-v1442 disabled precheck upstream echo non-participation 批次的关键命令证据。截图位于 `e/1442/图片/`，均由本地 HTML 证据页通过 Playwright MCP 截取生成。

## 01-cmake-configure-build.png

对应命令：

```powershell
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\ninja\win\x64;D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
& $cmake -S . -B cmake-build-v1442 -G Ninja -DCMAKE_BUILD_TYPE=Debug
& $cmake --build cmake-build-v1442
```

结果：CMake 配置和 Debug 构建成功。意义：确认新增 `shard_preview_disabled_precheck_upstream_echo_non_participation` core/stage/check/validation 模块、命令接入、SHARDJSON 聚合和测试目标均能在本机工具链下完整编译。

## 02-targeted-ctest.png

对应命令：

```powershell
$ctest = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir cmake-build-v1442 -C Debug -R "disabled_precheck_upstream_echo_non_participation|command_tests|line_editor_tests|shard_readiness_tests|type_barrel_split_follow_up_fixture_audit_fixture_tests" --output-on-failure
```

结果：定向测试通过。意义：集中验证新增 25 阶段证据、25 条检查、validation、standalone command、line editor completion、current SHARDJSON status rollover、v1418-v1442 fixture 链，以及 v1417 fixture audit 作为冻结源仍可被当前 fixture 引用。

## 03-full-ctest.png

对应命令：

```powershell
& $ctest --test-dir cmake-build-v1442 -C Debug --output-on-failure
```

结果：`271/271` CTest 全量通过。意义：证明本批次新增证据没有破坏 WAL、snapshot、TCP、runtime receipt、route preview、historical fixture、approval/value supply 等既有行为；旧 current status 断言已正确滚动到 v1442。

## 04-cli-smoke.png

对应命令：

```powershell
COMMANDS
COMMANDSJSON
EXPLAINJSON SHARDROUTEDISABLEDPRECHECKUPSTREAMECHONONPARTICIPATIONJSON
CHECKJSON SHARDROUTEDISABLEDPRECHECKUPSTREAMECHONONPARTICIPATIONJSON
SHARDROUTEDISABLEDPRECHECKUPSTREAMECHONONPARTICIPATIONJSON
SHARDJSON
QUIT
```

结果：CLI smoke 看到 `command_count=82`，新增命令进入 `COMMANDS` / `COMMANDSJSON`，`EXPLAINJSON` / `CHECKJSON` 均显示只读元数据语义，standalone command 和 current `SHARDJSON` 均包含 v1442 disabled-precheck upstream echo 非参与证据。

意义：确认新增命令是可读证据面，不是写命令、不是 WAL/admin execution，也没有开启 Node module import、Node check execution、endpoint read、credential read、active router、write routing 或 execution。

## 05-tcp-smoke.png

对应命令：

```powershell
cmake-build-v1442\minikv_server.exe 6511 127.0.0.1
cmake-build-v1442\minikv_client.exe 127.0.0.1 6511 5000 --connect-retries 10 --retry-delay-ms 100

SHARDROUTEDISABLEDPRECHECKUPSTREAMECHONONPARTICIPATIONJSON
QUIT
```

结果：TCP client 成功读取 `shard-route-disabled-precheck-upstream-echo-non-participation.v1`，输出包含 `v1442`、`sourceFixtureAuditReleaseVersion=v1417`、`miniKvImportsNodeModules=false`、`miniKvExecutesNodeChecks=false`、`rawEndpointParsed=false`、`credentialValueRead=false`、`writeRoutingAllowed=false`、`touchesWal=false`、`executionAllowed=false`。

进程处理：本轮 TCP smoke 启动的 `minikv_server` PID 为 `30288`，验证结束后已停止。没有保留 mini-kv 后台服务。

意义：证明真实 TCP 路径也只能读取 standalone 只读证据命令，不启动 Java/Node，不读取 endpoint 或 credential，不打开 managed audit connection，不触发写入、WAL、LOAD、RESTORE、COMPACT 或 active router。

## 06-diff-check.png

对应命令：

```powershell
git diff --check
git status --short
```

结果：`git diff --check passed`。意义：本批次源码、fixtures、README、讲解和归档说明没有 trailing whitespace 类文档质量问题；`git status --short` 记录了本批次待提交文件范围。

## 清理说明

截图生成期间短暂启动了本地 HTTP server，仅服务 `e/1442/_logs/html` 中的临时归档页；截图完成后已停止，PID 为 `33816`。`e/1442/_logs` 为临时日志和 HTML 渲染材料，最终 cleanup gate 会删除该目录，只保留 `e/1442/图片/` 和本说明文档。
