# mini-kv v178 命令归档说明

本轮重新读取 Node 最新计划 `D:\nodeproj\orderops-node\docs\plans3\v458-post-foundational-audit-route-group-split-roadmap.md`，确认 v458 已完成且仍是 route-registration-only 维护性拆分。mini-kv v178 不追新 Node 版本，而是做 contract-preserving audit hardening：冻结 v177，并把 route split window audit 的连续性/重复检测改为从版本向量计算。

## 01-cmake-configure-build.png

命令：使用 CLion 捆绑 CMake/Ninja 配置并构建 `cmake-build-v178`。

结果：配置成功，202 个构建目标全部完成。

意义：证明新增的计算化审计 helper 和测试源码可以从干净构建目录重新编译。

## 02-generate-shard-readiness-fixture.png

命令：通过 `SHARDJSON` 生成 `fixtures/release/shard-readiness.json`。

结果：当前 rolling fixture 为 `releaseVersion=v178`，状态为 `node-route-split-window-computed-audit-read-only`，窗口仍覆盖 `Node v458`，窗口数量 26，`contiguousNodeVersionWindow=true`，`duplicateWindowVersionsDetected=false`，`readOnly=true`，`executionAllowed=false`，摘要为 `fnv1a64:dd89f7c3bd63d7c3`。

意义：证明 v178 的审计字段由窗口版本列表计算后仍保持正确，而且没有打开任何 runtime 边界。

## 03-targeted-ctest.png

命令：运行 `command_tests` 与 `shard_readiness_tests` 两个定向测试。

结果：2/2 通过。

意义：重点确认 v177 frozen fixture、v178 rolling fixture、source frozen digest、computed contiguity/duplicate audit、no router/no write/no execution 字段一致。

## 04-full-ctest.png

命令：运行全量 CTest。

结果：71/71 通过。

意义：证明 v178 的维护性重构没有破坏 WAL、snapshot、TCP、restore、receipt、runtime-smoke 等既有行为。

## 05-tcp-smoke.png

命令：临时启动 `minikv_server.exe 6451 127.0.0.1`，用 `minikv_client.exe` 发送 `PING`、`SHARDJSON`、`QUIT`。

结果：TCP 响应包含 `releaseVersion=v178`、computed audit window mode、`contiguousNodeVersionWindow=true`、`duplicateWindowVersionsDetected=false`、`readOnly=true`、`executionAllowed=false`、`evidenceDigest=fnv1a64:dd89f7c3bd63d7c3`。验证结束后已停止本轮启动的服务端，端口 6451 不再监听。

意义：证明计算化审计字段通过真实 TCP 路径可读；该 smoke 没有执行写入、LOAD、RESTORE、COMPACT 或 runtime gate。
