# mini-kv v162 截图说明

本轮先只读确认 Node 当前有效计划为 `D:\nodeproj\orderops-node\docs\plans3\v425-post-credential-resolver-disabled-runtime-shell-readiness-route-group-split-roadmap.md`。该计划是 Node route registration 维护拆分，不新增 evidence gate、不改变 API path/response、不启动 Java 或 mini-kv，因此 mini-kv v162 按并行模式继续推进自己的只读证据：冻结 v161，并新增 evidence-only 的 16 槽 `slotTablePreview`。

截图通过 Playwright MCP 对本地临时渲染页生成。`file://` 在本环境不可用，因此临时启动 `python -m http.server 8162 --bind 127.0.0.1` 服务 `e/162/_render/`；截图完成后该服务会在 cleanup gate 中停止并删除 `_render` 与 `_logs`。

## 01-cmake-configure-generate-fixture.png

对应命令：
```powershell
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
& $cmake -S . -B cmake-build-v162 -G Ninja
& $cmake --build cmake-build-v162 --target minikv_cli
"SHARDJSON`nQUIT`n" | .\cmake-build-v162\minikv_cli.exe
```

结果与意义：CMake 配置和 `minikv_cli` 构建可用，随后通过交互式 `SHARDJSON` 重新生成 `fixtures/release/shard-readiness.json`。新 current fixture 为 v162，状态为 `slot-table-preview-read-only`，digest 为 `fnv1a64:8c01a3657f07cfd1`；`slotTablePreview` 包含 16 个 slot，`executionAllowed=false`、`writeRoutingAllowed=false`，`boundaryCatalogIndex` 为 `fieldCount=303`、`groupCount=16`。

## 02-targeted-ctest.png

对应命令：
```powershell
$ctest = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir cmake-build-v162 -R "^(command_tests|shard_readiness_tests)$" --output-on-failure
```

结果与意义：定向测试 2/2 通过。`command_tests` 与 `shard_readiness_tests` 证明 v162 current fixture、v161 frozen fixture、`slotTablePreview`、16 槽只读分配、catalog v3/count、no-router/no-write/no-execution 字段在命令层和 fixture 层一致。

## 03-full-build-ctest.png

对应命令：
```powershell
& $cmake --build cmake-build-v162
& $ctest --test-dir cmake-build-v162 --output-on-failure
```

结果与意义：全量构建无新增工作，71 条 CTest 全部通过。说明 v162 的 slot preview 拆分和 evidence 字段没有影响 WAL、snapshot、restore、TCP、RESP、runtime smoke、credential resolver non-participation receipts 等既有行为。

## 04-tcp-smoke-cleanup.png

对应命令：
```powershell
.\cmake-build-v162\minikv_server.exe 6435 127.0.0.1
# TCP 发送 SHARDJSON / GET missing:v162 / QUIT
```

结果与意义：临时启动的 `minikv_server` 在 6435 上返回 v162 `SHARDJSON`，响应包含 `slotTablePreview`、16 个 slot、`executionAllowed=false` 和 `writeRoutingAllowed=false`；`GET missing:v162` 返回 `(nil)`。验证结束后已停止该 server；本轮没有启动 6424 声明审批端口，没有打开 active router，也没有留下 mini-kv 后台进程。
