# mini-kv v177 命令归档说明

本轮读取 Node 最新计划 `D:\nodeproj\orderops-node\docs\plans3\v458-post-foundational-audit-route-group-split-roadmap.md` 后，确认 v458 是 route-registration-only 的 foundational audit route group split：不新增 gate、不改 API path/response、不启动 Java 或 mini-kv。mini-kv v177 因此只冻结 v176 证据并延伸只读兼容窗口到 Node v458。

## 01-cmake-configure-build.png

命令：使用 CLion 捆绑 CMake/Ninja 配置并构建 `cmake-build-v177`。

结果：配置成功，202 个构建目标全部完成，生成新的 `minikv_cli.exe`、`minikv_server.exe`、`minikv_client.exe` 和测试程序。

意义：证明 v177 的 C++ 源码和测试源码可以从干净构建目录重新编译，不依赖旧二进制。

## 02-generate-shard-readiness-fixture.png

命令：通过 `SHARDJSON` 生成 `fixtures/release/shard-readiness.json`。

结果：当前 rolling fixture 为 `releaseVersion=v177`，状态为 `node-route-split-window-foundational-audit-read-only`，窗口终点为 `Node v458`，窗口数量和审计数量均为 26，`readOnly=true`，`executionAllowed=false`，摘要为 `fnv1a64:e773708a4decc60e`。

意义：证明 v177 的 rolling fixture 已经落地，并且 v176 冻结基线与 v458 扩展后的当前证据分离。

## 03-targeted-ctest.png

命令：运行 `command_tests` 与 `shard_readiness_tests` 两个定向测试。

结果：2/2 通过。

意义：重点确认 `SHARDJSON` 输出、版本化 fixture、历史 fallback、route split window audit、no router/no write/no execution 字段一致。

## 04-full-ctest.png

命令：运行全量 CTest。

结果：71/71 通过。

意义：证明 v177 只是证据字段延伸，没有破坏 WAL、snapshot、TCP、restore、receipt、runtime-smoke 等既有行为。

## 05-tcp-smoke.png

命令：临时启动 `minikv_server.exe 6450 127.0.0.1`，用 `minikv_client.exe` 发送 `PING`、`SHARDJSON`、`QUIT`。

结果：TCP 响应包含 `releaseVersion=v177`、`windowEndNodeVersion=Node v458`、`readOnly=true`、`executionAllowed=false`、`evidenceDigest=fnv1a64:e773708a4decc60e`。验证结束后已停止本轮启动的服务端，端口 6450 不再监听。

意义：证明 v177 的只读证据不仅在单元测试中成立，也可以通过真实 TCP 路径读取；该 smoke 没有执行写入、LOAD、RESTORE、COMPACT 或 runtime gate。
