# v786 command archive explanation

This directory records key command evidence for v786 `SHARDROUTEVALUESUPPLYSIGNEDAPPROVALCAPTUREARTIFACTDRAFTTEXTPACKAGEREVIEWPREFLIGHTJSON`. PNG files are rendered terminal transcripts because this verification target is command output, not a browser page.

- `01-cmake-build.png`: CMake incremental build for `cmake-build-v786` using the CLion bundled CMake and MinGW runtime. The build includes the split draft text package review preflight module and the adjusted compatibility tests.
- `02-focused-ctest.png`: focused CTest for text package review preflight, instruction preflight, command catalog, line editor, SHARDJSON, artifact preflight, and authoring readiness compatibility. The run passed.
- `03-full-ctest.png`: full repository CTest. The run passed 131/131 after fixing stale rolling-status assertions in the historical artifact/authoring tests.
- `04-cli-smoke.png`: CLI smoke for the new command and SHARDJSON. It confirms `releaseVersion=v786`, `draftTextPackageReviewPreflightOnly=true`, no draft package parsing or acceptance, no approval grant, no imported values, no write routing, no WAL touch, and no execution.
- `05-tcp-smoke.png`: loopback TCP smoke through an owned `minikv_server.exe` on `127.0.0.1:17686` and `minikv_client.exe`. The new command and `CHECKJSON` stayed read-only; the owned server process was stopped after validation.
- `06-git-diff-check.png`: `git diff --check` quality gate summary after source, fixture, docs, archive, and v762-v785 walkthrough cleanup.

Files kept: final archive screenshots under `e/786/图片/`, this explanation under `e/786/解释/`, the v785 frozen fixture, the regenerated rolling fixture, source/tests/docs/walkthrough changes.

Files deleted later: temporary command transcript logs used only to render PNG evidence.

Processes stopped: the owned `minikv_server.exe` process used for TCP smoke.
