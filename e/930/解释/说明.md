# mini-kv v930 command archive

This archive records final verification evidence for mini-kv v921-v930 candidate submission precheck handoff.

Screenshots were generated with Playwright MCP from temporary local HTML pages served by a short-lived read-only `python -m http.server` process on `127.0.0.1:6930`. The server was stopped after screenshots were captured. No text-rendered PNG fallback was used. CLI/TCP raw output excerpts in the smoke screenshots are line-truncated because the full `SHARDJSON` response is a large single-line payload; the boolean smoke checks are preserved in full.

| Screenshot | Command or check | Result |
|---|---|---|
| `01-cmake-configure-build.png` | `cmake -S . -B cmake-build-v921-v930 -G Ninja`, then `cmake --build cmake-build-v921-v930` using the CLion bundled toolchain. | Configure and build completed successfully. CMake still reports existing long object path warnings for older long-named tests; the build succeeds. |
| `02-focused-ctest.png` | `ctest --test-dir cmake-build-v921-v930 -R "candidate_submission_precheck|candidate_request_package_integrity|command_tests|line_editor_tests|shard_readiness_tests" --output-on-failure`. | Focused tests passed, including the new submission precheck modules, fixture continuity, command catalog, line editor completion, and SHARDJSON readiness assertions. |
| `03-full-ctest.png` | `ctest --test-dir cmake-build-v921-v930 --output-on-failure`. | Full CTest passed: 164/164 tests. |
| `04-cli-smoke.png` | CLI stdin smoke: `COMMANDS`, `SHARDROUTECANDIDATESUBMISSIONPRECHECKJSON`, `SHARDJSON`, `QUIT`. | Verified `command_count=61`, read-only command catalog entry, Node v1411 plan linkage, v920 source integrity fixture, v930 submission precheck release, no additional precheck echo, no document intake, no router, no write, no WAL touch, no execution, and SHARDJSON current status. |
| `05-tcp-smoke.png` | Started `minikv_server.exe 6530 127.0.0.1`, then used `minikv_client.exe 127.0.0.1 6530 5000 --connect-retries 10 --retry-delay-ms 100` for `COMMANDS`, `SHARDROUTECANDIDATESUBMISSIONPRECHECKJSON`, `QUIT`. | TCP path verified the new short read-only command and command catalog. The server process was stopped after validation. SHARDJSON was verified through CLI because the current response is large. |

Final retained files:

- `e/930/图片/*.png`
- `e/930/解释/说明.md`
- `fixtures/release/shard-readiness-v920.json` through `fixtures/release/shard-readiness-v930.json`
- `fixtures/release/shard-readiness.json` as current v930

Cleanup candidates after final review and commit:

- `cmake-build-v921-v930`
- `e/930/_logs`
- temporary local HTML pages used only for Playwright screenshots
