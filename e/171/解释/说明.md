# mini-kv v171 command archive

This archive records the final closure evidence for mini-kv v171. The version freezes v170 as `fixtures/release/shard-readiness-v170.json`, extends the read-only `nodeRouteSplitCompatibilityWindow` through Node v444, and centralizes route split window metadata inside the Node compatibility formatter.

The screenshots were generated with Playwright MCP from temporary local HTML render pages served on `127.0.0.1:8171`. That HTTP server is temporary capture infrastructure and should be stopped during cleanup.

## 01-cmake-configure-build.png

Command:

```powershell
cmake -S . -B cmake-build-v171 -G Ninja
cmake --build cmake-build-v171
```

Result: CMake configure/generate succeeded and Ninja built the v171 code and test targets.

Meaning: v171 code and tests are present in the CMake build graph, including the centralized route split window metadata, Node v443/v444 window coverage, and the v170 frozen fixture.

## 02-generate-shard-readiness-fixture.png

Command:

```powershell
"SHARDJSON`nQUIT`n" | .\cmake-build-v171\minikv_cli.exe
```

Result: `fixtures/release/shard-readiness.json` was regenerated from real CLI output. Key fields:

- `releaseVersion=v171`
- `status=node-route-split-window-managed-audit-read-only`
- `windowEndNodeVersion=Node v444`
- `splitRouteGroupCount=12`
- `observedWindowVersionCount=12`
- `sourceFrozenFixturePath=fixtures/release/shard-readiness-v170.json`
- `catalogVersion=read-only-boundary-fields.v9`
- `fieldCount=420`
- `groupCount=22`
- `evidenceDigest=fnv1a64:9f6aa583ef521d1a`
- `executionAllowed=false`
- `writeRoutingAllowed=false`

Meaning: v171 extends the Node route split compatibility window and refactors formatter metadata without adding catalog fields or enabling routers, writes, runtime probes, service startup, restore/load/compact, or execution.

## 03-targeted-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v171 -R "^(command_tests|shard_readiness_tests)$" --output-on-failure
```

Result: targeted tests passed 2/2.

Meaning: the direct `SHARDJSON` contract tests verify v171 runtime/fixture parity, the frozen v170 fixture, the Node v443/v444 window extension, the 12-version audit count, boundary catalog v9 counts, and read-only boundary flags.

## 04-full-ctest.png

Command:

```powershell
ctest --test-dir cmake-build-v171 --output-on-failure
```

Result: full CTest passed 71/71.

Meaning: the v171 compatibility and formatter refactor does not regress WAL, snapshots, restore evidence, TCP tests, runtime smoke evidence, credential-resolver receipts, route split compatibility evidence, or existing shard-readiness fixtures.

## 05-tcp-smoke.png

Command:

```powershell
minikv_server.exe 6444 127.0.0.1
minikv_client.exe 127.0.0.1 6444
```

Client input:

```text
SHARDJSON
GET missing:v171
QUIT
```

Result: TCP smoke read v171 `SHARDJSON`, confirmed `windowEndNodeVersion=Node v444`, confirmed `observedWindowVersionCount=12`, confirmed `executionAllowed=false` and `writeRoutingAllowed=false`, observed `(nil)` for the missing key, stopped the server, and confirmed port `6444` was released.

Meaning: the real TCP path can consume the v444 compatibility window as evidence only. It does not activate routing, writes, probes, service startup, restore/load/compact, or execution.

## Cleanup

Temporary render/log directories for this task are `e/171/_render/` and `e/171/_logs/`. They are intermediate capture inputs and should be deleted after final verification. The temporary render HTTP server on port `8171` should be stopped. The final kept archive is `e/171/图片/` plus this explanation file.
