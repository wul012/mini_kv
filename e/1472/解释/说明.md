# mini-kv v1472 命令归档说明

本目录归档 v1443-v1472 sandbox endpoint credential resolver upstream echo non-participation 批次的关键命令证据。截图位于 `e/1472/图片/`，由本地 HTML 证据页通过 Playwright MCP 截取生成。

## 01-cmake-configure-build.png

对应命令：

```powershell
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\ninja\win\x64;D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
& $cmake -S . -B cmake-build-v1472 -G Ninja -DCMAKE_BUILD_TYPE=Debug
& $cmake --build cmake-build-v1472
```

结果：CMake 配置和 Debug 构建成功。构建日志仍显示既有长路径 warning，但 exit code 为 0。意义：确认新增 sandbox endpoint credential resolver upstream echo non-participation core/stage/check/validation 模块、命令接入、SHARDJSON 聚合和测试目标都能在本机工具链下完整编译。

## 02-targeted-ctest.png

对应命令：

```powershell
$ctest = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir cmake-build-v1472 -R "(sandbox_endpoint_credential_resolver_upstream_echo_non_participation|disabled_precheck_upstream_echo_non_participation_fixture_tests|command_tests|line_editor_tests|shard_readiness_tests)$" --output-on-failure
```

结果：定向 CTest 10/10 通过。意义：集中验证新增 30 阶段证据、30 条 check、validation、standalone command、fixture 链、命令目录、补全和 current SHARDJSON status rollover；同时确认 v1442 disabled-precheck upstream echo 仍作为冻结来源被当前 fixture 引用。

## 03-full-ctest.png

对应命令：

```powershell
& $ctest --test-dir cmake-build-v1472 --output-on-failure
```

结果：`276/276` CTest 全量通过。意义：证明本批次没有破坏 WAL、snapshot、TCP、runtime receipt、route preview、historical fixture、approval/value supply 等既有行为；当前 status 断言已正确滚动到 v1472。

## 04-cli-smoke.png

对应命令流：

```text
COMMANDS
COMMANDSJSON
EXPLAINJSON SHARDROUTESANDBOXENDPOINTCREDENTIALRESOLVERUPSTREAMECHONONPARTICIPATIONJSON
CHECKJSON SHARDROUTESANDBOXENDPOINTCREDENTIALRESOLVERUPSTREAMECHONONPARTICIPATIONJSON
SHARDROUTESANDBOXENDPOINTCREDENTIALRESOLVERUPSTREAMECHONONPARTICIPATIONJSON
SHARDJSON
QUIT
```

结果：CLI smoke 看到 `command_count=83`，新增命令进入 `COMMANDS` / `COMMANDSJSON`，`EXPLAINJSON` / `CHECKJSON` 均显示只读元数据语义，standalone command 返回 v1472，current `SHARDJSON` 返回 v1472 root status。关键边界全部保持 false：Node module import、Node check execution、endpoint handle read、raw endpoint parse、credential read、managed audit connection、write routing、WAL touch、execution allowed。

意义：确认新增命令是可读证据面，不是写命令、不是 admin execution、不是 Node/Java 启动入口，也不会打开 endpoint/credential/managed connection 边界。

## 05-tcp-smoke.png

对应命令：

```powershell
cmake-build-v1472\minikv_server.exe 6572 127.0.0.1
cmake-build-v1472\minikv_client.exe 127.0.0.1 6572 5000 --connect-retries 10 --retry-delay-ms 100
```

客户端命令流：

```text
SHARDROUTESANDBOXENDPOINTCREDENTIALRESOLVERUPSTREAMECHONONPARTICIPATIONJSON
QUIT
```

结果：TCP client 成功读取 `shard-route-sandbox-endpoint-credential-resolver-upstream-echo-non-participation.v1`，返回 `v1472`、`sourceDisabledPrecheckReleaseVersion=v1442`、`sourceNodeSandboxEndpointCredentialResolverUpstreamEchoCloseoutReleaseVersion=Node v1903`，并确认 Node import、endpoint handle read、raw endpoint parse、credential read、managed audit connection、write routing、WAL touch 和 execution allowed 全部为 false。

进程处理：本轮 TCP smoke 启动的 `minikv_server` PID 为 `8548`，验证结束后已停止。没有保留 mini-kv 后台服务。

## 06-diff-check.png

对应命令：

```powershell
git diff --check
git status --short
```

结果：`git diff --check passed`。意义：本批次源代码、fixtures、README、代码讲解和归档说明没有 trailing whitespace 类文档质量问题；`git status --short` 记录了本批次待提交文件范围。

## 清理说明

截图期间短暂启动了本地 HTTP server，仅服务 `e/1472/_logs/html` 中的临时证据页；第一次截图服务 PID 为 `1856`，刷新最终 diff-check 截图时的服务 PID 为 `11612`，二者均已停止。`e/1472/_logs` 是临时日志和 HTML 渲染材料，最终 cleanup gate 会删除该目录，只保留 `e/1472/图片/` 和本说明文档。
