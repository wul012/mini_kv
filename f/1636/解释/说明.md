# mini-kv v1636 命令归档说明

本目录记录 v1636 OSFS 课程设计最终交付包刷新的关键证据。截图使用 Playwright MCP 对临时本地 HTTP 证据页进行 full-page screenshot 生成；临时 HTTP 服务仅绑定 `127.0.0.1:8765`，截图完成后已停止。截图源来自本轮真实命令日志、真实 CLI transcript、PDF 渲染 contact sheet 和包自检输出。

## 01-build-summary.png

对应命令：

```powershell
$env:PATH='D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
$cmake='D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
& $cmake --build cmake-build-v1636
```

结果与意义：

- 全量测试目标完成构建，日志尾部显示构建推进到 `[682/682]`。
- 这一步证明 v1636 不是只改文档后跳过构建；最终 CTest 使用的是当前构建目录中的最新测试可执行文件。

## 02-real-cli-demo.png

对应命令：

```powershell
.\cmake-build-v1636\minikv_osfs.exe --disk .\cmake-build-v1636\v1636-final-demo.img --format --blocks 160 --script .\课程设计交付\v1636-osfs-final\osfs-demo-script.txt
```

结果与意义：

- transcript 以 UTF-8 保存到 `课程设计交付/v1636-osfs-final/osfs-demo-transcript.txt`。
- 关键输出包含 `OK wrote 4291 bytes offset=4291`、`DIRECT8|INDIRECT-BEGIN|`、`FSCK OK checks=6`、Bob 的权限拒绝、Bob 的非 root `USERADD` 拒绝、root 创建 Carol 和 Carol 新密码登录。
- 这张图证明 v1636 最终包中的演示材料来自真实可执行程序，而不是手写输出。

## 03-report-render-contact-sheet.png

对应命令：

```powershell
soffice.com --headless --norestore --convert-to pdf --outdir cmake-build-v1636\lo-ascii-render cmake-build-v1636\osfs-v1636-report.docx
pdftoppm.exe -png -r 120 cmake-build-v1636\lo-ascii-render\osfs-v1636-report.pdf cmake-build-v1636\pdf-pages-v1636\page
```

结果与意义：

- 最终 DOCX 通过 LibreOffice 转成 PDF，并渲染为 9 页 PNG。
- contact sheet 用来检查整份报告没有空白页、图像丢失、明显裁剪、文字重叠或矩阵断行问题。
- 本轮曾发现矩阵行跨页拆开，已通过 `w:cantSplit` 修复后重渲染。

## 04-package-verification.png

对应命令：

```powershell
python <v1636 package verification snippet>
```

结果与意义：

- 检查 v1636 最终包必备文件全部存在：README、需求矩阵、演示步骤、提交清单、demo script、demo transcript、DOCX、PDF 和四张报告图。
- 检查 transcript 无 UTF-8 空字节问题，包含 direct-to-indirect 边界、两次 `FSCK OK`、权限拒绝、root-only USERADD 和 Carol 新密码登录。
- 检查 PDF 页数为 9 页，中文讲解记录达到 3076 个中文字符，并且矩阵包含间接块、fsck、用户管理、教学简化和边界项。

## 05-focused-tests.png

对应命令：

```powershell
$ctest='D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir cmake-build-v1636 -R '^osfs_tests$|^osfs_cli_smoke$|code_walkthrough_quality_gate_np|f_folder_explanation_quality_closeout' --output-on-failure
& $ctest --test-dir cmake-build-v1636 -R 'code_walkthrough_quality_gate_np|f_folder_explanation_quality_closeout' --output-on-failure
```

结果与意义：

- OSFS focused tests、CLI smoke、代码讲解质量 gate 和 f-folder 解释质量 gate 全部通过。
- 这一步保护本版最相关的风险面：OSFS 机制、真实 CLI、中文讲解和归档说明。

## 06-full-ctest.png

对应命令：

```powershell
& $ctest --test-dir cmake-build-v1636 --output-on-failure
```

结果与意义：

- 全量 CTest `344/344` 通过。
- 这一步证明 v1636 的 OSFS 交付刷新没有破坏 mini-kv 原有 KV、WAL、snapshot、TCP/RESP、runtime evidence、archive quality gate 和 no-execution/non-participation 证据面。

## 清理记录

- 临时 HTTP 服务 `127.0.0.1:8765` 截图完成后已停止。
- 临时 HTML、构建目录、PDF 渲染页和日志属于本轮验证中间产物，会在最终 cleanup gate 中删除。
- `f/1636/图片/` 与本说明文档作为最终归档证据保留。
