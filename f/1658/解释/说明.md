# v1658 命令归档说明

本目录记录 mini-kv Production Excellence 阶段三的选择性证据。四张 PNG 均在代码、文档、聚焦测试、完整 CTest 和真实 CLI smoke 稳定后生成；使用 Playwright MCP 渲染最终摘要页面并截图，没有使用文本转图回退，也没有把完整构建日志或二进制放入归档。

## 01-e1-e10-gates.png

这张图对应 `docs/minikv-track-final-evidence.md` 的 E1-E10 gate matrix。它把 Build/CI、format、coverage、security、observability、error handling、docs honesty、release、code health 和 archive retention 各自映射到机械证据。图上同时保留 `READY FOR EXTERNAL REVIEW` 与 `External verdict: PENDING`，表示执行方完成了可复核自审，但没有替 Claude 外部终审宣布 PASS；成熟度仍是 single-project validation 加 cross-project contract alignment。

## 02-coverage-safe-default.png

这张图记录两项可量化收紧。第一，GitHub Actions run 29095734744 和 29097881334 的 coverage log 都给出 `TOTAL 2345 / 2122 / 90%`，因此 `.github/workflows/ci.yml` 的 floor 按简报从 88 提升为 90，`dependabot_config_tests` 防止降回旧值。第二，E4 审查发现 `TcpServer::Options` 原先默认 `0.0.0.0`，本版修为 `127.0.0.1`。真实进程 PID 28948 未传 host 启动后，系统监听表确认 local address 为 loopback，HEALTH/QUIT 正常；验证后进程已停止。

## 03-build-tests-cli.png

这张图对应新 `cmake-build-v1658` 构建树：MinGW Debug 配置注册 350 tests，全目标冷构建成功；终审、文档、census、logger、TCP、timeout 与 CI contract 聚焦门先行通过；完整 CTest 350/350、零失败、173.95 秒。真实 CLI 分三次执行 SMOKEJSON、`CHECKJSON LOAD data/prod.snap` 和 SHARDJSON：前者保持只读且无 write/admin/runtime write；CHECKJSON 只报告 LOAD 的潜在副作用，随后 SIZE 仍为 0；SHARDJSON 继续 no active router、no write routing、no execution。

## 04-waivers-archive.png

这张图把终审允许的两项 program deviation 限定为精确范围：E9 仅 `src/shard_readiness_boundary_field_names.cpp` 这个静态 registry 可超过 800 个非空行；E3 仅保留经 v1620 删除实验和 run 27428515080 证明仍需的 `negative_hits.warn_once_per_file`。`command.cpp` 按 K4 一致口径为 732 个非空行；receipt census 为 28 sources / 27 owners / 27 builder / 0 pending / 1 census exemption。图中的 archive 数字是添加 v1658 文件之前的稳定 v1657 基线；添加完成后的准确数字写入 `docs/archive-retention-index.md`。

## 进程与临时文件

- 默认 host smoke 启动的 PID 28948 已停止。
- Playwright 页面已关闭；截图工具生成的临时 `.playwright-mcp/` 会在版本清理门删除。
- `cmake-build-v1658/`、完整 CTest 临时日志和 server stdout/stderr 只用于验证，会在提交前删除。
- 最终保留本目录四张图片、本说明、`f/1658/证据/summary.txt`、v1658 中文讲解和项目终审文档。
