# v1672 命令与证据截图说明

本目录保留四张选择性截图和一份文本摘要，不保存构建目录、2 MB 原始 CLI 输出或 354 项完整日志。截图使用 Playwright MCP 从本地临时只读证据页生成；归档完成后会关闭页面、停止临时 HTTP 服务并删除 HTML。历史 `a/` 到 `f/` 的既有目录不移动、不改名。

## 01-command-architecture.png

展示重构前后的职责和体积。旧 `command.cpp` 有 732 个非空行，约 74 个 command 比较分支；v1672 保留 12 个真正需要状态或参数的分支，把 62 个无参数只读 formatter 放入 268 行私有表，`command.cpp` 降到 258 个非空行。图中的校验顺序强调 formatter 只在额外参数检查通过后调用。

## 02-byte-oracle.png

展示 v1671 与 v1672 对同一 3,463 字节输入的真实 CLI 结果。两边均输出 159 行、2,093,273 字节，SHA-256 同为 `b60a2ff0106c8803f879feb4034eed00c35b6bb319169382c5a577339c52f5b4`。这证明表驱动没有改写 62 个命令的响应字节，不只是在几个 JSON 字段上做抽查。

## 03-test-safety.png

展示测试结构门和 Release 红转绿。编译期固定 62 项并拒绝重名，command suite 验证 formatter、catalog 和 lookup；架构门钉住 260/12/62。预验收发现 Release 的 `NDEBUG` 会关闭测试断言，修复后 bundled、legacy、standalone 三条 target 创建路径统一恢复断言，Release 代表测试 4/4、标准默认预验收 354/354。

## 04-boundaries-and-next.png

展示本版边界与九分计划下一步。公共 header 与冻结 fixture 变化均为 0；没有新增 router、write、WAL、restore、network、credential、execution 或 order authority。v1673 只会把 342 项普通测试注册变为一份受 schema、唯一性、存在性和顺序约束的 manifest，不改变 CTest 名称或一例一进程语义。

## 清理记录

- 最终截图后关闭 Playwright 页面，停止本轮临时 HTTP 服务。
- 删除临时证据 HTML、CLI 全量输出和 `b1672*`、`b72` 验证目录。
- 保留本目录四张 PNG、本文与 `../证据/summary.txt`；没有启动需要长期保留的进程。
