# v1642 命令截图说明

本轮归档对应 mini-kv v1642：只把 credential-handle approval contract receipt 的顶层 JSON 组装迁移到 ordered builder，不新增运行时命令，不改 fixture，不改 digest 输入，不打开 router、Store/WAL 写入、credential 读取、raw endpoint 解析、HTTP/TCP、approval ledger、schema migration、LOAD/COMPACT/RESTORE/SETNXEX 或 execution authority。

截图使用 Playwright MCP 从命令摘要页面生成 PNG。页面内容来自本轮真实命令结果摘要，不保存完整超长日志和二进制构建产物。截图完成后会关闭 Playwright 页面，并在 cleanup gate 中删除 `cmake-build-v1642` 临时构建目录。

## 01-scope-and-diff.png

说明本版范围和差异：读取了 Node plans / final acceptance / plans2 v325 后续计划 / mini-kv v1637 brief，判断当前仍可在 mini-kv receipts consolidation 内部推进；实际代码只改 `src/runtime_credential_resolver_credential_handle_approval_receipts.cpp`，把 credential-handle 顶层 formatter 从长字符串拼接迁移到 ordered builder，并纳入 `AGENTS.md` 中用户要求的方法内核引用。

## 02-focused-tests.png

记录 focused 验证结果：`runtime_receipt_json_builder_tests`、`credential_handle_approval_contract_receipt_tests`、`smokejson_command_receipt_tests`、`release_verification_manifest_tests`、`runtime_smoke_evidence_tests` 5/5 通过。它们共同证明 credential-handle public formatter 仍与 frozen fixture 子对象逐字节一致，SMOKEJSON、runtime smoke evidence 和 release manifest 仍暴露同一 receipt。

## 03-full-ctest-and-cli-smoke.png

记录完整构建、全量 CTest 和真实 CLI smoke：完整 build 后 full local CTest 344/344 通过；`SMOKEJSON` 仍输出 runtime evidence 和 credential-handle receipt 链；`CHECKJSON LOAD data/prod.snap` 只报告 LOAD 是 write command / side effect，不执行 LOAD、不替换 store、不触发 WAL。

## 04-quality-gates.png

记录格式和收尾质量门：changed C++ source 的 `clang-format --dry-run --Werror` 通过；`git diff --check` 通过；`scripts/archive_inventory.py --budget-mib 8 --strict` 在新增 v1642 归档前通过，warnings 为 none。Git 在 Windows 工作区提示 LF/CRLF normalization，不是 trailing whitespace 或格式失败。

## 清理说明

本版启动过临时 CMake 构建目录 `cmake-build-v1642`，没有启动 mini-kv server、Node、Java、HTTP 服务或其他长期后台进程。最终 cleanup 会删除 `cmake-build-v1642`；长期保留本目录 PNG、这份说明、`f/1642/证据/summary.txt`、第四册讲解、治理 note、进度 ledger、源码和规则文件改动。
