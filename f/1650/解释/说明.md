# v1650 命令归档截图说明

v1650 在 v1648 exact-byte 基线与 v1649 family helper 下，把 runtime-shell decision record、post-decision plan intake、stop-or-prerequisite 三份连续 formatter 迁移到 ordered JSON builder。三份输出继续逐字节匹配 frozen fixture，专属决策字段留在各自文件，共同 readiness 与 non-participation tail 才由 family helper 追加。

## 01-exact-parity-migration.png

记录三份迁移对象的冻结字节数、顶层字段数和版本化摘要。decision record 保持 14208 bytes / 113 fields，post-decision plan intake 保持 14084 bytes / 113 fields，stop-or-prerequisite 保持 16306 bytes / 115 fields。三份 runtime object 都与 fixture byte-for-byte 相等，fixture 没有改动，也没有 apostrophe 或 whitespace waiver。

## 02-structure-census.png

记录生产结构与机械 census。三个 formatter 分别由 420→389、402→382、453→440 行，合计减少 64 行；更重要的是 nested source、summary、arrays 和顶层 fields 的所有权被显式化，三份重复的 boundary fragment 被删除。census 从 11/16/1 收紧到 14 builder-backed / 13 pending / 1 named waiver，CMake floor 同步升到 14。

## 03-focused-full-ctest.png

记录文档和讲解落地后的最终验证。focused lane 覆盖 census、通用 builder、五回执 family baseline、三份领域 receipt、SMOKEJSON、runtime evidence 和 release manifest，共 9/9 通过，耗时 2.08 秒。最终 full CTest 为 346/346，耗时 113.67 秒；全目标冷构建也成功完成。讲解正文含 3102 个中文字符。

## 04-real-cli-boundaries.png

记录真实 `minikv_cli` 执行 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap`、`QUIT` 的精简机械断言。三份 receipt marker 各出现 1 次，`read_only=true` 55 次、`execution_allowed=false` 81 次、`runtime_shell_implemented=false` 41 次、`external_request_sent=false` 74 次、`write_commands_executed=false` 37 次。LOAD 只被 CHECKJSON 分类，`touches_wal=false`，没有执行 LOAD，最后返回 `BYE`。

## 清理说明

四张 PNG 由 Playwright MCP 从只含最终断言的内存页面生成，未把原始巨型 SMOKEJSON 塞入截图。最终保留本目录、`f/1650/证据/summary.txt` 和第四册中文讲解；提交前删除 `cmake-build-v1650`、`.v1650-final-*.log` 与本轮四个 Playwright snapshot，关闭浏览器，不保留 mini-kv 服务进程，也不移动历史归档或 frozen fixture。
