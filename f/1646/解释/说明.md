# v1646 命令归档截图说明

v1646 只迁移三份已经由 v1645 证明 migration-ready 的 approval lifecycle formatter，收敛一份真实重复的 disabled-runtime-shell 边界后缀，并把 builder census floor 从 5 收紧到 8。`post-echo` 因独立空白漂移继续留在下一版，本版没有改 fixture、测试期望或 public command surface。

## 01-builder-census.png

记录 `cmake/check_receipt_builder_census.cmake` 的独立执行结果：28 个 runtime receipt source、27 个 formatter owner、8 个 builder-backed、19 个 pending、1 个命名 waiver。意义是进度由会失败的机械门证明，而不是由文档自报；floor 已同步收紧到 8。

## 02-focused-parity.png

记录 8 条 focused CTest 全绿：census、builder parity、三份目标 receipt，以及 SMOKEJSON、runtime smoke evidence、release manifest。意义是三份输出继续满足冻结 fixture/canonical 规则，并且真实下游嵌入表面没有漂移。

## 03-full-ctest.png

记录从干净 v1646 build 运行的完整 CTest：345/345 通过，0 failed，总时间约 186 秒。意义是共享 boundary helper 与 receipt builder 重构没有外溢到 WAL、snapshot、RESP/TCP、OSFS、shard readiness 或其他 receipt family。

## 04-cli-boundary.png

记录真实 `minikv_cli` 输入 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap`、`QUIT` 的关键结果。`SMOKEJSON` 仍为 `read_only=true`、`execution_allowed=false`；CHECKJSON 只分类 LOAD 为写命令，报告 `touches_wal=false`，不执行 LOAD；CLI 最后返回 `BYE`。

## 清理说明

截图与说明保留在 `f/1646/`，最终证据摘要保留在 `f/1646/证据/summary.txt`。验证结束会删除本轮创建的 `cmake-build-v1646` 及其中 smoke WAL，关闭截图浏览器，不保留 mini-kv 服务进程；历史 archive root、fixture 和旧 build 目录不移动、不删除。
