# v1607 命令归档说明

## 01-cmake-configure-build.png

命令：`cmake -S . -B cmake-build-v1607 -G "MinGW Makefiles"`，随后执行 `cmake --build cmake-build-v1607 -- -j 4`。

结果：CMake 配置成功，构建成功，新增的 f-folder 解释质量收口模块、命令、fixture 测试、中文讲解字数测试和 CLI 程序都已生成。

意义：证明 v1607 不是只改文档，而是完成了 C++ 模块、命令目录、测试目标和可运行二进制的构建闭环。配置阶段仍出现既有的 Windows object path 长度警告，但生成与构建均成功。

## 02-targeted-ctest.png

命令：目标 CTest，覆盖 `f_folder_explanation_quality_closeout`、`command_tests`、`command_catalog_tests`、`command_catalog_refactor_fixture_tests`、`shard_readiness_tests`、`line_editor_tests` 以及相关 current fixture 兼容测试。

结果：16/16 通过。

意义：证明新增 `SHARDFFOLDEREXPLANATIONQUALITYCLOSEOUTJSON` 已接入命令目录、自动补全、SHARDJSON、当前 fixture、规则目录、检查目录、validation 失败路径和中文讲解三千字测试。

## 03-full-ctest.png

命令：`ctest --test-dir cmake-build-v1607 --output-on-failure`。

结果：334/334 通过。

意义：证明 v1607 没有破坏既有 WAL、TCP、restore、credential、archive、runtime receipt、command catalog 和各类只读证据测试。新增解释质量规则保持 read-only，不影响存储行为。

## 04-cli-smoke.png

命令：通过 `minikv_cli.exe` 依次执行：

```text
SHARDFFOLDEREXPLANATIONQUALITYCLOSEOUTJSON
CHECKJSON SHARDFFOLDEREXPLANATIONQUALITYCLOSEOUTJSON
SHARDJSON
COMMANDSJSON
QUIT
```

结果：CLI smoke 标记全部通过，包括 contract、v1607 release、`minimumWalkthroughChineseCharacterCount=3000`、`fillerPaddingAllowed=false`、`pictureDirectory=f/1607/图片/`、validation passed、CHECKJSON read-only/no execution/no write/no WAL、SHARDJSON 嵌入新 section、`commandCount=94`、COMMANDSJSON 暴露新命令。

意义：证明用户真实入口可以读到 v1607 单大版本解释质量证据，且该证据不启动服务、不写 store、不触 WAL、不打开 router、不允许执行。

## 归档边界

本轮截图通过 Playwright MCP 截取。由于 MCP 阻止 `file://` 访问，临时启动了一个只服务 `.tmp-v1607-archive-pages` 的本地 HTTP server，用于渲染四个临时 HTML 页面；截图完成后会在 cleanup 中停止并删除临时页面和临时日志。
