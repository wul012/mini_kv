# v1633 最终交付证据说明

v1633 不新增文件系统功能，而是在 v1631 的磁盘用户表/MFD/UFD 与 v1632 的 fd 位移/range I/O 已通过后，重新执行验收、真实演示和文档渲染。本目录保留五张最终证据，不保存构建二进制或原始临时日志。

## 01-final-focused-tests.png

命令：`ctest --test-dir cmake-build-v1631 -R '^osfs_tests$|^osfs_cli_smoke$' --output-on-failure`。最终重跑结果 2/2 通过。它覆盖错误密码、二级目录、权限、持久化、offset、跨块 I/O 和真实 CLI smoke。

## 02-final-osfs-demo.png

真实 `minikv_osfs.exe` 使用 `课程设计交付/v1633-osfs-final/osfs-demo-script.txt` 生成。输出从未登录阻断开始，经过错误密码、Alice 创建/连续写/分段读、chmod 0600、Bob UFD 隔离和权限拒绝，最后由 root 列 Alice UFD 并读取 `course-design`。同一份输出原文保存在 `osfs-demo-transcript.txt`。

## 03-full-ctest-summary.png

命令：`ctest --test-dir cmake-build-v1631 --output-on-failure -j 4`。最终重跑结果 344/344 通过，耗时约 23 秒。该回归在全部目标重新链接后执行，证明 OSFS 公共头文件和共享静态库变化未破坏原 KV、WAL、snapshot、RESP、TCP 与治理证据测试。

## 04-report-render-check.png

最终 DOCX 通过 LibreOffice 导出 PDF，再由 Poppler 以 120 DPI 渲染全部 9 页并合成接触表。检查范围包括封面、需求表、三张结构图、模块表、两张运行证据、需求-证据表、设计边界、总结和参考资料。中间曾发现需求表跨页和近空白参考资料页，生成器调整分页与参考资料段落后重新导出；最终版本无裁切、重叠、缺字或无意义空白页。

## 05-doc-quality-gates.png

命令：`ctest --test-dir cmake-build-v1631 -R 'code_walkthrough_quality_gate_np|f_folder_explanation_quality_closeout' --output-on-failure`。最终重跑结果 11/11 通过。它证明 v1633 的中文讲解、f-folder 解释、归档说明和质量门没有因为最终材料收口而退回到短讲解、空解释或截图列表式凑数。

## 进程与临时文件

演示磁盘镜像已删除，LibreOffice 转换进程已结束。`cmake-build-v1631` 中的日志、渲染页和独立 LibreOffice profile 属于任务中间产物，将在最终提交前按 cleanup gate 删除；DOCX、PDF、最终图、transcript、需求矩阵和本说明保留。
