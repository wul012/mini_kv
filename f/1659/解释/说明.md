# v1659 命令归档说明

本目录记录有界优雅计划 E-M1 的选择性最终证据。截图在扫描器、baseline、文档、冷构建、聚焦合同、真实 CLI 和 351 条完整 CTest 稳定后生成；页面由 Playwright MCP 渲染并截图，没有保存构建二进制或整份冗长日志。

## 01-name-census.png

记录 40 字符预算的机械定义、740 个长文件名与 883 个公共头文件可见长标识符的真实起点，以及 exact set 加编译期 maximum 的双层 ratchet。数字代表需要逐步偿还的存量债务，不是对历史命名合理性的认可。

## 02-red-green-proof.png

记录负向与正向证据。临时注入第 741 个长文件名后，`elegance_name_census_contract` 明确失败并打印路径；`finally` 删除探针后，四条聚焦合同通过，全量 CTest 为 351/351。它证明新门会因真实退化变红，而不是只读取一份自述报告。

## 03-contract-boundaries.png

记录冻结 fixture 聚合摘要和真实 CLI 边界。1514 个 `fixtures/release/*.json` 的确定性 manifest SHA-256 为 `8eb430889b84bec547abcd4d963636666edd897379769969cf4c715af7f8e00f`；SMOKEJSON/CHECKJSON/SHARDJSON 继续证明 read-only、no router、no write execution、no execution。本版只增加维护门，不增加运行权限。

## 进程与临时文件

- 本版没有启动服务器或其他后台进程。
- Playwright 证据页面在截图后关闭。
- `cmake-build-v1659/` 在提交前删除；故意失败探针已经由验证命令的 `finally` 删除。
- 最终保留三张 PNG、本说明、`f/1659/证据/summary.txt`、v1659 中文讲解、scanner、baseline 和版本文档。
