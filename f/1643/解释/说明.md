# v1643 命令截图说明

本轮归档对应 mini-kv v1643：完整迁移 endpoint-handle allowlist approval contract receipt 的顶层 JSON 组装，删除 boundary object 先生成再剥壳拼回的临时桥接；不新增命令，不改 fixture/digest/public surface，不打开 router、Store/WAL 写入、credential 读取、raw endpoint 解析、HTTP/TCP、schema、restore/load/compact/SETNXEX、auto-start 或 execution authority。

截图使用 Playwright MCP 从真实命令结果摘要页面生成，保留关键结论而不归档完整编译日志或二进制。最终 cleanup 删除 `cmake-build-v1643` 并关闭本轮浏览器页。

## 01-scope-and-structure.png

记录计划范围、源码结构和迁移结果：Node 最新 plans 文件没有要求 mini-kv 新功能；本地 v1637 阶段一要求继续 receipts consolidation。endpoint formatter 改为命名嵌套对象/数组和顶层 ordered field list，endpoint 专属 boundary 表仍由本文件拥有。

## 02-focused-parity.png

记录 focused CTest 5/5：builder parity、endpoint receipt、SMOKEJSON、runtime smoke evidence、release manifest 全部通过。最关键的 endpoint fixture 子对象与 formatter 逐字节相等，没有 canonical normalization。

## 03-full-ctest-cli.png

记录最终完整 build、全量 CTest 与真实 CLI smoke。`SMOKEJSON` 继续输出 receipt；`CHECKJSON LOAD data/prod.snap` 只报告写入副作用并拒绝执行，不修改 Store/WAL。

## 04-quality-cleanup.png

记录 clang-format、文档/讲解/归档门、`git diff --check`、strict archive inventory 与 cleanup 结果；同时记录源文件 503→558 行是结构展开，不是 shrink stop condition。

## 清理说明

本版没有启动 mini-kv server、Node、Java 或长期服务。最终保留本目录 PNG、说明、`f/1643/证据/summary.txt`、中文讲解、治理 note、进度账本与源码；删除临时构建目录和截图页面。
