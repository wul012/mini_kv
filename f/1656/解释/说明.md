# v1656 命令归档说明

本目录归档 receipts consolidation 第一阶段完成版的最终证据。图片在实现、格式化、正式文档、3101 个中文字符的代码讲解、12 条 focused CTest、348 条 full CTest 与真实 CLI smoke 全部稳定后生成。页面使用 Playwright MCP 打开本地内存 HTML 并执行 full-page PNG capture；图片只把已经由真实命令得到的结果变成易读页面，机械原始结论以 `../证据/summary.txt`、CTest 退出码和 v1653 oracle 为准。

## 01-byte-parity.png

对应 `runtime_receipt_remaining_owner_baseline_tests`。manual dry-run 继续保持 fixture 3199 bytes/56 fields 与 runtime 3060 bytes/53 fields，且只使用既有的三个 frozen metadata 字段规则；manual precheck、disabled adapter、fake transport、endpoint handle 分别保持 4176/74、5361/92、6040/92、6340/92 的 raw exact equality。测试、fixture、digest 输入和兼容规则没有为迁移而修改。

## 02-builder-census-and-size.png

对应 `receipt_builder_census_contract` 与物理行盘点。机械结果为 28 sources、27 formatter owners、27 builder-backed、0 pending、1 named no-formatter waiver，floor 从 25 收紧到 27。evidence owner 拆成 700 行 receipt owner 与 52 行 session 支撑单元，formatter ownership 不变；sandbox owner 为 704 行，另有 46 行无参数 exact-common-field helper。两名 owner 均低于 800 行停止线，没有用 companion formatter 规避 census，也没有虚报 shrink。

## 03-build-and-tests.png

对应单一构建进程下 all-target build 100%、最终 focused 12/12 与 full CTest 348/348。首次 all-target attempt 因两个被工具超时中断但仍存活的 make 树重叠而缺少 `objects.a`；进程审计并停止本轮残留后，441.8 秒干净重建成功。最终全量测试耗时 183.03 秒，同时覆盖 Store、WAL、snapshot、RESP/TCP、OSFS、archive retention 和所有历史 receipt family。clang-format dry-run、`git diff --check` 通过，`tests/` 与 `fixtures/release/` 零改动。

## 04-cli-boundaries.png

对应真实 `minikv_cli` 的 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap` 与 `QUIT`。脚本结构化解析五份 runtime receipt，每份逐项确认 read-only 为 true，execution、credential read、managed-audit store、storage/write、runtime write、load/restore/compact、auto-start 与 order authority 为 false，均为 10/10；LOAD 仍只被解释，不执行、不触 WAL，最终退出为 `BYE`。

验证结束后不会遗留 mini-kv server 或浏览器服务。`cmake-build-v1656` 只用于本版验证，将在提交前按 cleanup gate 删除；最终保留四张 PNG、本说明、`证据/summary.txt`、生产重构、正式文档与代码讲解。
