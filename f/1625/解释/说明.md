# v1625 命令归档说明

## 01-scope-and-census.png

展示 v1625 的工作范围和量化基线：`runtime_*_receipts.cpp` 共 28 个文件、788,811 字节；credential resolver 子家族共 26 个文件、706,166 字节。本版只冻结 ordered zero-dependency builder、fixture 子对象 byte parity、分批迁移和停止条件，不修改 runtime formatter、fixture、digest、command、router、WAL、write 或 execution authority。

## 02-focused-ctest.png

展示 focused CTest 7/7 通过。覆盖新增 `receipts_consolidation_note_contract`、项目导览合同、SMOKEJSON receipt、runtime smoke fixture、verification manifest，以及未来首批候选 no-network / abort-rollback receipt 测试。

## 03-full-ctest.png

展示 fresh `cmake-build-v1625` 上全量 CTest 341/341 通过。说明新增设计合同已经进入完整测试总表，并且 Store、command、WAL、snapshot、RESP、TCP 与历史 evidence 测试没有回归。

## 04-quality-and-archive-gates.png

展示中文讲解 3462 个中文字符，超过 3000 字级别门槛；`git diff --check` 无 whitespace error；归档 inventory strict 检查无 warning，v1624 最新归档仅 0.18 MiB，低于 8 MiB 前向预算。

## 运行和清理说明

本版没有启动 mini-kv server、Node 或 Java，也没有执行真实 TCP/HTTP、LOAD、COMPACT、RESTORE、SETNXEX、credential 读取或 managed audit 连接。为生成截图临时启动的本地静态 HTTP 服务只暴露 `.tmp-v1625-evidence-html`，截图完成后停止；临时网页和 `cmake-build-v1625` 在提交前删除。最终只保留本目录说明和 `f/1625/图片/` 下四张精选 PNG。
