# mini-kv v1611 命令归档说明

本目录记录 production-excellence K1 required sanitizer gate 收口版本的关键验证截图。v1611 删除 sanitizer job 的 `continue-on-error: true`，让 Ubuntu ASan/UBSan job 成为 required CI gate；本版不新增运行时命令，不移动历史归档，不修改 release fixture，不打开 router，不写 store，不触 WAL，不授予 execution authority。

## 01-v1610-ci-green-prerequisite.png

对应命令：

```powershell
gh run view 27398549031 --repo wul012/mini_kv --json status,conclusion,jobs
```

结果：v1610 CI run 结论为 success，其中 `ubuntu sanitizer` job 的 configure、build、CTest 全部 success。

意义：证明 K1 诊断轨道已经在 GitHub Ubuntu 上跑绿，因此 v1611 可以合理移除 `continue-on-error: true`。

## 02-ci-required-diff.png

对应检查：

```powershell
git diff -- .github/workflows/ci.yml tests/dependabot_config_tests.cpp
```

结果：`.github/workflows/ci.yml` 删除 sanitizer job 下的 `continue-on-error: true`；`dependabot_config_tests.cpp` 从 `assert_contains` 改为 `assert_not_contains`。

意义：证明 sanitizer 轨道不只是当前 YAML required，还是被 C++ 测试守住的 required gate。

## 03-cmake-configure.png

对应命令：

```powershell
$cmake -S . -B cmake-build-v1611 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

结果：默认 Debug CMake 配置和生成通过。

意义：证明 required sanitizer CI 变更不影响本地默认构建配置。

## 04-cmake-build.png

对应命令：

```powershell
$cmake --build cmake-build-v1611 -- -j 4
```

结果：Debug MinGW 构建完成，所有目标编译链接通过。

意义：证明 CI 测试断言翻转后仍可正常构建。

## 05-focused-ctest.png

对应命令：

```powershell
$ctest --test-dir cmake-build-v1611 -R 'dependabot_config_tests|command_tests|smokejson_command_receipt_tests|runtime_smoke_evidence_tests|metrics_file_tests' --output-on-failure --timeout 120 --progress
```

结果：focused CTest 通过。

意义：证明 required sanitizer job 断言、命令路径、runtime smoke evidence、metrics 相关目标和默认只读边界都没有被打偏。

## 06-full-ctest.png

对应命令：

```powershell
$ctest --test-dir cmake-build-v1611 --output-on-failure --timeout 120 --progress
```

结果：334/334 通过，0 失败。

意义：证明 v1611 没有破坏命令行为、fixture、WAL、snapshot、RESP、TCP、恢复证据和治理 receipt 边界。

## 07-quality-checks.png

对应命令：

```powershell
git diff --check
clang-format --dry-run --Werror tests/dependabot_config_tests.cpp
```

并检查 v1611 中文讲解字符数。

结果：`git diff --check` 无 trailing whitespace；变更 C++ 测试文件 clang-format 通过；v1611 中文讲解超过 3000 中文字符。

意义：证明本版没有文档 trailing whitespace 瑕疵，并符合 v1607 后中文讲解质量门。

## 清理记录

- 本轮使用 Playwright MCP 渲染截图，截图完成后关闭浏览器会话。
- `cmake-build-v1611` 是本轮构建目录，最终 cleanup gate 会删除。
- 本轮没有启动长期服务端或后台进程。
