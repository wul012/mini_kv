# v1660 命令归档说明

本目录记录有界优雅计划 E-M2 的最终选择性证据。三张 PNG 在路径改名、Windows CRLF 修复、冷构建、聚焦测试、完整 CTest、fixture 摘要和真实 CLI 稳定后由 Playwright MCP 渲染；不保存构建二进制或整份日志。

## 01-top-five-ratchet.png

展示五个内部 `.cpp` stem 从 132-143 字符降至 28-33 字符，以及机械 census 从 740/883 收紧到 735/883。公共 header 和标识符数量保持不动，表示本版只偿还确认可安全修改的私有路径债务。

## 02-windows-ci-repair.png

展示 v1659 run `29176190797` 的真实结果：六个 job 通过，Windows 因 CRLF 行尾导致 baseline 全量错配。v1660 只剥离 `std::getline` 后的单个 `\r`，并把 CRLF 样本纳入每次运行的自检；本地修复后 351/351 通过。

## 03-byte-contracts.png

展示五对 Git blob 完全相等、1514 个 release fixture 的 manifest SHA-256 不变，以及真实 SMOKEJSON/CHECKJSON/SHARDJSON 继续 read-only、no router、no write execution、no execution。它证明路径清理没有借机修改实现或运行权限。

## 进程与临时文件

- 本版没有启动服务器或其他后台进程。
- Playwright 页面在截图后关闭，`.playwright-mcp/` 删除。
- `cmake-build-v1660/` 在提交前删除。
- 最终保留三张 PNG、本说明、`f/1660/证据/summary.txt`、v1660 中文讲解、五个 rename、CRLF parser 修复与版本文档。
