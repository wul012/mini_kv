# v1655 命令归档说明

本目录归档 implementation chain 五个 receipt owner 的 ordered-builder 迁移证据。所有图片都在代码、格式、测试、账本、consolidation note 和 3111 个中文字符的代码讲解稳定后生成。截图使用 Playwright MCP 打开本地内存证据页并执行 full-page PNG capture；页面只呈现已经由真实命令得到的机械结果，不替代 `证据/summary.txt`、CTest 退出码或 v1653 oracle。

## 01-byte-parity.png

对应 `runtime_receipt_remaining_owner_baseline_tests` 的最终结果。五份迁移对象仍分别为 16889/19163/13870/15264/21562 字节与 112/121/135/138/113 个顶层字段，全部为 fixture/runtime raw exact equality，摘要保持 v1653 冻结值。本版没有新增 apostrophe、字段顺序、metadata 或 whitespace 兼容规则，也没有修改 oracle、领域测试或 fixture。

## 02-builder-census-and-size.png

对应 `receipt_builder_census_contract` 与源文件物理行盘点。机械口径为 28 个 source、27 个 formatter owner、25 个 builder-backed、2 个 pending、1 个命名 waiver，floor 从 20 收紧到 25。五个 owner 迁移后为 460/551/479/461/501 行，均低于 800 行停止线；净增行来自具名对象和显式字段所有权，不以虚假 shrink 作为收益，也没有通过 companion 文件规避 census。

## 03-build-and-tests.png

对应 all-target build、代码 focused 10/10、文档归档完成后的最终 focused 22/22，以及两轮 full CTest 348/348。最终一轮耗时 106.62 秒；focused 同时覆盖 census、十三对象 oracle、五个领域 receipt、SMOKEJSON、managed-audit 聚合、readonly fixture、consolidation note、代码讲解和归档说明质量。全量继续覆盖 Store、WAL、snapshot、RESP/TCP、OSFS、archive retention 与全部历史 receipt family。最终 `git diff --check` 和 clang-format dry-run也通过，`tests/` 与 `fixtures/release/` 零改动。

## 04-cli-boundaries.png

对应真实 `minikv_cli` 的 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap` 与 `QUIT`。脚本结构化解析五份 runtime receipt，每份逐项确认 read-only 为 true，execution、credential read、managed-audit store、storage/write、runtime write、load/restore/compact、auto-start 与 order authority 为 false；检查 LOAD 仍不执行命令、不触 WAL，最终退出为 `BYE`。

验证结束后没有遗留 mini-kv server、浏览器服务或其他仓库后台进程。`cmake-build-v1655` 仅用于本版验证，将在提交前按 cleanup gate 删除；最终保留四张 PNG、本说明、`证据/summary.txt`、生产重构、正式文档和代码讲解。
