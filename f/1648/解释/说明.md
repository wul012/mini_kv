# v1648 命令归档截图说明

v1648 暂停生产 formatter 迁移，先为 runtime-shell 连续链上的五份回执冻结 exact fixture parity，并把通用 parity helper 从集中 builder 测试拆到 test-only 支撑 header。fixture 与生产源均未修改，builder census 保持 9/18/1。

## 01-family-frozen-matrix.png

记录五份 fixture 子对象的固定字节数、顶层字段数、v1648 前缀摘要和 exact runtime parity。五份 fixture/runtime 的 `\\u0027` 计数均为 0，说明本族没有新增 canonical spelling waiver；下一版迁移必须保持整对象相等。

## 02-focused-census.png

记录最终 focused lane 9/9 通过，以及 census 的 28 sources、27 formatter owners、9 builder-backed、18 pending、1 named waiver。意义是新 family CTest、通用 builder 测试、文档合同、census 和五个领域测试同时为绿，同时没有把测试新增虚报成生产迁移。

## 03-full-ctest.png

记录文档与索引落地后的最终 full CTest 346/346 通过，耗时 108.77 秒。意义是共享 test helper 抽取和第 346 个家族测试没有破坏 Store、command、WAL、snapshot、RESP/TCP、OSFS、shard readiness、archive、release manifest 或其他 receipt family。

## 04-real-cli-boundaries.png

记录真实 `minikv_cli` 依次执行 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap`、`QUIT` 的精简断言结果。五个 runtime-shell 回执 marker 均出现，`read_only` 与 `execution_allowed=false` 大量存在；LOAD 只被分类，`touches_wal=false`，最后返回 `BYE`。这证明测试拆分没有打开 router、write、WAL、network、credential 或 execution 路径。

## 清理说明

截图使用 Playwright MCP 从只含最终断言摘要的内存页面生成。最终保留 4 张 PNG、本文和 `f/1648/证据/summary.txt`；提交前删除 `cmake-build-v1648`、`.v1648-final-*.log` 与本轮 Playwright 临时输出，关闭浏览器，不保留 mini-kv 服务进程，也不移动历史归档或 frozen fixture。
