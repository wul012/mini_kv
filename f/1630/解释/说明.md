# mini-kv v1630 课程设计交付收尾归档说明

本目录归档 v1630 的关键命令与交付证据。v1630 不新增 OSFS 运行时功能，目标是把 v1629 已完成的 OSFS 文件系统层整理成可提交、可演示、可打印的操作系统课程设计材料。

## 01-course-requirements.png

来源：从 `D:\qqq\课程设计(操作系统)要求2026.pdf` 中抽取关键要求。

结果：截图记录了课程 PDF 第 7 页的提交要求，以及第 35 页“Linux 二级文件系统”的核心功能要求：二进制文件模拟磁盘、超级块、inode、文件信息、`dir/create/delete/open/close/read/write/login`、目录列字段和读写保护。

意义：证明本版交付方向不是临时扩展，而是直接对齐课程要求中的实验二。

## 02-build-focused-tests.png

命令：

```text
cmake -S . -B cmake-build-v1630 -G "MinGW Makefiles"
cmake --build cmake-build-v1630 --target minikv_osfs minikv_osfs_tests -j 4
ctest --test-dir cmake-build-v1630 -R "^osfs_(tests|cli_smoke)$" --output-on-failure
```

结果：`minikv_osfs` 和 `minikv_osfs_tests` 构建通过，`osfs_tests` 与 `osfs_cli_smoke` 2/2 通过。

意义：证明课程演示入口和 OSFS 重点测试仍然可构建、可运行。

## 03-osfs-course-demo.png

命令：

```text
minikv_osfs --disk cmake-build-v1630/osfs-course-demo.img --format --blocks 96 --script 课程设计交付/v1630-osfs-final/osfs-demo-script.txt
```

结果：演示输出包含 `WHOAMI`、`DIR`、`CREATE report`、`OPEN report rw`、`WRITE`、`READ`、`CHMOD report 0600`、`LOGIN guest`、权限拒绝、`DIR` 列表和 `STAT report`。

意义：证明课程演示路径覆盖文件创建、句柄式打开、写入读取、权限修改、普通用户拒绝读取和目录字段输出。

## 04-report-render-check.png

命令：

```text
python tools/build_osfs_coursework_report.py
soffice.com --headless --convert-to pdf --outdir 课程设计交付/v1630-osfs-final <docx>
pdftoppm -png -r 150 <pdf> cmake-build-v1630/report-render-pages-final/page
```

结果：生成 `操作系统课程设计报告-OSFS-v1630.docx` 和 `操作系统课程设计报告-OSFS-v1630.pdf`，PDF 最终渲染为 8 张页面 PNG，并抽查封面、结构图、流程图、演示截图、测试截图和结尾页。

意义：证明报告不是只生成文件名，而是完成了 Word 到 PDF 到 PNG 的视觉检查闭环。LibreOffice/Poppler 在本机中文用户名路径下输出了编码警告，但 DOCX、PDF 和页面 PNG 均成功生成。

## 05-full-ctest-summary.png

命令：

```text
ctest --test-dir cmake-build-v1630 --output-on-failure
```

结果：全量本地 CTest `344/344` 通过，失败数为 0，总耗时约 156.80 秒。

意义：证明 v1630 的报告和交付材料没有破坏 mini-kv 既有 KV、WAL、snapshot、TCP/RESP、receipt、archive guard 和 OSFS 测试面。

## 清理记录

本轮归档截图使用临时本地 HTTP 服务展示 HTML 页面，并已在截图后停止该服务。最终应保留本目录截图、交付目录、报告、PDF、说明文档和源码改动；构建目录与中间渲染目录在最终 cleanup gate 中删除。
