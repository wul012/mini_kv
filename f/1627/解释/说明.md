# mini-kv v1627 命令归档说明

本目录记录 v1627 的最终归档证据。本版范围是：先把已经具备 fixture byte parity 的 abort/rollback receipt formatter 迁移到内部 ordered JSON builder，再只读查清 no-network 的 pre-migration drift，暂不迁移 no-network formatter。

## 01-scope-and-no-network-drift.png

对应本版范围和 no-network 漂移诊断。截图说明：

- 迁移文件是 `src/runtime_credential_resolver_abort_rollback_semantics_receipts.cpp`。
- no-network formatter 本版不修改，`fixtures/release/*.json` 本版不修改。
- no-network 当前 formatter 与 committed fixture 的 JSON 语义一致：字段数 118、字段顺序一致、递归解析值无差异、`receipt_digest` 均为 `fnv1a64:3d8c483c93f8acf9`。
- 唯一原始字节差异是历史 apostrophe 转义：fixture 使用 `Node v323\u0027s no-network fixture contract`，当前 formatter 使用 `Node v323's no-network fixture contract`。

意义：本版没有把 no-network 漂移模糊处理，而是把它固定为一处可命名、可复现、可测试的字节级差异。下一版若迁移 no-network，必须显式处理这个差异。

## 02-focused-ctest.png

对应最终文件状态下的 focused CTest：

```powershell
ctest --test-dir cmake-build-v1627 --output-on-failure --timeout 120 --progress -R 'runtime_receipt_json_builder_tests|receipts_consolidation_note_contract|smokejson_command_receipt_tests|release_verification_manifest_tests|runtime_smoke_evidence_tests|no_network_safety_fixture_contract_receipt_tests|abort_rollback_semantics_contract_receipt_tests'
```

结果为 7/7 通过。覆盖面包括：

- `receipts_consolidation_note_contract`：文档契约仍能识别 receipts consolidation 的关键设计和边界。
- `runtime_receipt_json_builder_tests`：builder、abort/rollback byte parity、no-network 漂移定位均通过。
- `smokejson_command_receipt_tests`、`runtime_smoke_evidence_tests`、`release_verification_manifest_tests`：运行时证据、SMOKEJSON 和 manifest 消费未断链。
- `no_network_safety_fixture_contract_receipt_tests`、`abort_rollback_semantics_contract_receipt_tests`：两份关键 receipt 的边界字段仍保持原语义。

## 03-full-ctest.png

对应最终文件状态下的全量 CTest：

```powershell
ctest --test-dir cmake-build-v1627 --output-on-failure --timeout 120 --progress
```

结果为 342/342 通过，总耗时 118.45 秒。

意义：本版虽然主要是内部 formatter 迁移和测试/文档加固，但仍重新跑过全部 CTest，覆盖 WAL、snapshot、RESP、TCP、command、runtime evidence、release manifest、credential resolver receipts、归档和治理类契约，证明没有侧向破坏 runtime 行为或证据链。

## 04-quality-gates.png

对应质量闸：

```powershell
clang-format --dry-run --Werror src\runtime_credential_resolver_abort_rollback_semantics_receipts.cpp tests\runtime_receipt_json_builder_tests.cpp
git diff --check
python scripts\archive_inventory.py --budget-mib 8 --strict
```

结果：

- `clang-format --dry-run --Werror` 通过。
- `git diff --check` 通过；Windows 本地仅提示 Git LF/CRLF working-copy 警告，不是 trailing whitespace 或 patch error。
- archive inventory 严格检查通过，活动根 `f/` 仍在预算内。
- 本版代码讲解 `代码讲解记录_生产雏形阶段_第四册/995-version-1627-abort-rollback-builder-migration-and-no-network-drift.md` 长度为 9389 字符，满足 3000 字级中文讲解要求。

## 边界结论

v1627 没有新增命令、没有修改 fixture、没有打开 router、没有 Store/WAL 写入扩张、没有 credential value/raw endpoint/network access、没有 rollback/deployment/LOAD/COMPACT/RESTORE/SETNXEX execution authority。mini-kv 仍然只是只读证据生产方。
