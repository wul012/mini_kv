# v1618 命令归档说明

本目录归档 mini-kv v1618 production-excellence K4 command-family executor split 的关键命令截图。v1618 只做 C++ command 侧维护性拆分：`command_dispatch.cpp` 降为薄分发器，string/expiry/persistence/introspection 四类命令执行体进入独立 `.cpp` 文件；没有新增命令、没有改 public command output、没有打开 active shard router、write routing 或执行权限。

## 截图清单

1. `01-cmake-configure.png`：重新配置 `cmake-build-v1618`，确认新增 translation unit 已进入 CMake 工程。输出只有既有长路径 warning，没有 configure error。
2. `02-build.png`：构建 v1618，确认 `command_string_ops.cpp`、`command_expiry_ops.cpp`、`command_persistence_ops.cpp`、`command_introspection_ops.cpp` 与原 `CommandProcessor` 链接成功。
3. `03-focused-tests.png`：运行 command/fixture/WAL/snapshot/runtime/TCP 相关定向测试，确认命令家族拆分不改变输出和证据。
4. `04-full-ctest.png`：运行全量 CTest，`337/337` 通过，覆盖 shard readiness、runtime receipts、restore、TCP/RESP 等后段证据。
5. `05-format-diff-check.png`：运行 `clang-format 18.1.3` dry-run 和 `git diff --check`，确认格式 gate 与 trailing whitespace gate 干净。
6. `06-cli-tcp-smoke.png`：运行真实 CLI smoke 和本地 TCP smoke，证明拆分后的命令路径在可执行程序中也可用；TCP server 验证结束后已停止。

## 结果意义

v1618 的证据链证明：命令执行体从 dispatch 文件拆出后，`SET/GET/DEL`、`SETNXEX/EXPIRE/TTL`、`SAVE/LOAD/COMPACT/WALINFO`、`SIZE/KEYS/CHECKJSON/HELP` 仍保持原有行为；`CHECKJSON` 仍只解释契约，不执行目标命令；WAL、snapshot、TCP/RESP 与只读 evidence fixture 均未被破坏。K4 仍未到 review 点，下一步需要拆 `shard_readiness.cpp` 并完成整个 K4 milestone 后再进入 planner review。
