# v1647 命令归档截图说明

v1647 关闭 post-echo 的单字节 `} },` runtime 漂移，再迁移完整 formatter。fixture 未改，测试从专属 whitespace 诊断分支收紧为唯一 Node v310 apostrophe 规则下的整对象 parity，census floor 由 8 提到 9。

## 01-pre-gate-red.png

记录只修生产 formatter 一个空格、尚未改测试时的预期失败：旧断言仍要求 fixture/runtime 差 4 字节，在 `runtime_receipt_json_builder_tests.cpp:442` 失败，CTest exit code 8。意义是证明生产修正先发生，测试确实捕获状态变化。

## 02-parity-census.png

记录测试收紧后的 parity+census 2/2 通过，以及 28 sources、27 formatter owners、9 builder-backed、18 pending、1 waiver。意义是 whitespace 特例已删除，post-echo 只剩命名 apostrophe 规则，builder ratchet 同步收紧。

## 03-focused-surfaces.png

记录 post-echo 领域测试、builder parity、census、SMOKEJSON、runtime evidence 和 release manifest 共 6/6 通过。意义是 standalone 与三条下游消费面共同接受唯一 compact surface。

## 04-full-cli.png

记录 full CTest 345/345，以及真实 CLI 中 compact transition=1、spaced transition=0、read-only/deny-execution、CHECKJSON LOAD 分类、`touches_wal=false` 和 `BYE`。意义是全项目无回归，真实程序没有打开执行路径。

## 清理说明

最终保留 4 张 PNG、本文与 `f/1647/证据/summary.txt`。验证结束删除 `cmake-build-v1647`、smoke WAL 和本轮 Playwright snapshot，关闭浏览器，不保留 mini-kv 服务进程；不移动历史归档或 fixture。
