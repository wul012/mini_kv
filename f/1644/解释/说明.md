# v1644 命令截图说明

v1644 完整迁移 signed-human approval artifact receipt 顶层 JSON，并删除 boundary strip-and-splice 临时桥。唯一 `Node v314\\u0027s` fixture 与 runtime apostrophe 差异仍由测试中的命名常量单点处理，生产 builder 不做 normalization。

## 01-scope-canonical.png

展示本版范围、491→545 行结构展开和 signed-human family 单点 canonical 规则；没有新增签名、凭据、网络、写入或执行能力。

## 02-focused-parity.png

展示 focused 5/5：builder parity、signed-human receipt、SMOKEJSON、runtime smoke、release manifest。整对象在单次命名规范化后相等。

## 03-full-ctest-cli.png

展示最终完整 build、344/344 CTest 和真实 CLI smoke；CHECKJSON 仍只分类 LOAD，不执行写入。

## 04-quality-cleanup.png

展示固定 hook formatter、staged pre-commit、文档/归档门、strict inventory 和 cleanup。

## 清理说明

最终保留 PNG、说明、证据 summary、中文讲解、源码与进度文档；删除 `cmake-build-v1644`，关闭 Playwright 页面，不保留 server 或其他后台进程。
