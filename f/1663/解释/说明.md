# v1663 命令归档说明

本目录保存 OSFS 通俗教程与韧性证据版的关键命令截图。截图只压缩呈现已核实的本地终端结果；完整命令、数字和边界见 `../证据/summary.txt`。本轮没有浏览器页面或可复用 Playwright 会话，命令输出采用本地文本渲染 PNG，未把 Windows 输出冒充 FinalShell/Linux 证据。

## 图片说明

1. `01-focused-osfs-tests.png`：重新构建后运行原核心测试、新 resilience 测试和真实 CLI smoke，3/3 通过。它证明新增边界用例没有替代原行为测试。
2. `02-indirect-cli-demo.png`：当前 v1663 二进制执行 v1636 demo script，写入 4291 字节，从 offset 4088 读出 direct/indirect 跨界标记，并完成用户管理和干净 FSCK。
3. `03-fsck-corruption.png`：从 STAT 取得物理块 20，清除 block bitmap 对应位后，FSCK 报告同一块号 mismatch。镜像仅用于本地故障注入，验证后删除。
4. `04-doc-quality.png`：九个教程 Markdown 的链接检查、文档合同、讲解中文字数和段落长度结果。它证明新材料不是只有目录入口。
5. `05-full-ctest.png`：修正 41 字符测试名后重新跑最终全量，注册数 352，352/352 通过，0 失败，耗时 122.05 秒。

## 保留与待办

保留教程、测试、讲解、说明、五张 PNG 和 summary。`cmake-build-v1663` 属于临时验证构建，将在提交前删除。FinalShell Step 08/09 目前只保留命令和待实跑说明；远端真实输出由用户在 FinalShell 执行后再补，不能从本地截图转写。
