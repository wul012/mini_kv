# v1638 命令归档说明

## 01-focused-approval-parity-tests.png

对应命令：

```powershell
ctest --test-dir cmake-build-v1638 -R 'runtime_receipt_json_builder_tests|signed_human_approval_artifact_contract_receipt_tests|credential_handle_approval_contract_receipt_tests|endpoint_handle_allowlist_approval_contract_receipt_tests|smokejson_command_receipt_tests|runtime_smoke_evidence_tests|release_verification_manifest_tests' --output-on-failure
```

结果：7/7 通过。意义是新增审批合同 Slice 2 迁移前基线、三份历史审批合同 receipt 测试、SMOKEJSON 嵌入、runtime smoke evidence 和 release manifest 在同一个 focused gate 下保持一致。

## 02-full-ctest.png

对应命令：

```powershell
ctest --test-dir cmake-build-v1638 --output-on-failure
```

结果：344/344 通过，用时 191.27 秒。意义是 v1638 的测试护栏没有破坏 OSFS、WAL、Snapshot、TCP/RESP、release manifest、runtime evidence、receipt chains 或既有 governance fixture。

## 03-format-diff-and-walkthrough.png

对应命令：

```powershell
clang-format --dry-run --Werror tests/runtime_receipt_json_builder_tests.cpp
git diff --check
git diff --stat
(Get-Content 代码讲解记录_生产雏形阶段_第四册/1006-version-1638-approval-contract-parity-baseline.md -Encoding utf8 -Raw).Length
```

结果：clang-format dry-run 通过；`git diff --check` exit code 为 0；讲解长度为 6494 字符。Git 输出 LF/CRLF 工作区提示，属于本机换行提示，不是 whitespace error。

## 边界说明

本版只新增审批合同迁移前 parity 基线和对应文档，没有修改 `fixtures/release/*.json`，没有迁移 formatter，没有新增 runtime command，没有启动服务，没有打开网络连接，没有读取 credential value，没有解析 raw endpoint URL，没有写 Store/WAL，没有执行 `LOAD`、`COMPACT`、`RESTORE`、`SETNXEX`、rollback 或 deployment，也没有修改 Node/Java 项目。
