# v1645 命令截图说明

v1645 只建立 approval lifecycle 迁移前 parity 与全局 receipt census 门，不修改 production formatter。四候选中三份 migration-ready，post-echo 因独立 `} },` 空格漂移被阻塞。

## 01-census.png

记录 28 sources、27 formatter owners、5 builder-backed、22 pending、1 named waiver，以及 builder floor=5。

## 02-parity-baseline.png

记录四份 fixture 的 size/digest/field count：12949/108、16483/107、17116/107、14169/101；前三份可迁移。

## 03-post-echo-stop.png

记录 Node v310 apostrophe 之外的 `}},` vs `} },` 一字节漂移；明确它不是第二个 waiver，post-echo 从 v1646 排除。

## 04-full-quality.png

记录最终全量 CTest、格式、diff、archive inventory、中文讲解门和 cleanup。

## 清理说明

最终保留 tests/CMake census、文档、四张 PNG 与 summary；删除 `cmake-build-v1645`，关闭 Playwright，不留服务进程。
