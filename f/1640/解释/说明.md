# mini-kv v1640 命令归档说明

本版继续执行 `治理计划/v1637-production-excellence-completion-brief.md` 的 receipt consolidation 方向，把 endpoint-handle allowlist approval contract receipt 的 closed-boundary boolean flag block 迁移到内部 ordered JSON builder。迁移范围刻意和 v1639 对称：只迁移高风险布尔边界字段群，不重写整份 formatter，不改 fixture，不扩大 signed-human 的 historical spelling waiver。

截图使用 Playwright MCP 从命令摘要页面直接生成 PNG，未使用文本渲染回退。

## 01-focused-endpoint-boundary-tests.png

对应命令：

```powershell
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
ctest --test-dir cmake-build-v1640 --output-on-failure -R "runtime_receipt_json_builder_tests|smokejson_command_receipt_tests|release_verification_manifest_tests|runtime_smoke_evidence_tests|endpoint_handle_allowlist_approval_contract_receipt_tests"
```

结果：5/5 通过，总耗时 2.65 秒。

意义：focused lane 证明 endpoint-handle formatter 在迁移 closed-boundary flag block 后仍与 frozen fixture 子对象保持 byte parity；SMOKEJSON、runtime smoke evidence、release manifest 与 ordered JSON builder 行为也没有漂移。

## 02-full-build-and-ctest.png

对应命令：

```powershell
cmake -S . -B cmake-build-v1640 -G "MinGW Makefiles"
cmake --build cmake-build-v1640
ctest --test-dir cmake-build-v1640 --output-on-failure
```

结果：344/344 通过，`100% tests passed, 0 tests failed out of 344`，总耗时 157.44 秒。

意义：这是完整构建后的全量回归证据，覆盖 KV、WAL、snapshot、TCP/RESP、OSFS、runtime receipts、release manifest、governance evidence 等路径。

## 03-doc-format-walkthrough.png

对应检查：

```powershell
ctest --test-dir cmake-build-v1640 --output-on-failure -R "receipts_consolidation_note_contract|code_walkthrough_quality_gate_np"
clang-format --dry-run --Werror src/runtime_credential_resolver_endpoint_handle_allowlist_approval_receipts.cpp
git diff --check
```

结果：文档/讲解 focused CTest 6/6 通过，总耗时 0.94 秒；clang-format 通过；`git diff --check` 通过；中文讲解 6365 字符、64 行。

意义：本版不是只让代码编译通过，也把 receipt consolidation note、第四册索引、中文讲解和空白质量门补齐。范围结论是：fixture 未改，public command surface 未改，endpoint/raw-endpoint/network/write/WAL/credential/execution 边界仍关闭。
