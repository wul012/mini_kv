# mini-kv v1639 命令归档说明

本版继续执行 `治理计划/v1637-production-excellence-completion-brief.md` 的 receipt consolidation 方向，但只做一个可闭环的小切片：把 credential-handle approval receipt 里的 closed-boundary boolean flag block 从手写字符串迁移为 `OrderedJsonField` + `json_bool` 构造。冻结 fixture 不改，endpoint-handle 不在本版迁移，运行时边界仍然是只读、no execution、no write、no network。

截图使用 Playwright MCP 从命令摘要页面直接生成 PNG，未使用文本渲染回退。

## 01-focused-credential-boundary-tests.png

对应命令：

```powershell
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
ctest --test-dir cmake-build-v1639 --output-on-failure -R "runtime_receipt_json_builder_tests|smokejson_command_receipt_tests|release_verification_manifest_tests|runtime_smoke_evidence_tests|credential_handle_approval_contract_receipt_tests"
```

结果：5/5 通过。

意义：这条 focused lane 把本版最容易出错的面保护住。`runtime_receipt_json_builder_tests` 证明通用 JSON builder 行为稳定；`credential_handle_approval_contract_receipt_tests` 证明 credential-handle approval receipt 仍然和冻结 fixture 保持契约一致；smoke/release/runtime evidence 测试则确认迁移没有破坏对外证据面。

## 02-full-build-and-ctest.png

对应命令：

```powershell
cmake --build cmake-build-v1639
ctest --test-dir cmake-build-v1639 --output-on-failure
```

结果：344/344 通过，`100% tests passed, 0 tests failed out of 344`，总耗时 110.73 秒。

意义：本版第一次全量 CTest 曾因只构建了 focused targets 而缺少部分测试可执行文件；这属于构建顺序问题，不是代码失败。完整构建后重新跑全量 CTest 得到干净摘要，本截图记录的是最终有效证据。

## 03-format-diff-walkthrough.png

对应检查：

```powershell
clang-format --dry-run --Werror src/runtime_credential_resolver_credential_handle_approval_receipts.cpp
git diff --check
```

结果：`clang-format` 通过，`git diff --check` 通过；新增中文代码讲解 `1007-version-1639-credential-handle-boundary-flags-builder.md` 共 6814 字符、71 行。

意义：这张图证明本版没有靠测试绿色就匆忙收尾，而是补齐了格式、空白、讲解密度和范围审查。范围结论是：冻结 fixture 未改，endpoint-handle 未迁移，核心源码变化限于 credential-handle approval closed-boundary boolean flags。
