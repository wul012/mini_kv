# v1653 命令归档说明

本目录记录 v1653“剩余 receipt owner 迁移前全量基线”的最终命令证据。图片在代码、测试、consolidation note 和中文讲解稳定后生成；本轮没有可复用的 Playwright MCP 页面会话，因此按仓库规则退回为等宽文本渲染 PNG。图片只呈现已执行命令的摘要，不代替 `证据/summary.txt`、CTest 退出码或测试源码中的机械断言。

## 01-baseline-contract.png

对应新增 CTest `runtime_receipt_remaining_owner_baseline_tests`。它显示十三份对象按三条 family 分组，并列出 11 份 raw exact、基础 resolver 单字段顺序规则和 manual dry-run 三个冻结元数据字段规则。这里的 named compatibility 不是通用 JSON 宽松比较：测试同时冻结两侧字节、字段数和摘要，规则各只能使用一次，应用后仍必须整对象逐字节等于当前 runtime surface。

## 02-focused-and-full-ctest.png

对应 all-target build、十九条 focused CTest 和完整 348 条 CTest。focused lane 同时覆盖 builder/census、新 baseline、十三个领域 receipt、SMOKEJSON、runtime-smoke evidence 和 verification manifest；全量继续覆盖 Store、WAL、snapshot、RESP/TCP、OSFS 与历史证据链。最终结果分别为 19/19 和 348/348，没有用旧测试二进制代替重新构建。

## 03-cli-boundaries.png

对应真实 `minikv_cli` 输入 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap`、`QUIT`。结构化检查确认十三个目标 marker 全部存在，且每份 receipt 都保持 `read_only=true`、`execution_allowed=false`、`managed_audit_store=false`、无写入、无恢复执行、无订单权威；`CHECKJSON LOAD` 只做命令分类，`wal.touches_wal=false`，最后正常返回 `BYE`。

## 04-census-and-scope.png

对应 `cmake/check_receipt_builder_census.cmake` 与版本范围审计。census 仍为 28 sources、27 formatter owners、18 builder-backed、9 pending、1 named waiver；v1653 没有把测试基线冒充 production migration。图片同时列出后续三批自然链，明确 fixture、public command、Store/WAL/snapshot/RESP/TCP/OSFS 和跨项目仓库均未修改。

验证结束后没有遗留服务端、浏览器或其他后台进程。`cmake-build-v1653` 只服务本版构建验证，将在版本完成后按 cleanup gate 删除；最终保留本目录四张 PNG、本文、`证据/summary.txt`、测试源码、正式文档和代码讲解。
