# v1597 命令归档说明

## 01-cmake-configure.png

命令：

```powershell
$cmake -S . -B cmake-build-v1601 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug -DCMAKE_MAKE_PROGRAM="$mingw\mingw32-make.exe"
```

结果：CMake configure/generate 通过，生成目录为 `cmake-build-v1601`。日志里仍有历史对象路径较长 warning，但退出码为 0，不影响本版构建和测试判断。

意义：v1597 作为 mini-kv owner receipt bundle 的计划接入和源证据冻结起点，先确认新增模块、CMake target、测试 target 可以被构建系统识别。本版只接入只读证据链，不打开 router、write、WAL 或 execution。
