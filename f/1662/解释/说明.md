# v1662 优雅第二轮归档说明

本目录记录 E2-M1 条件式 pin 审计的选择性证据。最终只保留三张 PNG、本说明和机器摘要；完整测试日志位于临时构建目录，提取最终摘要后随 `cmake-build-v1662/` 清理，不把大段重复输出留进历史归档。Round 2 外部 verdict 保持 PENDING。

## 01-pin-audit-ranking.png

展示统一前十排名得到 6 个文件名、4 个公开标识符，其中 9 个由公开 header 路径或标识符钉住，唯一 safe 项是只在 CMake 注册的私有测试路径。图中明确给出 `safe=1<5`，因此分叉为 close，而不是 E2-M2 rename。

## 02-audit-contract-flow.png

展示 baseline、稳定排序、TSV 数据、机械证据与分叉之间的关系。特别标出 TSV 只保存数据，`elegance_name_census_contract` 重算排名和判定；`.gitattributes` 同时固定 baseline/TSV 为 LF，防止跨平台文本漂移再次出现。

## 03-final-verification.png

展示全量构建、351/351 CTest、1514-file fixture digest 和真实 CLI 四层证据。CLI 只摘录边界字段，不归档 2.2MB SHARDJSON 原文；图中保留 `External verdict: PENDING` 与“不启动 E2-M2”的硬停止状态。

## 进程与临时文件

- 本版没有启动 `minikv_server` 或其他后台服务。
- Playwright MCP 页面在三张截图完成后关闭，`.playwright-mcp/` 删除。
- `cmake-build-v1662/` 在候选提交前删除，包含其中的临时 CTest 日志。
- 最终保留三张 PNG、本说明、summary、pin audit、中文讲解、测试/文档变更、commit/tag 与远端 CI 证据。
