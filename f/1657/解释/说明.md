# v1657 命令归档说明

本目录归档 Production Excellence 阶段二文档诚实性刷新的证据。图片基于直接运行的 archive inventory、CMake/CTest、OSFS/KV CLI 结果和已核实源码路径生成；Playwright MCP 只负责把这些结果排版成 PNG，不替代 `../证据/summary.txt`、`docs/project-docs-honesty-matrix.md` 或真实命令退出码。

## 01-document-reconciliation.png

对应 README、START_HERE、capability snapshot、changelog、archive index、治理简报与新 honesty matrix 的对账。README recent block 恢复为恰好十条 v1657-v1648，START_HERE 不再停在 v1628，changelog 补齐 v1642-v1657。新 CTest 会在关键指针、路径、数字或 brief 状态丢失时失败。

## 02-osfs-evidence-map.png

对应能力快照与 matrix 的六条 OSFS 实现链：MFD/UFD、持久用户/认证、descriptor offsets、direct + single-indirect、read-only FSCK、USERADD/PASSWD。每条都有源码 owner 和 `tests/osfs_tests.cpp` 中的具名测试，不以命令名或报告截图冒充实现；教学 hash、无自动 repair、无双/三级间接和无任意深目录等边界同时披露。

## 03-archive-growth.png

对应 `python scripts/archive_inventory.py` 与课程交付目录的单独只读测量。v1657 归档前，active `f/` 为 1597-1656、293 files、10,501,451 bytes；OSFS `f/1629-1636` 为 42 files、3,440,216 bytes；`课程设计交付/` 为 102 files、5,445,971 bytes。全部在预算内，warnings 为 none，没有移动历史证据。

## 04-build-tests-cli.png

对应明确 MinGW 生成器下的 all-target build、`ctest -N` 的 349 注册数、focused 6/6、首轮 full 349/349，以及真实 OSFS/KV smoke。图中也记录初始 NMake 选择失败和冷构建超过工具窗口的偏差；两者都在单构建树、无残留进程条件下修正，不被隐藏为“直接通过”。

截图与最终 inventory 生成后，会把 archive index 更新到包含 `f/1657/`，再跑最终 honesty/focused/full 门。清理时删除 `cmake-build-v1657` 与 Playwright 临时 YAML，保留本目录六个最终文件、正式文档、机械 CMake 门和 3378 字中文讲解。
