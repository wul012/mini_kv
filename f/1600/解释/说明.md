# v1600 命令归档说明

## 01-full-ctest.png

命令：

```powershell
$ctest --test-dir cmake-build-v1601 --output-on-failure
```

结果：全量 CTest 323/323 通过。

意义：v1600 是 clean closeout 前的全量回归门。它证明本批新增 mini-kv owner receipt bundle 没有破坏既有 WAL、snapshot、TCP、restore、runtime safety、command catalog 或历史 non-participation fixture。
