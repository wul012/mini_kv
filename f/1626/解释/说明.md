# mini-kv v1626 归档截图说明

本目录记录 v1626 runtime receipt JSON Slice 0 的关键验证结果。截图通过 Playwright MCP 从临时本地页面生成，临时 HTTP server 使用 `127.0.0.1:8626`；首次截图 PID 为 `22148`，质量门重截 PID 为 `26728` 和 `27360`，截图完成后均已停止。临时 HTML 页面仅用于渲染归档，不属于最终交付源码。

## 01-scope-and-foundation.png

对应本版范围说明：v1626 只新增内部 ordered JSON builder、fixture 子对象 fail-closed 提取器和 `runtime_receipt_json_builder_tests`。截图记录本版没有迁移任何 public formatter，没有修改 fixture/manifest/SMOKEJSON 输出，没有新增 router/write/WAL/network/execution 权限，也没有启动 Node、Java 或 mini-kv 服务。

截图中的两个冻结对象是本版的主要字节基线：

```text
no-network 17738 bytes fnv1a64:794a730788ce0e87
abort/rollback 19183 bytes fnv1a64:0e41aded16909c6d
```

## 02-focused-gates.png

对应定向构建和 focused CTest。`minikv_runtime_receipt_json_builder_tests` 构建通过，新增测试覆盖 ordered object/array assembly、字符串 escaping、raw object/raw array 不二次转义、false/0/空对象/空数组不省略、malformed fixture fail closed、fixture 子对象冻结和 candidate formatter parity state。

截图还记录相关证据门通过：`receipts_consolidation_note_contract`、`smokejson_command_receipt_tests`、`release_verification_manifest_tests`、`runtime_smoke_evidence_tests`，并补跑 `no_network_safety_fixture_contract_receipt_tests` 与 `abort_rollback_semantics_contract_receipt_tests`。

## 03-full-ctest.png

对应全量本地回归：

```text
ctest --test-dir cmake-build-v1626 --output-on-failure --timeout 240 --progress
342/342 tests passed
Total Test time (real) = 116.93 sec
```

这张图证明本版新增内部 builder 与测试后，Store、command、WAL、snapshot、TCP/RESP、SMOKEJSON、runtime smoke evidence、release manifest、credential resolver receipt 系列和历史 fixture 测试仍全部通过。

## 04-quality-and-cleanup-gates.png

对应最终质量门：

```text
clang-format --dry-run --Werror
git diff --check
python scripts/archive_inventory.py --budget-mib 8 --strict
```

结果：changed C++/test files 的 clang-format dry-run 通过；`git diff --check` 通过，仅有 Git 对 CRLF 转换的提示；archive inventory strict 通过，`f/1626` 低于 8 MiB 预算，无 warnings。具体字节数以最终收尾命令为准，避免截图文件和说明文件自身变化导致递归改数。

## 清理状态

截图用临时 HTTP server 已停止。最终提交前还会删除 `cmake-build-v1626` 和 `.tmp-v1626-evidence-html`。历史 `a/` 到 `f/` 归档根、`fixtures/release/*.json`、既有讲解和本版最终截图不移动、不重命名、不压缩替换。
