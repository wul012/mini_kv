# v1671 命令与证据截图说明

本目录只保留四张选择性截图和一份文本摘要，不保存构建目录、测试可执行文件、完整 700 行 CTest 输出或临时 smoke 日志。截图来自本地最终代码与文档状态，使用 Playwright MCP 对临时只读证据页逐块截图；临时 HTTP 服务和页面在归档完成后停止、删除。

## 01-build-topology.png

对应两组短路径 Ninja 实测。旧拓扑干净构建为 1033 步、350 次 executable link、151.44 秒；默认 v1671 为 707 步、16 次 executable link、130.15 秒。对 `src/atomic_file_writer.cpp` 做内容不变 touch 后，旧拓扑为 349 步/347 次链接/56.87 秒，v1671 为 15 步/13 次链接/21.36 秒。意义在于证明收益来自消除重复链接，而不是删除测试或只截取一次偶然快跑。

## 02-contract-compatibility.png

对应 `ctest -N` 名称顺序比较、`test_architecture_contract`、runner 红路、instrumented 冲突门和 legacy 代表集。默认与 legacy 均为 354 项且顺序差异为 0；342 个 linked case 稳定分到 8 个 runner；344 个无参 main 必须显式返回；缺参和未知 case 均退出 2；legacy 四个代表测试 4/4。意义在于证明本版只更改最终链接身份，没有合并 CTest 坐标或把多个 case 放入同一进程连续执行。

## 03-full-suite-and-size.png

对应最终默认全量 `ctest --output-on-failure --timeout 120 --progress` 与可执行文件盘点。全量 354/354 通过，CTest 实测 130.96 秒；全部测试 exe 从 345 个/3,983,351,060 字节降到 11 个/415,219,566 字节，体积下降 89.58%；109 个改动 C++ 文件通过 changed-files clang-format 门，产品源码、公开头和冻结 fixture 变化路径为 0。意义在于把构建收益、行为完整性和变更边界放在同一证据面上。

## 04-cli-boundaries.png

对应真实 `minikv_cli` 输入 `SMOKEJSON`、`CHECKJSON GET v1671:probe`、`QUIT`。程序退出 0，输出保留 `read_only=true`、`execution_allowed=false`、`order_authoritative=false`、`write_command=false`、`touches_wal=false`。本次校验脚本最初误期待不存在的 `mutates_store` 字段，随后按实际 schema 改查 `write_command`，未改产品输出。意义在于证明构建重构没有扩张 router、写入、WAL、恢复、网络、凭据或订单权限。

## 清理记录

- 截图完成后停止本轮启动的临时 HTTP 服务并关闭 Playwright 页面。
- 删除临时证据 HTML、构建/CTest/CLI 日志与 `b1671*` 验证目录。
- 保留本目录四张 PNG、本文和 `../证据/summary.txt`；历史 `a/` 到 `f/` 路径不移动、不改名。
