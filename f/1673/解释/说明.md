# v1673 测试注册清单化证据说明

本目录保留四张选择性截图、迁移前后两份有序 CTest 名称文本和一份摘要，不保存构建目录、完整 Ninja/CTest 日志或故障注入临时清单。截图由 Playwright MCP 从本地临时证据页生成；视觉检查后已关闭页面、停止 PID 8020 的临时 HTTP 服务并删除 HTML。历史 `a/` 到 `f/` 目录没有移动或改名。

## 01-manifest-architecture.png

展示数据与行为的分界。根 `CMakeLists.txt` 从 2,597 行降到 436 行，342 条普通测试由一个五列 LF manifest 保存，其中 8 条属于 `pre_smoke`、334 条属于 `main`。130 行 `MinikvTestSuite.cmake` 仍显式保存 standalone、script/smoke、properties、test parts 和 target include/definition；重复没有被搬进另一个巨型模块。

## 02-ctest-order-oracle.png

展示公开测试面前后不变。v1672、v1673 默认 8-shard 和 v1673 legacy 三次配置均产生 354 个相同名称、相同顺序的 LF 文本，字节级 SHA-256 都是 `f4ecf10465a185e4a3c04d5b0e0292ea47aba0a8d48e5992c7eb8d9159a3310e`。`证据/ctest-names-before.txt` 与 `ctest-names-after.txt` 可直接逐字节比较；运行中的架构合同还读取生成的 `CTestTestfile.cmake` 做同序检查。

## 03-parser-topologies.png

展示 fail-closed parser 与双拓扑。真实 CMake checker 分别拒绝 CRLF、未知 kind、重复 target 和缺失 source；C++ 架构门另覆盖未知 phase、坏列、重复 CTest 名和非法路径。默认模式配置为 342 case / 8 runner，预验收 full CTest 354/354；legacy focused 的 architecture、command parts、release manifest 与 no-network 四类代表测试 4/4。

## 04-scout-boundaries.png

展示一次门禁触发的实际清偿。`release_verification_manifest_tests.cpp` 在格式化后达到 1,090 个非空行，架构门拒绝新增热点；本版没有加入 baseline，而是拆成 820 行静态 manifest/build 检查和 280 行真实命令输出检查。产品 `src/include`、fixture、公开输出、CTest 名称/顺序以及 router/write/network/credential/WAL/restore/execution/order-authority 均未改变。

## 清理记录

- 已关闭 Playwright 页面并停止临时 HTTP 服务 PID 8020。
- 已删除 `v1673-evidence.html`；故障注入清单仅位于待删除的构建目录中。
- 本版提交前会删除 `b1673`、`b1673-legacy`、`b1673-oracle`、`b1673-release`，保留四张 PNG、两份有序名称 oracle、本文与 `证据/summary.txt`。
- 没有启动需要长期保留的进程；没有触碰用户已有的 `治理计划/readme-exhibition-brief.md` 改动。
