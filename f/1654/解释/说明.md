# v1654 命令归档说明

本目录归档 base resolver 三 formatter 的 ordered-builder 迁移证据。图片在代码、测试、consolidation note 和 3098 个中文字符的代码讲解稳定后生成。本轮没有可用的 Playwright MCP 页面会话承载终端，因此按规则使用等宽文本渲染 PNG；原始机械事实仍以 v1653 oracle、CTest 退出码、census 脚本和 `证据/summary.txt` 为准。

## 01-byte-parity.png

对应 `runtime_receipt_remaining_owner_baseline_tests`。三份迁移对象仍是 7944/10291/11201 字节与 104/118/133 个字段；基础 decision 只保留 v1653 已命名的单字段顺序规则，disabled precheck 和 test-only shell 仍为 raw exact equality。测试、fixture、摘要常量和兼容规则均未因迁移修改。

## 02-builder-census.png

对应 `receipt_builder_census_contract`。两个 source owner 直接使用 builder，所以三份 formatter 的迁移让 owner 口径从 18/9/1 变为 20/7/1，而不是按函数数误报 21。图片同时记录旧 common boundary comma fragment 已删除，37 个共同关闭字段由无参数 ordered appender 按历史顺序追加。

## 03-build-and-ctest.png

对应 all-target build、最终 focused 8/8 和 full CTest 348/348。focused 同时覆盖完整对象 oracle、三份领域 receipt、SMOKEJSON、runtime-smoke、verification manifest 与 census；全量继续覆盖 Store、WAL、snapshot、RESP/TCP、OSFS 和其他 receipt family。

## 04-cli-and-size.png

对应真实 `minikv_cli` 的 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap`、`QUIT`，以及源文件 size census。三份对象全部保持 read-only、no credential、no store/router authority、no write、no execution、no auto-start、no order authority；检查 LOAD 仍不触 WAL。三个 touched 文件为 583/361/142 行，均低于 800 行停止线。

验证结束后没有遗留 mini-kv、浏览器或服务端进程。`cmake-build-v1654` 只用于本版构建验证，将在版本提交前按 cleanup gate 删除；最终保留四张 PNG、本文、`证据/summary.txt`、生产重构、正式文档和代码讲解。
