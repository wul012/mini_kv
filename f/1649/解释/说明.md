# v1649 命令归档截图说明

v1649 在 v1648 exact-byte 基线下，把 disabled runtime shell 与 candidate gate 两份链首 formatter 迁移到 ordered builder。新增 family 专属字段 header，只共享真正同序的 readiness 与 non-participation tail；candidate 三个决策字段仍在两个 helper 调用之间显式保留。

## 01-exact-parity-migration.png

记录两份迁移对象的冻结字节数、字段数、摘要和 whole-object parity。disabled receipt 保持 12523 bytes / 110 fields，candidate gate 保持 14936 bytes / 114 fields，两份 runtime 输出均与 frozen fixture 逐字节相等，fixture 未改，也没有 apostrophe/whitespace waiver。

## 02-structure-census.png

记录结构与 census 变化：两个领域源文件分别 385→358、423→403 行，共同字段集中到 91 行专属 header；builder census 从 9/18/1 收紧到 11/16/1。candidate-only 三字段保留在调用点，没有使用布尔参数式万能 helper。

## 03-focused-full-ctest.png

记录最终 incremental build 无工作、focused lane 9/9 通过，以及文档落地后的 full CTest 346/346 通过，耗时 139.47 秒。意义是 exact parity、领域命令、SMOKEJSON、runtime evidence、release manifest、文档合同、census 和全工程回归同时为绿。

## 04-real-cli-boundaries.png

记录真实 `minikv_cli` 执行 `SMOKEJSON`、`CHECKJSON LOAD data/prod.snap`、`QUIT` 的精简断言。两份 marker 均出现，read-only/deny-execution/runtime-shell-disabled/no-network/no-write 字段仍存在；LOAD 只分类，`touches_wal=false`，最后返回 `BYE`。

## 清理说明

截图使用 Playwright MCP 从只含最终机械断言的内存页面生成。最终保留 4 张 PNG、本文和 `f/1649/证据/summary.txt`；提交前删除 `cmake-build-v1649`、`.v1649-final-*.log` 和 Playwright 临时输出，关闭浏览器，不保留 mini-kv 服务进程，也不移动历史归档或 frozen fixture。
