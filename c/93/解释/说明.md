# mini-kv v93 命令归档说明

本轮先按 AGENTS.md 要求只读查看了 Node 计划：

- `D:\nodeproj\orderops-node\docs\plans\README.md`
- `D:\nodeproj\orderops-node\docs\plans\v223-post-external-adapter-readiness-roadmap.md`

计划里的 mini-kv v91 质量门已完成；本轮是用户明确要求继续合理拆分后的 v93。范围只做 C++ 侧质量重构：把 live-read session、SMOKEJSON failure taxonomy、operator window、binary provenance、retention provenance 等运行时证据收据 formatter 从 `src/command.cpp` 拆到 `include/minikv/runtime_evidence_receipts.hpp` 和 `src/runtime_evidence_receipts.cpp`。本版不新增执行入口，不扩展 `INFOJSON` / `SMOKEJSON` 字段契约，不触碰 write/admin/WAL/snapshot/restore 行为，也不让 mini-kv 成为 Node/Java 的执行或存储权威。

## 截图

- `01-cmake-configure.png`：`cmake -G "MinGW Makefiles" -S . -B cmake-build-v93 -DCMAKE_BUILD_TYPE=Debug` 成功生成 v93 构建目录。
- `02-cmake-build.png`：`cmake --build cmake-build-v93 --parallel 2` 构建全部目标通过，包含新增 `src/runtime_evidence_receipts.cpp`。
- `03-ctest.png`：`ctest --test-dir cmake-build-v93 --output-on-failure` 全量 31/31 测试通过。
- `04-runtime-smoke.png`：真实启动 `minikv_server`，用 `minikv_client` 执行 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`。输出确认 v93 read-only/no-execution 边界、runtime evidence receipt split 后的 digest、sandbox non-participation receipt 和 `GET (nil)` 均有效。

## 关键 Digest

- live_read_session read list：`fnv1a64:5bef33f2fbe65cc5`
- failure taxonomy：`fnv1a64:f92fcba55feb26a2`
- binary_provenance：`fnv1a64:077c4510bc08fe58`
- retention_provenance_check：`fnv1a64:123b13c1e2e31ba2`
- retention_provenance_replay_marker：`fnv1a64:b0daa0cec61f2931`
- restore_boundary receipt：`fnv1a64:72e12ce65542a472`
- non_authoritative_storage receipt：`fnv1a64:790f02e1ed7726ce`
- command_dispatch_quality receipt：`fnv1a64:1717cb8feca8551e`
- adapter_shell_non_storage_guard receipt：`fnv1a64:5062dc664522b60a`
- external_adapter_non_participation receipt：`fnv1a64:a29e619cc23f2105`
- sandbox_adapter_non_participation receipt：`fnv1a64:a09d9fc409ab38ec`

## 进程

真实 TCP smoke 启动的 `minikv_server` 已停止。归档结束时未保留 mini-kv 后台进程。
