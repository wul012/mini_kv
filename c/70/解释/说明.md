# v70 运行调试归档说明

本轮读取的最新跨项目计划是：

```text
D:\nodeproj\orderops-node\docs\plans\v169-post-production-environment-preflight-roadmap.md
```

计划要求 mini-kv v70 补 `restore drill evidence fixture`，记录 restore drill target、`CHECKJSON LOAD/COMPACT/SETNXEX` 风险解释、digest comparison placeholder，并明确不执行 restore。本轮实现范围与该计划一致。

## 01-cmake-configure.png

命令：

```powershell
cmake -S . -B cmake-build-v70 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug -DCMAKE_MAKE_PROGRAM="D:/CLION/CLion 2025.1.1/bin/mingw/bin/mingw32-make.exe"
```

结果：配置和生成成功，构建目录写入 `cmake-build-v70`。

## 02-cmake-build.png

命令：

```powershell
cmake --build cmake-build-v70 --parallel 2
```

结果：全部目标构建成功，新增 `minikv_restore_drill_evidence_tests` 也完成编译。

## 03-ctest.png

命令：

```powershell
ctest --test-dir cmake-build-v70 --output-on-failure
```

结果：`25/25` 测试通过，新增 `restore_drill_evidence_tests` 通过。

## 04-real-smoke.png

命令要点：

```powershell
minikv_server 6410 127.0.0.1
minikv_client 127.0.0.1 6410 5000 --connect-retries 10 --retry-delay-ms 100
```

客户端发送：

```text
INFOJSON
CHECKJSON LOAD data/restore-drill.snap
CHECKJSON COMPACT
CHECKJSON SETNXEX restore:drill-token 30 value
STORAGEJSON
HEALTH
GET restore:drill-token
QUIT
```

结果：`INFOJSON` 返回 `version=0.70.0`；三个 `CHECKJSON` 只返回只读风险契约；`STORAGEJSON` 显示 `live_keys=0` 且 `order_authoritative=false`；`GET restore:drill-token` 返回 `(nil)`，证明没有执行真实写入或恢复。验证结束后已停止本轮启动的服务端进程。

## 清理说明

本轮临时构建目录和临时日志目录会在提交前清理；`c/70/图片/` 和 `c/70/解释/说明.md` 是正式归档，保留。
