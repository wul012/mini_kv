# v86 运行调试归档说明

## 本版范围

v86 按 `D:\nodeproj\orderops-node\docs\plans\v213-post-restore-drill-plan-roadmap.md` 推进 mini-kv 侧，只补充 `managed_audit_adapter_restore_boundary_receipt`。它给 Node v215 的 managed audit dry-run adapter candidate 一个只读边界回执：mini-kv 仍然不执行 `LOAD`、`COMPACT`、`SETNXEX`、`RESTORE`，不写 managed audit，不承担订单权威状态。

## 截图说明

1. `01-cmake-configure.png`
   - 命令：`cmake -S . -B cmake-build-v86 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
   - 结果：配置成功，生成 v86 构建目录。

2. `02-cmake-build.png`
   - 命令：`cmake --build cmake-build-v86 --parallel 2`
   - 结果：库、CLI、server、client、测试目标全部构建成功。

3. `03-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v86 --output-on-failure`
   - 结果：31/31 测试通过，覆盖命令输出、readonly fixture、release manifest、runtime smoke evidence 和恢复边界样例。

4. `04-real-smoke-client.png`
   - 命令：启动 `minikv_server 6424 127.0.0.1` 后，用 `minikv_client` 发送 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`。
   - 结果：真实 TCP 路径读到 `managed_audit_adapter_restore_boundary_receipt`，并且 `GET restore:real-read-token` 返回 `(nil)`。

5. `05-real-smoke-summary.png`
   - 命令：对真实 smoke 输出做关键字段核对。
   - 结果：确认 `version=0.86.0`、`consumed_release_version=v85`、`consumed_marker_digest=fnv1a64:1ea4570c967cfdb1`、`receipt_digest=fnv1a64:f39d8e3ef98654ea`、`adapter_write_allowed=false`、`restore_execution_allowed=false`、`load_restore_compact_executed=false`、`managed_audit_write_executed=false`、`HEALTH OK`。

## 进程与清理

真实 smoke 期间启动过一个本地 `minikv_server`，验证结束后已停止。构建目录和临时日志只在提交前保留用于截图生成，最终清理时删除。