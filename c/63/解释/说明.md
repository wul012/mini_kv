# 第六十三版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v159-post-release-evidence-review-roadmap.md
D:\nodeproj\orderops-node\docs\plans\v161-post-controlled-idempotency-drill-roadmap.md
```

`v159` 计划已经收口，并明确后续由 `v161-post-controlled-idempotency-drill-roadmap.md` 接力。最新有效计划要求 mini-kv v63 做 release verification manifest，固化 CMake configure/build、CTest、关键命令 smoke、fixture/version 清单，不新增 runtime 命令。

该计划合理。本轮只执行 mini-kv / C++ 侧，不修改 Node 或 Java。

## 本版目标

新增发布验证清单：

```text
fixtures/release/verification-manifest.json
tests/release_verification_manifest_tests.cpp
```

清单覆盖：

```text
CMake configure
CMake build
CTest
targeted tests
read-only command smoke
fixture inventory
version manifest
no runtime command added boundary
```

## 代码改动

```text
CMakeLists.txt -> 0.63.0，新增 minikv_release_verification_manifest_tests
fixtures/release/verification-manifest.json
fixtures/readonly/infojson-empty-inline.json
tests/release_verification_manifest_tests.cpp
README.md
代码讲解记录_生产雏形阶段/README.md
代码讲解记录_生产雏形阶段/119-version-63-release-verification-manifest.md
c/63/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v63 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

CMake build：

```text
& $cmake --build cmake-build-v63 --parallel 2
```

CTest：

```text
& $ctest --test-dir cmake-build-v63 --output-on-failure
100% tests passed, 0 tests failed out of 18
```

release manifest 测试单跑：

```text
.\cmake-build-v63\minikv_release_verification_manifest_tests.exe
exit_code=0
```

readonly fixture 测试单跑：

```text
.\cmake-build-v63\minikv_readonly_fixture_tests.exe
exit_code=0
```

真实 server 只读 release smoke：

```text
.\cmake-build-v63\minikv_server.exe 6403 127.0.0.1
INFOJSON
COMMANDSJSON
CHECKJSON SETNXEX release:token 30 value
STORAGEJSON
HEALTH
GET release:token
QUIT
```

结果：

```text
INFOJSON -> version=0.63.0
COMMANDSJSON -> command catalog 可读
CHECKJSON SETNXEX -> read_only=true，execution_allowed=false
STORAGEJSON -> order_authoritative=false
HEALTH -> OK live_keys=0
GET release:token -> (nil)
server 已停止
```

## 图片记录

本版关键命令截图保存到：

```text
c/63/图片/01-cmake-configure.png
c/63/图片/02-cmake-build.png
c/63/图片/03-ctest.png
c/63/图片/04-release-manifest-tests.png
c/63/图片/05-readonly-fixture-tests.png
c/63/图片/06-real-server-release-smoke.png
c/63/图片/07-version-docs-check.png
c/63/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不新增 runtime 命令
不修改 Node
不修改 Java
不启动 Node 或 Java
不打开 UPSTREAM_ACTIONS_ENABLED=true
不执行 mini-kv 写命令 smoke
不把 mini-kv 接进 Java 订单交易链路
```

`CHECKJSON SETNXEX ...` 是只读契约检查，不执行 `SETNXEX`。真实 smoke 用 `GET release:token -> (nil)` 证明没有写入 token。

## 清理记录

结束前清理：

```text
tmp-v63-logs
cmake-build-v63
本轮真实 smoke server 已停止
```
