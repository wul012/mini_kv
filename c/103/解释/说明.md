# mini-kv v103 运行归档说明

本目录记录 v103 current version path hint cleanup 的关键命令截图。截图保存在同级 `../图片/`。

## 01-cmake-configure.png

命令：

```powershell
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\ninja\win\x64;D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
& $cmake -S . -B cmake-build-v103 -G Ninja
```

结果：CMake configure 成功，生成 `cmake-build-v103`。

意义：证明新增 `current_version_path_hint` 后，CMake 生成头和工程配置仍正常。

## 02-cmake-build.png

命令：

```powershell
& $cmake --build cmake-build-v103 --clean-first
```

结果：94 个构建步骤成功，生成 `minikv_cli.exe`、`minikv_server.exe`、`minikv_client.exe` 和全部测试目标。

意义：证明 `managed_audit_receipts.cpp`、`runtime_evidence_receipts.cpp`、`command_response_formatters.cpp` 改用共享常量后仍可完整编译。

## 03-ctest.png

命令：

```powershell
$ctest = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir cmake-build-v103 --output-on-failure
```

结果：`35/35` 测试通过，`0` failed。

意义：测试继续从外部契约角度断言 `"current_artifact_path_hint":"c/102/"` 等字段，证明本版只是生产代码去重，没有改变 Node v239 所需的 v102 runtime evidence 输出。

## 04-real-tcp-smoke.png

命令：

```powershell
$server = Start-Process -FilePath .\cmake-build-v103\minikv_server.exe -ArgumentList @('6503','127.0.0.1') -PassThru -WindowStyle Hidden
type c\103\.tmp\smoke-input.txt | .\cmake-build-v103\minikv_client.exe 127.0.0.1 6503 5000 --connect-retries 10 --retry-delay-ms 100
Stop-Process -Id $server.Id
```

输入命令包括：

```text
SMOKEJSON
INFOJSON
STORAGEJSON
HEALTH
GET restore:real-read-token
QUIT
```

结果：真实 TCP 客户端连接 `127.0.0.1:6503` 成功；`SMOKEJSON` / `INFOJSON` 仍输出 `version="0.102.0"` 与 `current_artifact_path_hint="c/102/"`，包含 `operator_window_no_start_no_write_receipt`，并保持 read-only / no-execution / no-auto-start / no-credential / no-schema / no-restore / no-order-authority 边界；`GET restore:real-read-token` 返回 `(nil)`；服务端验证后已停止。

意义：证明 v103 常量抽取没有引入运行时写入，也没有改变 Node 当前计划中要消费的 v102 operator-window evidence contract。

## 后台进程与清理

真实 TCP smoke 使用的 `minikv_server.exe` 已在验证结束后停止。`cmake-build-v103` 与 `c/103/.tmp/` 只用于本轮构建、测试和截图生成，最终清理时删除；保留本说明文档和 `../图片/` 下的 PNG 作为版本归档。
