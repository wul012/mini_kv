# v74 运行调试归档说明

本轮按 `D:\nodeproj\orderops-node\docs\plans\v179-post-pre-approval-roadmap.md` 推进 mini-kv v74：只补 restore approval boundary fixture，不执行真实 `LOAD`、`COMPACT`、`SETNXEX` 或 restore，不启动 Node/Java，不读取生产密钥。

## 1. 01-cmake-configure.png

命令：

```powershell
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
& 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe' -S . -B cmake-build-v74 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

结果：CMake 使用 CLion 捆绑 MinGW 工具链完成配置，生成 `cmake-build-v74`。这一步证明 v74 的 CMake 项目版本、生成头文件和新增测试目标能正常进入构建系统。

## 2. 02-cmake-build.png

命令：

```powershell
& 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe' --build cmake-build-v74 --parallel 2
```

结果：全部目标构建通过，新增 `minikv_restore_approval_boundary_tests` 成功编译链接。v74 没有新增 runtime 命令，只新增 fixture 保护测试和版本证据。

## 3. 03-ctest.png

命令：

```powershell
& 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe' --test-dir cmake-build-v74 --output-on-failure
```

结果：`29/29` 个 CTest 全部通过。新增 `restore_approval_boundary_tests` 通过，历史 release evidence、WAL、Snapshot、TCP、RESP、client history、line editor 等测试也保持通过。

## 4. 04-real-smoke.png

服务端：

```powershell
.\cmake-build-v74\minikv_server.exe 6414 127.0.0.1
```

客户端：

```powershell
.\cmake-build-v74\minikv_client.exe 127.0.0.1 6414 5000 --connect-retries 10 --retry-delay-ms 100
```

输入命令：

```text
INFOJSON
CHECKJSON LOAD data/approval-boundary-restore.snap
CHECKJSON COMPACT
CHECKJSON SETNXEX restore:approval-token 30 value
STORAGEJSON
HEALTH
GET restore:approval-token
QUIT
```

结果：`INFOJSON` 和 `STORAGEJSON` 显示版本 `0.74.0` 且 `order_authoritative=false`；`CHECKJSON` 只解释 `LOAD`、`COMPACT`、`SETNXEX` 风险；`GET restore:approval-token` 返回 `(nil)`，证明没有执行真实 token 写入或恢复动作。验证结束后已停止本轮启动的服务端进程。

## 清理记录

本轮用于生成归档截图的临时日志后续清理，不作为长期归档保留。最终保留本目录下 PNG 截图和本说明文档。
