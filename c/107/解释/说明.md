# v107 归档说明

本版新增 `manual_sandbox_dry_run_command_non_participation_receipt`，用于 Node v244 只读核对 Node v241 disabled dry-run command package 对 mini-kv 的边界：不自动启动、不写 storage、不读 credential value、不执行 LOAD/COMPACT/RESTORE/SETNXEX、不成为 managed audit storage backend。

1. `01-cmake-configure.png`
   - 命令：`cmake -S . -B cmake-build-v107 -G Ninja`
   - 结果：配置成功
   - 意义：确认新增 receipt formatter、测试目标和 fixture 变更可被当前 CMake/Ninja/MinGW 工具链识别

2. `02-cmake-build.png`
   - 命令：`cmake --build cmake-build-v107`
   - 结果：全部目标构建成功
   - 意义：确认 `runtime_evidence_receipts` 新接口、`SMOKEJSON` 组合逻辑和新增测试目标链接正常

3. `03-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v107 --output-on-failure`
   - 结果：`36/36` 通过
   - 意义：确认新增 v107 receipt、既有 runtime evidence、release manifest、command、WAL、TCP、restore/fixture 等测试全部保持一致

4. `04-runtime-smoke.png`
   - 命令：启动 `minikv_server.exe 6507 127.0.0.1`，再用 `minikv_client.exe` 执行 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`
   - 结果：返回中包含 `manual_sandbox_dry_run_command_non_participation_receipt`、`"current_artifact_path_hint":"c/107/"`、`"source_package_command_count":6`、`"source_package_disabled_by_default":true`、`"mini_kv_auto_start_allowed":false`、`"storage_write_allowed":false`，且 restore token 仍为 `(nil)`
   - 意义：证明真实 TCP 路径能读取 v107 只读回执，同时没有触发写入、restore、LOAD/COMPACT/SETNXEX 或自动启动语义

验证结束后，临时构建目录和临时日志目录会清理，只保留正式归档截图和本说明文件。
