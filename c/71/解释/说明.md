# v71 运行调试归档说明

本轮先只读读取 Node 最新计划：`D:\nodeproj\orderops-node\docs\plans\v173-post-release-window-readiness-roadmap.md`。计划要求 mini-kv v71 做 restore handoff checklist fixture，并明确不执行 `LOAD`、`COMPACT`、`SETNXEX` 或 restore；本轮实现范围与计划一致。

## 01-cmake-configure.png

命令：

```powershell
D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe -S . -B cmake-build-v71 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug -DCMAKE_MAKE_PROGRAM="D:/CLION/CLion 2025.1.1/bin/mingw/bin/mingw32-make.exe"
```

结果：配置成功，生成 `cmake-build-v71`，项目版本为 `0.71.0`。

## 02-cmake-build.png

命令：

```powershell
cmake --build cmake-build-v71 --parallel 2
```

结果：全部目标构建成功，新增 `minikv_restore_handoff_checklist_tests` 也完成编译链接。

## 03-ctest.png

命令：

```powershell
ctest --test-dir cmake-build-v71 --output-on-failure
```

结果：`26/26` 通过。新增 `restore_handoff_checklist_tests` 通过，release manifest、历史 release evidence、WAL、Snapshot、TCP/RESP、client history 等既有测试也通过。

## 04-real-smoke.png

命令：启动真实 TCP server：

```powershell
cmake-build-v71\minikv_server.exe 6411 127.0.0.1
```

然后用 bundled TCP client 发送：

```text
INFOJSON
CHECKJSON LOAD data/handoff-restore.snap
CHECKJSON COMPACT
CHECKJSON SETNXEX restore:handoff-token 30 value
STORAGEJSON
HEALTH
GET restore:handoff-token
QUIT
```

结果：`INFOJSON` 与 `STORAGEJSON` 返回 `0.71.0`；三个 `CHECKJSON` 只返回只读风险合同；`GET restore:handoff-token` 返回 `(nil)`，证明没有执行真实 `SETNXEX` 写入。验证结束后服务端已停止。

## 清理记录

本轮临时日志目录为 `tmp-v71-logs/`，构建目录为 `cmake-build-v71/`。归档 PNG 已写入 `c/71/图片/`；最终提交前会删除临时日志和构建目录，只保留 `c/71/` 作为本版证据。