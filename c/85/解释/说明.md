# v85 运行调试归档说明

## 本版范围

v85 按 `D:\nodeproj\orderops-node\docs\plans\v211-post-managed-audit-packet-roadmap.md` 推进 mini-kv 侧，只补充 `retention_provenance_replay_marker`。它说明 Node v211 已消费过 mini-kv v84 的 retention provenance evidence，并给 Node v213 的 managed audit packet restore drill plan 一个只读回执。

本版不执行 `LOAD`、`COMPACT`、`SETNXEX`、`RESTORE`，不写 managed audit 状态，不把 mini-kv 放进 Java 订单权威链路。

## 截图说明

1. `01-cmake-configure.png`
   - 命令：`cmake -S . -B cmake-build-v85 -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Debug`
   - 结果：配置成功，生成 v85 构建目录。

2. `02-cmake-build.png`
   - 命令：`cmake --build cmake-build-v85 --parallel 2`
   - 结果：库、CLI、server、client、31 个测试目标全部完成构建。

3. `03-ctest.png`
   - 命令：`ctest --test-dir cmake-build-v85 --output-on-failure`
   - 结果：31/31 测试通过，覆盖命令输出、readonly fixture、release manifest、runtime smoke evidence、恢复边界等。

4. `04-real-smoke-client.png`
   - 命令：启动 `minikv_server 6423 127.0.0.1` 后，用 `minikv_client` 发送 `SMOKEJSON`、`INFOJSON`、`STORAGEJSON`、`HEALTH`、`GET restore:real-read-token`、`QUIT`。
   - 结果：真实 TCP 路径读到 `retention_provenance_replay_marker`，并且 `GET restore:real-read-token` 返回 `(nil)`。

5. `05-real-smoke-summary.png`
   - 命令：对真实 smoke 输出做关键字段核对。
   - 结果：确认 `version=0.85.0`、`consumed_release_version=v84`、`consumed_artifact_path_hint=c/84/`、`consumed_check_digest=fnv1a64:357cc7e9eec3f223`、`marker_digest=fnv1a64:1ea4570c967cfdb1`、`replay_executed=false`、`managed_audit_write_executed=false`、`HEALTH OK`。

## 进程与清理

真实 smoke 期间启动过一个本地 `minikv_server`，验证结束后已停止。构建目录和临时日志只在提交前保留用于截图生成，最终清理时删除。