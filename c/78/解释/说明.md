# v78 运行调试归档说明

本轮依据 `D:\nodeproj\orderops-node\docs\plans\v194-post-real-read-archive-roadmap.md` 推进 mini-kv v78。目标是在 v77 `SMOKEJSON.failure_taxonomy` 上补充稳定 `taxonomy_digest` 和 verification sample，让 Node v196 导入人工真实只读窗口结果前能判断结果是否来自预期 mini-kv 证据版本。本轮不执行 `LOAD`、`COMPACT`、`SETNXEX` 或 restore，不启动 Node/Java，也不让 mini-kv 成为 Java 订单权威。

## 01-cmake-configure.png

命令：

```powershell
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
& $cmake -S . -B cmake-build-v78 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

结果：配置成功，CMake 生成 `cmake-build-v78`。意义：确认版本号 `0.78.0`、manifest、fixture 和测试目标能被本机 CLion MinGW 工具链识别。

## 02-cmake-build.png

命令：

```powershell
& $cmake --build cmake-build-v78 --parallel 2
```

结果：构建成功，所有程序和测试目标生成完成。意义：确认 `SMOKEJSON` taxonomy digest、verification sample、fixture、README 和测试断言能一起编译，不破坏既有 TCP/RESP/WAL/Snapshot 目标。

## 03-ctest.png

命令：

```powershell
$ctest = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir cmake-build-v78 --output-on-failure
```

结果：`31/31` 通过。意义：`command_tests`、`release_verification_manifest_tests`、`runtime_smoke_evidence_tests` 已覆盖 v78 digest/sample 字段，老的 release evidence、WAL、Snapshot、TCP 和 RESP 回归也保持通过。

## 04-real-smoke.png

服务端：

```powershell
.\cmake-build-v78\minikv_server.exe 6416 127.0.0.1
```

客户端：

```powershell
.\cmake-build-v78\minikv_client.exe 127.0.0.1 6416 5000 --connect-retries 10 --retry-delay-ms 100
```

输入命令：

```text
SMOKEJSON
INFOJSON
STORAGEJSON
HEALTH
GET restore:real-read-token
QUIT
```

结果：`SMOKEJSON` 返回 `version:"0.78.0"`、`read_only:true`、`execution_allowed:false`、`restore_execution_allowed:false`、`order_authoritative:false`，并包含 `failure_taxonomy.consumer:"Node v196 imported window result packet"` 和 `taxonomy_digest:"fnv1a64:f92fcba55feb26a2"`。`GET restore:real-read-token` 返回 `(nil)`，说明本轮真实 smoke 没有执行 token 写入。

清理：真实 smoke 结束后已停止本轮启动的 `minikv_server.exe`。
