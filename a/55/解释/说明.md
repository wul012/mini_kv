# 第五十五版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v95-production-gap-roadmap.md
D:\nodeproj\orderops-node\docs\plans\v92-production-ops-roadmap.md
```

同时只读核对了 Node v98：

```text
tag: v98
commit: 3f7ae18 v98 add access control readiness profile
archive: D:\nodeproj\orderops-node\a\98
code notes: D:\nodeproj\orderops-node\代码讲解记录\102-access-control-readiness-profile-v98.md
```

最新有效计划 `v95-production-gap-roadmap.md` 显示 Node v96-v98 已收口，下一步是 Java v46 + mini-kv v55。mini-kv v55 的目标是新增 WAL/snapshot restart recovery sample，输出前后 key count、digest、recovered=true/false，不改核心协议、不扩大写命令范围。

该计划合理，本轮只执行 mini-kv / C++ 侧，不修改 Node 或 Java。

## 本版目标

新增稳定 fixture：

```text
fixtures/recovery/restart-recovery-evidence.json
```

它证明的流程：

```text
snapshot baseline 保存成功
WAL delta 重放成功
恢复前后 key_count 一致
恢复前后 digest 一致
recovered=true
digests_match=true
```

## 代码改动

```text
CMakeLists.txt -> 0.55.0
include/minikv/recovery_evidence.hpp
src/recovery_evidence.cpp
tests/recovery_evidence_tests.cpp
fixtures/recovery/restart-recovery-evidence.json
fixtures/recovery/README.md
README.md
代码讲解记录/README.md
代码讲解记录/111-restart-recovery-evidence-v55.md
a/55/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v55 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
Build files have been written to: D:/C/mini-kv/cmake-build-v55
```

CMake build：

```text
& $cmake --build cmake-build-v55 --parallel 2
[100%] Built target minikv_recovery_evidence_tests
```

CTest：

```text
& $ctest --test-dir cmake-build-v55 --output-on-failure
100% tests passed, 0 tests failed out of 14
```

新测试单跑：

```text
.\cmake-build-v55\minikv_recovery_evidence_tests.exe
exit_code=0
```

真实 fixture smoke：

```text
snapshot_saved_keys=1
wal_replay_applied_records=1
recovered=true
digests_match=true
fixture_exact_match=true
```

## 图片记录

本版关键命令截图保存到：

```text
a/55/图片/01-cmake-configure.png
a/55/图片/02-cmake-build.png
a/55/图片/03-ctest.png
a/55/图片/04-recovery-evidence-test.png
a/55/图片/05-recovery-fixture-smoke.png
a/55/图片/06-version-docs-check.png
a/55/图片/07-code-explanation-check.png
a/55/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不新增 server 命令
不新增写命令
不改变 WAL / snapshot 核心协议
不让 mini-kv 承担订单权威数据
```

## 清理记录

结束前已清理：

```text
tmp-v55-logs
cmake-build-v55
mini-kv server/client/test 进程
```
