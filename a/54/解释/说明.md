# 第五十四版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v95-production-gap-roadmap.md
D:\nodeproj\orderops-node\docs\plans\v92-production-ops-roadmap.md
```

最新有效指导是 `v95-production-gap-roadmap.md`。它要求 mini-kv v54 新增只读 storage evidence 输出，覆盖 version、WAL/snapshot 状态、key count 和 read-only diagnostics，并保持 `CHECKJSON GET` 语义不变。

该计划合理，本轮只执行 mini-kv / C++ 侧，不修改 Node 或 Java。

## 本版目标

新增命令：

```text
STORAGEJSON
```

关键边界：

```text
read_only=true
execution_allowed=false
store.order_authoritative=false
diagnostics.write_commands_executed=false
diagnostics.order_authoritative=false
```

## 代码改动

```text
CMakeLists.txt -> 0.54.0
src/command.cpp
src/line_editor.cpp
tests/command_tests.cpp
tests/line_editor_tests.cpp
README.md
代码讲解记录/README.md
代码讲解记录/110-storagejson-evidence-v54.md
a/54/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v54 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
Build files have been written to: D:/C/mini-kv/cmake-build-v54
```

CMake build：

```text
& $cmake --build cmake-build-v54 --parallel 2
[100%] Built target minikv_metrics_file_tests
```

CTest：

```text
& $ctest --test-dir cmake-build-v54 --output-on-failure
100% tests passed, 0 tests failed out of 13
```

真实 server + client smoke：

```text
minikv_server.exe 6420 127.0.0.1
minikv_client.exe 127.0.0.1 6420
STORAGEJSON
GET orderops:1
QUIT
```

结果：

```text
storage_json_seen=True
get_after_storagejson_nil=True
quit_seen=True
```

## 图片记录

本版关键命令截图保存到：

```text
a/54/图片/01-cmake-configure.png
a/54/图片/02-cmake-build.png
a/54/图片/03-ctest.png
a/54/图片/04-command-storagejson-tests.png
a/54/图片/05-storagejson-smoke.png
a/54/图片/06-version-docs-check.png
a/54/图片/07-code-explanation-check.png
a/54/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不执行 mini-kv 写命令作为计划证据
不新增订单权威数据存储
不新增集群、事务或权限系统
```

## 清理记录

结束前已清理本轮临时目录和后台进程：

```text
tmp-v54-logs
cmake-build-v54
mini-kv server/client 进程
```
