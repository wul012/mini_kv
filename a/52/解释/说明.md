# 第五十二版运行调试归档说明

本轮版本目标：

```text
按最新跨项目计划，为 CHECKJSON 增加稳定 fixture / sample response。
样本用于后续 Node fixture-driven smoke，减少手写 mock 和真实 mini-kv contract 格式漂移。
```

本轮只产出 mini-kv 自己的 fixture/sample 和测试防漂移机制，不修改 Node 或 Java，不执行 mini-kv 写命令，不新增事务，不新增集群。

最新计划读取：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v71-post-execution-contract-roadmap.md
```

---

# 1. 01-cmake-configure.png

对应命令：

```powershell
$cmake = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\cmake.exe'
$env:PATH = 'D:\CLION\CLion 2025.1.1\bin\mingw\bin;' + $env:PATH
& $cmake -S . -B build-v52 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

结果：

```text
Exit code: 0
Configuring done
Generating done
Build files have been written to build-v52
```

意义：

```text
CMake 能识别 v52，并生成包含 0.52.0 的构建目录。
```

---

# 2. 02-cmake-build.png

对应命令：

```powershell
& $cmake --build build-v52 --parallel 2
```

结果：

```text
Exit code: 0
所有目标构建通过
```

意义：

```text
新增 fixture 读取测试、CMake 测试编译定义、README 和代码讲解记录没有引入编译问题。
```

---

# 3. 03-ctest.png

对应命令：

```powershell
$ctest = 'D:\CLION\CLion 2025.1.1\bin\cmake\win\x64\bin\ctest.exe'
& $ctest --test-dir build-v52 --output-on-failure
```

结果：

```text
100% tests passed, 0 tests failed out of 13
```

意义：

```text
全量回归测试通过，新增 fixture 精确比对没有破坏已有 Store、Command、WAL、Snapshot、RESP、TCP、client history、line editor 和 metrics file 测试。
```

---

# 4. 04-command-fixture-test.png

对应命令：

```powershell
.\build-v52\minikv_command_tests.exe
```

结果：

```text
Exit code: 0
fixture_path=fixtures/checkjson/set-orderops-write-contract.json
fixture_digest_field=fnv1a64:9f5c7ff14c0ebc5e
```

意义：

```text
command_tests 已读取稳定 CHECKJSON fixture，并确认真实 CHECKJSON SET 响应与样本文件精确一致。
```

---

# 5. 05-fixture-smoke.png

对应流程：

```powershell
启动 minikv_server.exe 6415 127.0.0.1
使用 minikv_client.exe 发送：
  CHECKJSON SET orderops:1 value
  GET orderops:1
  QUIT
读取 fixtures/checkjson/set-orderops-write-contract.json 并和 client 输出比对
```

结果：

```text
SMOKE OK
fixture_exact_match=True
get_after_checkjson_nil=True
quit_seen=True
```

意义：

```text
真实 server + client 链路证明 fixture 与当前 CHECKJSON 运行时格式一致，并且 CHECKJSON SET 没有写入 orderops:1。
```

---

# 6. 06-version-docs-check.png

对应检查：

```powershell
Select-String 检查 CMakeLists.txt、fixtures/checkjson、tests/command_tests.cpp、README.md
```

结果：

```text
能找到 VERSION 0.52.0、MINIKV_SOURCE_DIR、set-orderops-write-contract.json、fixture exact-match、README Version 52。
```

意义：

```text
版本号、fixture 文件、测试防漂移机制和 README 说明已经同轮更新。
```

---

# 7. 07-code-explanation-check.png

对应检查：

```powershell
Select-String 检查 106-checkjson-fixture-sample.md、107-version-52-tests-docs.md 和 代码讲解记录/README.md
```

结果：

```text
代码讲解记录已补充第 106、107 篇，并写入索引。
```

意义：

```text
本轮代码增删改继续和讲解记录同轮闭环。
```

---

# 8. 08-cross-roadmap-check.png

对应检查：

```powershell
读取 D:\nodeproj\orderops-node\docs\plans\README.md
读取 D:\nodeproj\orderops-node\docs\plans\v71-post-execution-contract-roadmap.md
```

结果：

```text
确认最新计划要求 mini-kv v52 产出 CHECKJSON 稳定 fixture / sample response。
确认本轮不需要 Node 执行 mini-kv SET / DEL / EXPIRE，不需要单版大改 Node、Java、mini-kv 三个项目。
```

意义：

```text
本轮版本范围与最新跨项目计划一致，没有越过 mini-kv 自己的项目边界。
```

---

# 清理说明

```text
真实 smoke 启动过的 minikv_server.exe 已停止。
本轮临时日志目录 tmp-v52-logs 在最终清理阶段删除。
build-v52 是本轮验证构建目录，在最终清理阶段删除。
```
