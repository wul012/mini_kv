# v37 运行调试归档说明

本轮版本目标：

```text
为 minikv_server 增加可选 metrics file export，服务长时间 benchmark / soak test 的指标留存。
```

v37 重点验证：

```text
TcpServer::Options 新增 metrics_exporter 回调。
event=server_metrics 继续输出到 logger，同时可额外写入 metrics file。
event=tcp_stop 作为最终指标快照进入 metrics_exporter。
普通连接生命周期事件不进入 metrics_exporter。
server_main 支持 --metrics-file path，启动时截断创建文件并逐行 flush。
真实 server + client smoke 能生成包含 command_breakdown 的 metrics 文件。
代码讲解记录在开发调试同一轮内同步补齐。
```

## 01-cmake-configure.png

- 命令：`cmake -S . -B cmake-build-debug`
- 结果：配置成功，构建文件写入 `cmake-build-debug`。
- 意义：确认 v37 新增的 `MetricsExportHandler`、CLI 参数和测试没有破坏 CMake 工程生成。

## 02-cmake-build.png

- 命令：`cmake --build cmake-build-debug --parallel`
- 结果：全目标编译和链接成功。
- 意义：确认 `metrics_exporter` 回调、`export_metrics_event`、`--metrics-file` 文件写入逻辑都能通过实际编译。

## 03-ctest.png

- 命令：`ctest --test-dir cmake-build-debug --output-on-failure`
- 结果：12 个测试全部通过。
- 意义：确认 v37 没有破坏 Store、Command、WAL、Snapshot、RESP、TCP server、client history、line editor 等既有能力。

## 04-tcp-server-tests.png

- 命令：`.\cmake-build-debug\minikv_tcp_server_tests.exe`
- 结果：退出码为 0。
- 意义：专项验证 TcpServer exporter 语义：
  - `event=server_metrics` 会进入 `metrics_exporter`。
  - `metrics_interval_ms=20` 会进入导出内容。
  - `event=tcp_stop` 会进入 `metrics_exporter`。
  - `event=tcp_client_accepted` 不会进入 `metrics_exporter`。

## 05-metrics-file-smoke.png

- 命令：真实启动 `minikv_server.exe`，带上 `--metrics-interval-ms 50 --metrics-file tmp-v37-run/server.metrics.log`，再用真实 `minikv_client.exe` 输入命令。
- client 输入流程：

```text
PING
SET metric one
GET metric
STATSJSON
QUIT
```

- 关键结果：
  - client 收到 `PONG`。
  - `SET metric one` 返回 `OK inserted`。
  - `GET metric` 返回 `one`。
  - `STATSJSON` 返回 JSON，且包含 command metrics。
  - client 收到 `BYE`。
  - metrics 文件包含 `event=server_metrics`、`total_commands=`、`command_breakdown=` 和 `metrics_interval_ms=50`。
  - server stdout 的 `event=server_start` 显示 `metrics_file="..."`。

调试意义：

```text
单元测试覆盖 exporter 选择逻辑，真实 smoke 证明 CLI 文件导出和 server/client 运行闭环都可用。
```

## 06-version-docs-check.png

- 命令：PowerShell 一致性检查。
- 检查项：
  - `git diff --check`
  - `CMakeLists.txt` 版本为 `0.37.0`
  - README 当前版本为 `Version 37`
  - README 记录 `--metrics-file`
  - `include/minikv/tcp_server.hpp` 声明 `MetricsExportHandler`
  - `src/tcp_server.cpp` 包含 `export_metrics_event`
  - `src/server_main.cpp` 解析 `--metrics-file`
  - `tests/tcp_server_tests.cpp` 覆盖 `metrics_exports`
  - `a/37/解释/说明.md` 已创建
- 结果：全部 PASS。
- 意义：确认版本、文档、核心代码、测试入口和归档说明已经同步到 v37。

## 07-code-explanation-check.png

- 命令：代码讲解记录一致性检查。
- 检查项：
  - `代码讲解记录/76-server-metrics-file-export.md` 已创建。
  - `代码讲解记录/77-version-37-tests-docs.md` 已创建。
  - `代码讲解记录/README.md` 已列出 76 和 77。
  - `代码讲解记录/README.md` 包含“第三十七版补充理解”。
  - 讲解记录解释了 `MetricsExportHandler`、`export_metrics_event`、`--metrics-file` 和 `a/37` 归档。
- 结果：全部 PASS。
- 意义：确认 v37 的代码讲解记录已经跟随开发调试同轮补齐。

## 自动化说明

- 本轮按用户要求创建并调整了 Codex 自动化“mini-kv 自动连续开发”。
- 当前计划为每 30 分钟重复检测一次。
- 自动化规则：当前版本没完成就继续当前版本；当前版本完成且工作区干净后，再从 Roadmap 或未完成事项继续推进下一版。

## 清理说明

- `tmp-v37-run` 只用于保存本轮真实 metrics file smoke 的临时输入、client 输出、server 输出和 metrics 文件，最终回复前删除。
- 本轮 smoke 启动过真实 `minikv_server.exe` 进程，验证结束后已停止。
- 关键命令输出保留在 `a/37/图片/`。
- 本说明保留在 `a/37/解释/说明.md`。
