# 第五十三版运行调试归档说明

## 计划依据

本版推进前重新读取最新计划：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v75-post-fixture-diagnostics-roadmap.md
```

计划对 mini-kv v53 的目标是新增一个 `CHECKJSON` read-command sample。结合 mini-kv 当前真实语义，`GET` 的样本保留：

```text
side_effects=["store_read"]
side_effect_count=1
```

因为当前 `EXPLAINJSON` / `CHECKJSON` 会把读取 Store 作为可观测 side effect。这个语义比把读操作手写为空数组更准确，也更适合 Node v78 识别 read scenario。

## 本版目标

新增 read-command fixture：

```text
fixtures/checkjson/get-orderops-read-contract.json
```

对应命令：

```text
CHECKJSON GET orderops:1
```

关键字段：

```text
read_only=true
execution_allowed=false
command=GET
write_command=false
side_effects=["store_read"]
side_effect_count=1
wal.durability=not_applicable
warnings=["not a write command"]
```

## 代码改动

```text
CMakeLists.txt -> 0.53.0
fixtures/checkjson/get-orderops-read-contract.json
tests/command_tests.cpp
README.md
fixtures/checkjson/README.md
代码讲解记录/README.md
代码讲解记录/108-checkjson-read-fixture-sample.md
代码讲解记录/109-version-53-tests-docs.md
a/53/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v53 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
Build files have been written to: D:/C/mini-kv/cmake-build-v53
```

CMake build：

```text
& $cmake --build cmake-build-v53 --parallel 2
[100%] Built target minikv_metrics_file_tests
```

CTest：

```text
& $ctest --test-dir cmake-build-v53 --output-on-failure
100% tests passed, 0 tests failed out of 13
```

command fixture test：

```text
.\cmake-build-v53\minikv_command_tests.exe
write_fixture_digest=fnv1a64:9f5c7ff14c0ebc5e
read_fixture_digest=fnv1a64:e8d7a3d61e2a8111
read_fixture_side_effects=["store_read"]
read_fixture_side_effect_count=1
```

真实 CLI smoke：

```text
minikv_server.exe 6416
minikv_client.exe 127.0.0.1 6416
CHECKJSON GET orderops:1
GET orderops:1
QUIT
```

结果：

```text
fixture_exact_match=True
get_after_checkjson_nil=True
quit_seen=True
```

## 图片记录

已保存：

```text
a/53/图片/01-cmake-configure.png
a/53/图片/02-cmake-build.png
a/53/图片/03-ctest.png
a/53/图片/04-command-fixture-tests.png
a/53/图片/05-read-fixture-smoke.png
a/53/图片/06-version-docs-check.png
a/53/图片/07-code-explanation-check.png
a/53/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不执行 mini-kv SET / DEL / EXPIRE
不新增真实 execution endpoint
不新增数据库或网络依赖
```

## 清理记录

已执行清理：

```text
build-v53 exists=False
cmake-build-v53 exists=False
tmp-v53-logs exists=False
mini-kv processes=0
```
