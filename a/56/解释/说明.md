# 第五十六版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v99-production-readiness-v2-roadmap.md
D:\nodeproj\orderops-node\docs\plans\v95-production-gap-roadmap.md
```

最新有效计划 `v99-production-readiness-v2-roadmap.md` 显示 Node v100-v102 已收口，下一步是 Java v47 + mini-kv v56。mini-kv v56 的目标是补 recovery evidence 的说明 endpoint / fixture index。

mini-kv 当前没有 HTTP endpoint 层，本轮选择 fixture index，不新增 server command，不改变核心协议。

## 本版目标

新增稳定索引：

```text
fixtures/recovery/index.json
```

它说明：

```text
当前有哪些 recovery evidence fixture
fixture 相对路径是什么
evidence version 是什么
digest 是什么
下游 Node v103 如何消费
哪些边界不能突破
```

## 代码改动

```text
CMakeLists.txt -> 0.56.0
fixtures/recovery/index.json
fixtures/recovery/README.md
tests/recovery_fixture_index_tests.cpp
README.md
代码讲解记录/README.md
代码讲解记录/112-recovery-fixture-index-v56.md
a/56/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v56 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
Build files have been written to: D:/C/mini-kv/cmake-build-v56
```

CMake build：

```text
& $cmake --build cmake-build-v56 --parallel 2
[100%] Built target minikv_recovery_fixture_index_tests
```

CTest：

```text
& $ctest --test-dir cmake-build-v56 --output-on-failure
100% tests passed, 0 tests failed out of 15
```

新测试单跑：

```text
.\cmake-build-v56\minikv_recovery_fixture_index_tests.exe
exit_code=0
```

fixture index smoke：

```text
index_version=mini-kv-recovery-fixtures.v1
fixture_count=1
path=fixtures/recovery/restart-recovery-evidence.json
digest=fnv1a64:0991cf4e41a8005a
consumer_hint=Node v103 production readiness summary v3
```

## 图片记录

本版关键命令截图保存到：

```text
a/56/图片/01-cmake-configure.png
a/56/图片/02-cmake-build.png
a/56/图片/03-ctest.png
a/56/图片/04-recovery-index-test.png
a/56/图片/05-recovery-index-smoke.png
a/56/图片/06-version-docs-check.png
a/56/图片/07-code-explanation-check.png
a/56/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不新增 server 命令
不新增写命令
不改变 WAL / snapshot 核心协议
不让 mini-kv 承担订单权威数据
```

## 清理记录

结束前已清理：

```text
tmp-v56-logs
cmake-build-v56
mini-kv server/client/test 进程
```
