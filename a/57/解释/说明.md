# 第五十七版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v103-production-auth-audit-roadmap.md
D:\nodeproj\orderops-node\docs\plans\v99-production-readiness-v2-roadmap.md
```

最新有效计划 `v103-production-auth-audit-roadmap.md` 显示 Node v104-v106 已收口，下一步是 Java v48 + mini-kv v57。mini-kv v57 的目标是增强 recovery fixture index，补 restart/replay cost 或 retention boundary 说明字段。

该计划合理。本轮只执行 mini-kv / C++ 侧，不修改 Node 或 Java。

## 本版目标

增强：

```text
fixtures/recovery/index.json
```

新增说明：

```text
restart_replay_cost
retention_boundary
Node v107 consumer_hint
```

## 代码改动

```text
CMakeLists.txt -> 0.57.0
fixtures/recovery/index.json
fixtures/recovery/README.md
tests/recovery_fixture_index_tests.cpp
README.md
代码讲解记录_生产雏形阶段/README.md
代码讲解记录_生产雏形阶段/113-version-57-recovery-index-boundaries.md
a/57/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v57 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

CMake build：

```text
& $cmake --build cmake-build-v57 --parallel 2
```

CTest：

```text
& $ctest --test-dir cmake-build-v57 --output-on-failure
100% tests passed, 0 tests failed out of 15
```

新测试单跑：

```text
.\cmake-build-v57\minikv_recovery_fixture_index_tests.exe
exit_code=0
```

fixture index smoke：

```text
restart_replay_cost=True
wal_records_replayed=1
retention_boundary=True
production_retention_policy=not_defined
consumer_hint=Node v107 production readiness summary v4
```

## 图片记录

本版关键命令截图保存到：

```text
a/57/图片/01-cmake-configure.png
a/57/图片/02-cmake-build.png
a/57/图片/03-ctest.png
a/57/图片/04-recovery-index-boundary-test.png
a/57/图片/05-recovery-index-boundary-smoke.png
a/57/图片/06-version-docs-check.png
a/57/图片/07-code-explanation-check.png
a/57/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不新增 server 命令
不新增写命令
不改动 WAL / snapshot 核心协议
不定义生产 retention / backup / rotation policy
不让 mini-kv 承担订单权威数据
```

## 清理记录

结束前清理：

```text
tmp-v57-logs
cmake-build-v57
本轮未保留后台服务进程
```
