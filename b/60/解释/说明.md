# 第六十版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v156-post-read-only-window-capture-roadmap.md
```

最新有效计划显示：Node v157、v158 已完成，下一步是 Java v51 + mini-kv v60 一起补只读 capture 后的证据解释增强。mini-kv v60 的目标是补 `INFOJSON` / `STATSJSON` 字段说明和 read-only smoke 字段解释。

该计划合理。本轮只执行 mini-kv / C++ 侧，不修改 Node 或 Java。

## 本版目标

新增只读字段说明证据：

```text
fixtures/readonly/runtime-read-field-guide.json
```

它解释：

```text
INFOJSON stable fields
INFOJSON dynamic fields
STATSJSON stable fields
STATSJSON dynamic fields
read-only smoke allowed commands
write commands not used
skipped/mixed evidence is not production pass
```

## 代码改动

```text
CMakeLists.txt -> 0.60.0
fixtures/readonly/runtime-read-field-guide.json
fixtures/readonly/index.json
fixtures/readonly/infojson-empty-inline.json
fixtures/readonly/README.md
tests/readonly_fixture_tests.cpp
README.md
代码讲解记录_生产雏形阶段/README.md
代码讲解记录_生产雏形阶段/116-version-60-runtime-read-field-guide.md
b/60/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v60 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

CMake build：

```text
& $cmake --build cmake-build-v60 --parallel 2
```

CTest：

```text
& $ctest --test-dir cmake-build-v60 --output-on-failure
100% tests passed, 0 tests failed out of 16
```

只读 fixture 测试单跑：

```text
.\cmake-build-v60\minikv_readonly_fixture_tests.exe
exit_code=0
```

runtime field guide smoke：

```text
guide_version=mini-kv-runtime-read-field-guide.v1
read_only=True
execution_allowed=False
order_authoritative=False
consumer_hint=Node v159 read-only capture release evidence review
commands=INFOJSON,STATSJSON
allowed_smoke=INFOJSON,STATSJSON,CHECKJSON GET orderops:1,HEALTH,QUIT
write_commands_not_used=SET,DEL,EXPIRE
index_fixture_count=5
index_has_field_guide=True
```

真实 server 只读 smoke：

```text
.\cmake-build-v60\minikv_server.exe 6400 127.0.0.1
.\cmake-build-v60\minikv_client.exe 127.0.0.1 6400 5000 --connect-retries 10 --retry-delay-ms 100
INFOJSON
STATSJSON
CHECKJSON GET orderops:1
HEALTH
QUIT
```

本轮 smoke 只发送只读命令和只读契约检查，未执行 `SET` / `DEL` / `EXPIRE` 等写命令。验证结束后已停止 server 进程。

## 图片记录

本版关键命令截图保存到：

```text
b/60/图片/01-cmake-configure.png
b/60/图片/02-cmake-build.png
b/60/图片/03-ctest.png
b/60/图片/04-readonly-fixture-test.png
b/60/图片/05-runtime-field-guide-smoke.png
b/60/图片/06-real-server-readonly-smoke.png
b/60/图片/07-version-docs-check.png
b/60/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不新增 server 命令
不新增写命令
不执行 mini-kv 写命令 smoke
不打开 UPSTREAM_ACTIONS_ENABLED=true
不把 skipped/mixed evidence 标记为 production pass
不让 mini-kv 承担订单权威数据
```

## 清理记录

结束前清理：

```text
tmp-v60-logs
cmake-build-v60
本轮真实 smoke server 已停止
```
