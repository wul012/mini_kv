# 第五十八版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v149-post-production-pass-evidence-roadmap.md
```

最新有效计划显示：Node v150-v151 已完成，当前下一步是 Java v49 + mini-kv v58 一起补只读 evidence。mini-kv v58 的目标是补 `CHECKJSON`、`INFOJSON`、`STATSJSON` 只读样本与说明。

该计划合理。本轮只执行 mini-kv / C++ 侧，不修改 Node 或 Java。

## 本版目标

新增只读 evidence 包：

```text
fixtures/readonly/index.json
fixtures/readonly/infojson-empty-inline.json
fixtures/readonly/statsjson-empty-inline.json
fixtures/readonly/README.md
```

它把已有 `fixtures/checkjson/` 样本和新增 `INFOJSON` / `STATSJSON` 样本统一为 Node v152/v153 可引用的只读证据入口。

## 代码改动

```text
CMakeLists.txt -> 0.58.0，并注册 readonly_fixture_tests
tests/readonly_fixture_tests.cpp
fixtures/readonly/index.json
fixtures/readonly/infojson-empty-inline.json
fixtures/readonly/statsjson-empty-inline.json
fixtures/readonly/README.md
fixtures/checkjson/README.md
README.md
代码讲解记录_生产雏形阶段/README.md
代码讲解记录_生产雏形阶段/114-version-58-readonly-evidence-fixtures.md
b/58/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v58 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

CMake build：

```text
& $cmake --build cmake-build-v58 --parallel 2
```

CTest：

```text
& $ctest --test-dir cmake-build-v58 --output-on-failure
100% tests passed, 0 tests failed out of 16
```

新测试单跑：

```text
.\cmake-build-v58\minikv_readonly_fixture_tests.exe
exit_code=0
```

fixture index smoke：

```text
index_version=mini-kv-readonly-fixtures.v1
read_only=True
execution_allowed=False
write_execution_allowed=False
order_authoritative=False
fixture_count=4
```

真实 server 只读 smoke：

```text
.\cmake-build-v58\minikv_server.exe 6398 127.0.0.1
.\cmake-build-v58\minikv_client.exe 127.0.0.1 6398
INFOJSON
STATSJSON
CHECKJSON SET orderops:1 value
HEALTH
QUIT
```

本轮 smoke 只发送只读命令和只读契约检查，未执行 `SET` / `DEL` / `EXPIRE` 等写命令。验证结束后已停止 server 进程。

## 图片记录

本版关键命令截图保存到：

```text
b/58/图片/01-cmake-configure.png
b/58/图片/02-cmake-build.png
b/58/图片/03-ctest.png
b/58/图片/04-readonly-fixture-test.png
b/58/图片/05-readonly-fixture-smoke.png
b/58/图片/06-real-server-readonly-smoke.png
b/58/图片/07-version-docs-check.png
b/58/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不新增 server 命令
不新增写命令
不执行 mini-kv 写命令 smoke
不打开 UPSTREAM_ACTIONS_ENABLED=true
不把只读样本标记为 production pass evidence
不让 mini-kv 承担订单权威数据
```

## 清理记录

结束前清理：

```text
tmp-v58-logs
cmake-build-v58
本轮未保留后台服务进程
```
