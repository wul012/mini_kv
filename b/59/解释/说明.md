# 第五十九版运行调试归档说明

## 计划依据

本轮推进前只读检查了：

```text
D:\nodeproj\orderops-node\docs\plans\README.md
D:\nodeproj\orderops-node\docs\plans\v153-post-operator-runbook-roadmap.md
```

最新有效计划显示：Node v154-v155 已完成，下一步是 Java v50 + mini-kv v59。mini-kv v59 的目标是补强 `INFOJSON` / `STATSJSON` smoke 输出字段稳定性。

该计划合理。本轮只执行 mini-kv / C++ 侧，不修改 Node 或 Java。

## 本版目标

增强运行时只读 JSON 输出：

```text
INFOJSON
STATSJSON
```

新增稳定自描述字段：

```text
schema_version
read_only
execution_allowed
order_authoritative
evidence_type
diagnostics.write_commands_executed
diagnostics.dynamic_fields
```

## 代码改动

```text
CMakeLists.txt -> 0.59.0
src/command.cpp
tests/command_tests.cpp
tests/readonly_fixture_tests.cpp
fixtures/readonly/index.json
fixtures/readonly/infojson-empty-inline.json
fixtures/readonly/statsjson-empty-inline.json
fixtures/readonly/README.md
README.md
代码讲解记录_生产雏形阶段/README.md
代码讲解记录_生产雏形阶段/115-version-59-runtime-read-self-description.md
b/59/解释/说明.md
```

## 验证记录

CMake configure：

```text
& $cmake -S . -B cmake-build-v59 -G 'MinGW Makefiles' -DCMAKE_BUILD_TYPE=Debug
```

CMake build：

```text
& $cmake --build cmake-build-v59 --parallel 2
```

CTest：

```text
& $ctest --test-dir cmake-build-v59 --output-on-failure
100% tests passed, 0 tests failed out of 16
```

新测试单跑：

```text
.\cmake-build-v59\minikv_readonly_fixture_tests.exe
exit_code=0
```

runtime self-description smoke：

```text
INFOJSON -> schema_version=1 read_only=true execution_allowed=false order_authoritative=false evidence_type=runtime_identity
STATSJSON -> schema_version=1 read_only=true execution_allowed=false order_authoritative=false evidence_type=runtime_metrics
```

真实 server 只读 smoke：

```text
.\cmake-build-v59\minikv_server.exe 6399 127.0.0.1
.\cmake-build-v59\minikv_client.exe 127.0.0.1 6399
INFOJSON
STATSJSON
CHECKJSON GET orderops:1
HEALTH
QUIT
```

本轮 smoke 只发送只读命令和只读契约检查，未执行 `SET` / `DEL` / `EXPIRE` 等写命令。验证结束后已停止 server 进程。

## 图片记录

本版关键命令截图保存到：

```text
b/59/图片/01-cmake-configure.png
b/59/图片/02-cmake-build.png
b/59/图片/03-ctest.png
b/59/图片/04-readonly-fixture-test.png
b/59/图片/05-runtime-self-description-smoke.png
b/59/图片/06-real-server-readonly-smoke.png
b/59/图片/07-version-docs-check.png
b/59/图片/08-cross-roadmap-check.png
```

## 边界

本版不做：

```text
不修改 Node
不修改 Java
不新增 server 命令
不新增写命令
不执行 mini-kv 写命令 smoke
不打开 UPSTREAM_ACTIONS_ENABLED=true
不把只读样本标记为 production pass evidence
不让 mini-kv 承担订单权威数据
```

## 清理记录

结束前清理：

```text
tmp-v59-logs
cmake-build-v59
本轮未保留后台服务进程
```
