# Step 08：执行结果与解释

状态：已于 2026-07-13 在 FinalShell 连接的 Linux 主机上完成实跑。

## 实际执行结果

先生成 4191 字节的测试内容和命令脚本：

```text
azureuser@myvps:~$ cd /home/azureuser/mini-kv
azureuser@myvps:~/mini-kv$ rm -f /tmp/osfs-step08.img /tmp/osfs-step08.txt /tmp/osfs-step08.log
azureuser@myvps:~/mini-kv$
azureuser@myvps:~/mini-kv$ python3 - <<'PY'
> from pathlib import Path
>
> payload = "X" * 4088 + "DIRECT8|INDIRECT-BEGIN|" + "Y" * 80
> commands = [
>     "LOGIN alice alice123",
>     "CREATE bigfile",
>     "OPEN bigfile w",
>     f"WRITE 3 {payload}",
>     "TELL 3",
>     "CLOSE 3",
>     "OPEN bigfile r",
>     "SEEK 4 4088",
>     "READ 4 64",
>     "CLOSE 4",
>     "FSCK",
>     "QUIT",
> ]
> Path("/tmp/osfs-step08.txt").write_text("\n".join(commands) + "\n", encoding="utf-8")
> print("payload_bytes=", len(payload))
> PY
payload_bytes= 4191
```

这里的长度不是随意选取的。OSFS 每块为 512 字节，八个直接块合计 4096 字节；4191 已超过该容量，文件后半部分必须经过一级间接块保存。

随后运行 OSFS，并从完整日志中提取边界读写和一致性检查结果：

```text
azureuser@myvps:~/mini-kv$
azureuser@myvps:~/mini-kv$ ./build-osfs-linux/minikv_osfs \
>   --disk /tmp/osfs-step08.img \
>   --format --blocks 192 \
>   --script /tmp/osfs-step08.txt \
>   > /tmp/osfs-step08.log
azureuser@myvps:~/mini-kv$
azureuser@myvps:~/mini-kv$ echo

azureuser@myvps:~/mini-kv$ echo "[1] Direct-to-indirect boundary evidence"
[1] Direct-to-indirect boundary evidence
azureuser@myvps:~/mini-kv$ grep -E '^(OK wrote|fd=3 |OK seek|DIRECT8\|INDIRECT-BEGIN|FSCK OK|BYE)' \
>   /tmp/osfs-step08.log
OK wrote 4191 bytes offset=4191
fd=3 read_offset=0 write_offset=4191
OK seek 4 4088
DIRECT8|INDIRECT-BEGIN|YYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYYY
FSCK OK checks=6
BYE
azureuser@myvps:~/mini-kv$
azureuser@myvps:~/mini-kv$ rm -f /tmp/osfs-step08.img /tmp/osfs-step08.txt /tmp/osfs-step08.log
azureuser@myvps:~/mini-kv$
azureuser@myvps:~/mini-kv$ echo

azureuser@myvps:~/mini-kv$ echo "========== Step 8 done =========="
```

`write_offset=4191` 与实际载荷长度一致。读取从 offset 4088 开始：`DIRECT8|` 正好占用直接块区域末尾的 8 字节，紧接着的 `INDIRECT-BEGIN|` 位于 offset 4096 之后。两段内容连续返回，说明读路径能够从第八个直接块平滑切换到一级间接块。

最后的 `FSCK OK checks=6` 表明跨边界写入后，超级块计数、inode、目录以及块位图等检查仍然通过。临时镜像、命令脚本和日志随后已删除。

## 结论

本步骤的四项验收信号均已出现：写入长度超过 4096 字节、写偏移达到 4191、跨边界标记连续可读、FSCK 返回 OK。Step 08 完成。
