# 代码讲解文档整治批次记录

## 第一批：入口治理和增量收口

范围：

- 新增 `代码讲解记录_总索引.md`。
- 新增 `代码讲解记录_治理/README.md`。
- 新增 `代码讲解记录_治理/production-readiness-walkthrough-template.md`。
- 新增 `代码讲解记录_生产雏形阶段_第三册/README_分段入口.md`。
- 新增 `代码讲解记录_生产雏形阶段_第四册/README.md`。
- 在第三册 README 顶部标注归档状态。
- 在 `AGENTS.md` 中补充 Node-style walkthrough governance，明确“可以没有讲解；有就必须参照模式”。

判断：

- 本批不移动第三册 774 篇历史文件，避免制造大规模 rename diff。
- 本批先解决“从哪里进入、以后写到哪里、按什么格式写”的问题。
- 本批是文档治理，不新增 mini-kv 命令、不改 C++ 运行行为、不改 fixture 证据、不启动服务。

验收：

- 新增文档可从总索引进入。
- 第三册有分段入口。
- 第四册成为新的活跃讲解册。
- `git diff --check` 无 trailing whitespace。

## 第二批建议：第三册主题索引

建议后续做，但不在第一批硬做：

- 按主题补充第三册主题索引，例如 command catalog、route preview、candidate package、production live capture、type barrel split、credential resolver。
- 每个主题索引只列关键版本范围和代表文档，不复制 774 条完整目录。
- 不移动旧文件。

## 第三批建议：旧 README 瘦身

建议后续在确认无外部链接依赖后再做：

- 将第三册巨型 README 拆成 `README_legacy_full.md` 和短 README。
- 保留旧入口跳转，避免破坏历史链接。
- 拆分前先用 `rg` 查内部链接引用，避免误伤。

## 第四批建议：新讲解抽样整改

建议从最近 20 篇开始：

- 检查是否符合 Node-style production readiness pattern。
- 对不符合但仍有维护价值的讲解进行局部补强。
- 对纯流水账且价值低的讲解，不重写全文，只在索引中标记为历史记录。
